# 项目进度

## 当前任务

| 任务 | 状态 | 负责人 | 完成条件 | 证据 ID | 最后更新 |
|---|---|---|---|---|---|
| M0 设计基线 | 已完成 | Codex | 十份主题设计文档与索引完整、一致、可追踪 | E-006,E-007 | 2026-08-05 |
| M1 工程与平台地基 | 已完成 | Codex | 模块化结构、数据库、租户、组织、权限、审计、事件与测试基座通过 Gate | E-008 | 2026-08-05 |
| M2 管理纵向闭环 | 已完成 | Codex | 网页可完成目标—项目—风险—决策—行动闭环 | E-009,E-010 | 2026-08-05 |
| M3 Agent 平台 | 已完成 | Codex | 上下文、引用、工具、策略、确认、执行和评测通过安全 Gate | E-011,E-012 | 2026-08-05 |
| M4 三平台连接器 | 已完成（协议 Gate） | Codex + 用户授权 | 协议/Fixture 测试通过；真实测试企业作为独立外部验收项 | E-013,E-014 | 2026-08-05 |
| M5 流程、审批、会议和知识 | 已完成 | Codex | 持久流程、审批、会议确认、权限知识与 Agent 辅助通过 Gate | E-015,E-016 | 2026-08-05 |
| M6 数据智能、经营与人才 | 已完成 | Codex | 战略经营、指标驾驶舱、组织责任与人才保护通过 Gate | E-017,E-018 | 2026-08-05 |
| M7 生产化与试点准备 | 已完成（本地工程 Gate） | Codex + 用户外部授权 | OIDC、Secret、监控、备份恢复内核、容器、安装与试点材料通过；真实环境继续由 A-005/A-008/A-009 跟踪 | E-019,E-020 | 2026-08-05 |
| M8 自建客户端与最终验收 | 已完成（本地工程 Gate） | Codex | 安装型 PWA、设备策略、通知、安全离线草稿、统一客户端 BFF 与全表审计闭环通过 Gate | E-021,E-022 | 2026-08-05 |
| M9 企业管理控制面 | 已完成（本地工程 Gate） | Codex | 权威授权重算、目标立项、组织交接、基线/补偿、管理关注、结项 Gate、决定替代和 73 项需求追踪通过 | E-023,E-024 | 2026-08-05 |
| M10 企业接入验收控制面 | 已完成（本地工程 Gate） | Codex + 企业管理员外部授权 | OIDC/三平台真实预检、企业绑定、追加证据、两阶段测试外发与未知结果停止重试通过本地 Gate | E-025,E-026 | 2026-08-05 |
| M11 运行事实与持久执行 | 已完成（本地工程 Gate） | Codex | Inbox/Outbox/Agent Job 均由持久 Worker 处理并具备租约、重试预算、死信/未知结果、卡片确认、崩溃恢复；生产 UI 无演示事实；readiness/需求 Gate 以行为证据为准 | E-027,E-028 | 2026-08-05 |
| M12 企业管理智能中枢一期 | 已完成（本地工程 Gate） | Codex + 企业管理员外部授权 | 管理节奏、指标语义、组合情景、企业事项、AI 治理和企业微信动作共享统一 API、权限、审计与证据；网页/390×844 和协议 Fixture 通过本地 Gate | E-029,E-030 | 2026-08-05 |
| M13 主对话与实时任务指挥中枢 | 已完成（本地工程 Gate） | Codex | 主对话成为全部企业办公意图的极简入口；任务仅为高频轻量侧栏；LLM 原生路由、实时任务能力和桌面/移动端极简界面通过回归 | E-031,E-032,E-033,E-034,E-036 | 2026-08-12 |
| M14 任务责任与沟通分轨 | 已完成（本地工程 Gate） | Codex | 正式任务定向至个人或部门并经过权限/范围/确认/状态证据门禁；消息推送进入独立公司/部门池，可反馈、不产生任务义务，LLM 通过声明式 Skill/Tool 自主选择 | E-037 | 2026-08-12 |
| M15 任务交接连续性 | 已完成（本地工程 Gate） | Codex | 多人交接以冻结任务/资料快照和接收人签收链传递；签收前原负责人不脱钩，签收时 CAS 原子切换责任，任何历史参与者可查询交接与文件/资料引用；待签收冻结路径已映射为可处理的 409 冲突 | E-038,E-041,E-044 | 2026-08-17 |
| M16 企业微信 AI 权限控制 | 已完成（本地工程 Gate） | Codex + 企业管理员外部授权 | AI 权限面、应用配置 R3 Tool、租户连接绑定、Secret 隔离、可信域名、管理系统只读 API 和权限目录迁移通过；真实 Secret/激活连接/公网回调与 API 调用继续由外部 Gate 跟踪 | E-039 | 2026-08-14 |
| M17 企业微信自建应用消息接口 | 已完成（本地工程 Gate；真实外发待凭据） | Codex + 企业管理员外部授权 | AI 通过 R3 Tool 调用自建应用 API，服务端唯一姓名解析后定向发送；UserID/Secret/token 隔离、幂等、失效成员和异常回执失败关闭；真实平台回执需凭据 | E-040 | 2026-08-14 |
| M18 分级 Agent 记忆与统一办公 Tool 覆盖 | 已完成（本地工程 Gate） | Codex | 对话、上下文、长期、任务、情景记忆按租户/用户/会话/任务作用域持久化；显式长期记忆、共享记忆与删除受权限/确认/版本门禁，RLS 与原子审计可核验；主对话按权限回注记忆并以声明式 Skill/Tool 覆盖治理、经营、知识、会议、流程等办公只读理解 | E-044 | 2026-08-17 |
| M19 Agent 数据边界、版本化交付物与可验证路由 | 已完成（本地工程 Gate） | Codex | 敏感输入、自动记忆和模型出站按分级失败关闭；交接使用带摘要、分类和版本的不可变 Artifact 快照并支持连续继承；模型只接收权限可用的 Skill/Tool，最终输出和实际路由均可验证；迁移、RLS、审计、API、遥测与回归通过 | E-045 | 2026-08-17 |
| M20 Pi Agent 企业统一开发 Runtime | 进行中（本地控制面已实现；生产 Runner Gate 未完成） | Codex | Pi SDK 通过受控 ResourceLoader/Profile/Tool/ModelRuntime 接入；Session、事件、检查点、差异和租户 RLS 可运行；默认沙盒不访问宿主机且生产未配置 Firecracker/Kata 时失败关闭；独立 Runner、MCP Bridge、Skill 签名、审批和崩溃恢复待后续 Gate | E-047（待完整 Gate） | 2026-08-18 |
| M20.1 Pi Adapter 本地纵向切片 | 已完成（开发控制面范围） | Codex | Pi SDK、受控 Tool/Profile、租户 Session API、SSE、Checkpoint、Diff、虚拟沙盒与生产 fail-closed Provider 通过本地测试和构建 | E-047 | 2026-08-18 |
| M20.2 Pi 企业平台实施基线 | 已完成（规划 Gate） | Codex | 五份项目事实文件包含目标架构、F-054～F-075、PI-MOD/PI-API/PI-INT/PI-EVT、M21～M34、Gate、证据和回退规则，且治理校验通过；不改变任何实现状态 | E-048 | 2026-08-19 |
| M21 契约与持久调度基座 | 进行中 | Codex | RunManifest、命令/事件状态机、幂等键、租约队列、取消/超时/重试/未知结果、Runner 内部协议和数据库迁移形成可执行契约；E-064 加固 lease 绑定状态终态、过期 claim 标记、旧 lease 拒绝和安全恢复分流；E-065 在真实临时 PostgreSQL 非表所有者连接下验证 100 并发幂等、跨租户拒绝和过期 lease 接管；E-066 增加显式 Run 状态机/数据库 Guard、0025 rollback/forward、取消幂等、有限重试和死信真实回归；E-067 补充 interrupt/timeout 的调度命令路径和租约回收联动；E-068 补充 abort 到达时立即停止新 claim、先写 draining heartbeat、排队命令保持 attempts=0；E-069 补齐 InMemory/PostgreSQL `listBacklog`，冻结服务端租户作用域、默认未完成状态集、状态过滤、稳定排序和 1～1000 上限，并用非表所有者真实 PostgreSQL 回归；E-070 增加 `PiRunScheduler` claim/release/renew/drain 门面和带当前租约校验的 `complete/fail/deadLetter` 原子终态提交；完整 Gate 仍待持久 Queue 运维、生产长稳与正式回退演练 | E-049,E-050,E-064,E-065,E-066,E-067,E-068,E-069,E-070；待 G-025 | 2026-08-20 |
| M22 独立 Pi Runner 与恢复 | 进行中（本地与真实子进程故障及 bounded drain/soak 切片已实现，Gate 未通过） | Codex | Web 请求只持久化命令；独立 Runner 领取租约、运行 Pi SDK、流式落事件、独立 Supervisor 心跳、run heartbeat、取消/interrupt、timeout、崩溃恢复和优雅排空通过；E-064 验证运行前重排队、运行中 unknown 且不自动重放；E-065 增加真实独立 bundle 子进程 kill/lease-expiry/reclaim；E-067 覆盖 after-claim、Sandbox 创建/限制/网络/挂载、Runtime、prompt/model、Tool、event flush 和 terminal commit 阶段强杀，验证事件连续、Manifest 唯一、已创建 Sandbox 不自动重放且回收后进入 unknown、interrupt/timeout；E-068 增加同步 beginDrain claim barrier、draining heartbeat、已启动 Runtime/已创建资源进入 unknown、无资源 Run 才重排队，以及真实子进程 48 次 bounded soak；E-070 将 Runner claim、heartbeat renew、成功终态和本地 drain admission 接入 `PiRunScheduler`；E-071 将远程 Sandbox 请求绑定短期 Run Token，真实 HTTP Supervisor 夹具校验 Token scope；E-083 以 `PiRunnerWorker` 实际编排 Workspace/Sandbox/Runtime，固定资源清理先于成功终态，清理不确定时 Run/Session/Command 进入 `unknown` 且不自动重放；E-085 修复错误路径清理异常被吞掉的问题，runtime failure 后 cleanup failure 同样不自动重试；测试运行时和 HTTP Supervisor 仍是隔离夹具，完整 Gate 仍待持久 Queue、真实 Firecracker/Kata、24h drain/soak 运维、真实模型/Tool 和生产部署 | E-049,E-050,E-064,E-065,E-066,E-067,E-068,E-070,E-071,E-083,E-085；待 G-026 |
| M23 微 VM 沙盒与受控网络 | 进行中（Firecracker API/进程/cgroup/vsock 适配与独立 Supervisor 已实现，真实隔离 Gate 未通过） | Codex + 平台运维 | SandboxOrchestrator、资源限制、默认拒绝/代理白名单编译、沙盒记录和 Firecracker/Kata Supervisor 适配已形成；E-071 增加短期 Run Token、远程请求身份绑定、Supervisor 回显校验和恢复路径范围重验；E-072 增加 Supervisor HTTP/health/readiness、绑定持久化、Linux/KVM/vsock/cgroup/rootfs/kernel/Secret 预检、Firecracker Unix API 配置、最小环境进程、Guest Agent vsock JSONL、恢复元数据和非 none 网络 fail-closed；E-073 收紧恢复 PID 必须属于 `cgroup.procs`，并覆盖 Guest Agent CONNECT/OK、requestId 相关性和非法握手失败关闭；E-074 增加 VMM 可执行文件身份、network policy digest/字段和 runtime/socket/cgroup 残留复验；E-075 增加内部 Workspace ref allowlist 与 Guest Agent 路径/载荷边界；E-084 确认当前 Windows preflight 为 `not_ready`，缺少 Linux/KVM/vhost-vsock/cgroup/provider/rootfs/kernel/HTTPS endpoint/Run Token；真实微 VM、Guest Agent rootfs、RuntimeClass/seccomp/capability、Network Controller、网络/逃逸攻击矩阵仍待环境 | E-050,E-071,E-072,E-073,E-074,E-075,E-084；待 G-027 | 2026-08-20 |
| M24 Workspace、Git 与 Artifact | 进行中（本地控制面/适配器、Supervisor 状态恢复与六旅程 virtual/local 证据，真实交付 Gate 未通过） | Codex + 平台运维 | 已落地 Workspace/Git/Artifact 领域合同、`0027` 租户 RLS/审计迁移、Postgres/InMemory Store、短期凭据租约、精确 base SHA、临时分支、Checkpoint/Diff、对象版本/摘要/分类/保留期/单对象下载授权、Runner 挂载/清理和只读查询 API；E-076 增加 Remote Workspace endpoint/path/身份及内部 ref 校验、Object Storage digest/size 双校验和无凭据 HTTPS download grant URL；E-077 统一对象存储 put/download/revoke/delete 的真实 tenant/actor/session/run/trace scope，缺少 Run 时失败关闭；E-078 统一 Credential issue/revoke 与 Remote Workspace 请求使用持久 Workspace 的真实 session/run，跨租户/用户/Session/Run 撤销失败关闭；E-079 新增独立 HTTPS Workspace Supervisor、服务端 Forgejo Git lease、真实 checkout/base/分支/Checkpoint/Diff/Push/Cleanup、S3 put/Grant/下载/撤销/删除和跨租户 scope 回归；E-080 增加无密钥 JSON 原子状态、坏状态失败关闭、过期 lease/Grant 清理以及 Supervisor 重建后的 Git diff/S3 Grant 恢复；E-081 增加 `0038` 按租户强制 RLS 状态表、owner lease fencing、version CAS、renew/release 和 DB-backed 入口；E-082 通过 `PiWorkspaceService` 验证六类 virtual/local Vibe Coding 路径、Review 写入拒绝、PR 分支提交和清理失败 unknown；E-083 通过 `PiRunnerWorker` 验证 Prompt/Checkpoint 的 Workspace/Sandbox/Runtime 编排、资源清理先于成功终态和清理不确定时全链路 `unknown`；E-086 增加服务端 Profile/tenant-policy ValidationPlan、受控 test/scan 执行、摘要事件和 tenant/session/run 绑定报告 Artifact，已知测试失败与执行器 unknown 分流且不自动重试；E-087 修正 InMemory retention consumer 仅按 tenant+actor 过期，补充同租户跨员工状态不越界回归；E-088 收紧 Supervisor S3 object key 的幂等与 scope 冲突，拒绝跨 actor 覆盖并验证短期 Grant 过期/撤销；真实多进程状态协调虽有非表所有者 PostgreSQL 状态 store 证据，但真实微 VM checkout、Forgejo/S3、测试/扫描任务、URL 过期/越权攻击矩阵和 G-028 仍待环境 | E-051,E-076,E-077,E-078,E-079,E-080,E-081,E-082,E-083,E-086,E-087,E-088；待 G-027、G-028 | 2026-08-20 |
| M25 Skill/Package/Extension 供应链 | 进行中（本地控制面/签名适配器；G-029 未通过） | Codex + 安全管理员 | 已实现 Skill/Package/Extension Registry、摘要/Ed25519 验证、审核/灰度/撤销、扫描结果权限门禁、受控 ResourceLoader allowlist、Session/RunManifest 资源快照、恶意仓库不发现和 OCI 制品缺失失败关闭；E-089 增加按快照逐项校验的 PiResourceMaterializer、Firecracker/Kata 执行前置、Extension Factory 到真实 EnterpriseResourceLoader 的接线和 Sandbox root 路径绑定；E-090 增加显式 Guest execution boundary 双重校验和 materializer AbortSignal；E-091 修复 PostgreSQL `revoked_at` partial update/重新审批清除语义，保持 rollout/scan 局部更新不误清除撤销状态；真实 OCI 构建/SBOM/SCA/签名服务、G-028 前置和 G-029 仍待环境 | E-052,E-089,E-090,E-091；待 G-029 | 2026-08-20 |
| M26 MCP Bridge 与 Tool Gateway | 进行中（本地控制面已实现；G-030 未通过） | Codex + 集成管理员 | 已实现 MCP Server 注册/探测/Schema 固化、租户绑定、服务端 Secret Broker/OpenBao 适配器、双重授权、注入式出站边界、调用审计、熔断与恢复/撤销；E-092 将 Pi 注册 MCP Tool 绑定当前 Session record.id 与 Runner lease.runId，Runner/Runtime 对 MCP Binding 缺少 Tool Gateway 或 Run scope 失败关闭，并验证审计 scope 连续；E-093 将 Invocation/Audit scope 改为必填、Gateway/Bridge 双重拒绝缺失或错配 scope，PostgreSQL 新审计写入拒绝无效 UUID；E-094 将历史 audit constraint 的 `NOT VALID` 状态暴露为 preproduction readiness blocker，未完成数据治理/constraint validation 不报告通过；真实 OpenBao、测试 MCP、出站代理、生产配额/幂等和 G-029 前置仍待 | E-053,E-054,E-092,E-093,E-094；待 G-030 | 2026-08-20 |
| M27 Policy、Approval 与变更交付 | 进行中（本地 Approval/Change Delivery control plane 与受控 Pi Tool wiring 已实现；G-030/G-031 未通过） | Codex + 安全/发布审批人 | E-055 的 R2/R3 审批内核之上，E-095 已实现 ChangeSet/Checkpoint/Test/Scan evidence 校验、Submission/PR/Merge/Release Proposal、Outbox CAS、租户/操作者 Postgres Store、`0040` 强制 RLS/审计迁移、脱敏 HTTP 控制面和 system-only 外部 dispatch；E-096 增加审批等待态对象版本重验/CAS 激活、`pi-change-delivery` Durable Worker、`0041` Worker 角色、`0042` lease fencing 和默认关闭的服务端 Forgejo Gateway；E-097 增加 ResourceLoader 强制注入的 enterprise policy Extension、R2/R3 持久审批等待、批准后二次对象版本校验、拒绝/漂移/中断/超时 fail-closed，以及 Runner 的 awaiting/resumed/denied 事件和租约心跳；W27-08 以现有租户权威授权事实源实现 Postgres 审批人目录，active/有效期/permission/requester 排除均在服务端校验。Gateway 异常或过期 lease 进入 `unknown` 终态且不自动重放；生产 approver directory 当前为 fail-closed，实际 Pi Tool/模型执行、OpenBao/受控出站、真实 Forgejo/发布目标、生产 Runner 和 G-030/G-031 仍待外部 Gate | E-055,E-095,E-096,E-097；待 E-098,G-030,G-031 | 2026-08-20 |
| M28 Session Tree、Profile 与受限子 Agent | 进行中（本地 Session Tree/Profile/Delegation 控制面；G-032 未通过） | Codex | 已实现 branch/tree/fork/resume/continuity、确定性压缩摘要、Profile Registry、父子 Tool/风险/数据范围/预算交集、深度/并发/循环限制、委派账本和失败关闭 Runner；真实跨进程恢复、子 Agent 执行、Artifact 汇总和 G-032 仍待 | E-056；待 G-032 | 2026-08-20 |
| M29 员工端与治理控制台 | 进行中（本地员工端与治理快照/资源管理已实现；G-033 未通过） | Codex | 已实现员工端 Session/Profile 目录、创建会话、消息入队、SSE 游标重连、运行状态、Session Tree/fork/resume/compact、Diff/Artifact/Checkpoint 只读交付轨、桌面/移动响应式和键盘交互；新增服务端治理快照、资源发布/审核/灰度/撤销/扫描 API、MCP 脱敏摘要和治理控制台；Profile 发布、Policy/Quota、审批执行、submit-change、PR/合并/发布与真实 Runner 仍待 Gate | E-057,E-058；待 G-033 | 2026-08-20 |
| M30 模型网关、可观测、评测与成本 | 进行中（本地控制面已实现；G-034 未通过） | Codex + 平台运维 | 已实现 Model Route/Provider 合同、数据分级授权、用量/成本、Trace/Metric/Evaluation/Regression、Quota Reservation/Consume/Release、RLS 迁移、M30 API 与真实 API 驱动运营视图；至少两类真实模型、OTel、生产配额、Run/Tool 全链路和 G-034 仍待外部环境 | E-059；待 G-034 | 2026-08-20 |
| M31 安全、供应链、韧性与性能 | 进行中（本地安全/韧性控制面已实现；G-035 未通过） | Codex + 安全/运维 | 已实现全局/租户/能力级 Kill Switch、不可变安全事件、提示/仓库不可信封装、容量背压、开发故障注入、租户范围加密备份和发布回退摘要；真实跨租户攻击矩阵、SSRF/DNS/Metadata/逃逸、供应链传播、跨进程故障、长稳、生产灾备和 G-035 仍待外部环境 | E-060；待 G-035 | 2026-08-20 |
| M32 隔离预生产运行 Gate | 进行中（本地预生产控制面已实现；G-036 未通过） | Codex + 企业管理员/运维 | 已实现发布候选/制品摘要、Readiness 快照、Secret Lease 元数据、预生产事件、租户 RLS、Fail-Closed 探针、发布/回退和运营摘要；真实 OIDC、PostgreSQL 角色 RLS、Queue、Forgejo、对象存储、OCI/OpenBao、微 VM、MCP、模型/OTel 全链路和 `/ready=200` 仍待 G-036 | E-061；待 G-036 | 2026-08-20 |
| M33 企业试点 | 进行中（Pilot 本地控制面已实现；真实 Gate 未开始） | 用户 + Codex + 试点团队 | 已实现本地 Pilot/成员/六类 journey/观察/抽检/事故/退出和 fail-closed readiness；真实项目完成新增功能、修 Bug、重构、测试失败修复、审查和 PR 六类旅程，连续运行并形成质量/成本/安全/采纳证据仍待 | E-062；待 G-037 | 2026-08-20 |
| M34 1.0 发布与持续治理 | 进行中（Release Governance 本地控制面已实现；真实 Gate 未开始） | 用户 + 发布委员会 | 已实现本地发布候选、Gate Attestation、风险、双人签字、灰度/回退/撤销、upstream/API 兼容和持续评测；真实所有 P0 Gate、灾备/回滚/事故演练、未决高危清零、发布签字和生产灰度仍待 | E-063；待 G-038 | 2026-08-20 |
| M35 Agent 开发团队工作流 | 完成（本地工程范围） | 技术负责人 + 交付负责人 | 已实现需求五文档自动归档、阶段门禁、主要版本 Diff/功能留档、逐版本功能测试、交付清单、Skill 建议（含测试阶段只读 AgentOps 健康检查）和桌面/移动工作台 | E-100、E-101、G-039 本地 Gate 通过 | 2026-08-26 |
| VastMind 企业微信真实接入 | 进行中（服务器入口已部署；云侧与外部 Gate 待完成） | Codex + 企业管理员 | `office.vastmind.com.cn` 公网 HTTPS、后台自建应用/智能机器人配置、真实身份/消息/回复/卡片与故障旅程全部通过 | E-046（服务器入口）；待 E-035 | 2026-08-17 |

## Pi 企业平台详细实施路径

| 阶段 | 目标版本 | 工作包 | 前置条件 | 退出 Gate | 回退锚点 |
|---|---|---|---|---|---|
| M21 | `0.15.0-pi-runner-spike` | W21-01 固化 OpenAPI/内部协议；W21-02 建 `agent_run_commands`/leases/outbox；W21-03 RunManifest 摘要与状态机；W21-04 幂等、取消、超时、重试预算；W21-05 迁移/RLS/审计；W21-06 契约测试 | E-047、D-021 | G-025：持久命令与协议 Gate | 保留现有内联 Adapter，仅开发环境可用；新队列表和路由由开关隔离，迁移只前向补表 |
| M22 | `0.15.0-pi-runner-spike` | W22-01 独立 Runner 入口；W22-02 租约领取/续租/旧令牌拒绝；W22-03 Pi Session 创建与事件映射；W22-04 取消/中断/超时；W22-05 崩溃重领与未知结果；W22-06 心跳/readiness/优雅排空 | G-025 | G-026：Runner 分离与恢复 Gate | 调度关闭后停止新领用，排空旧 Runner；命令留在队列，不回退已持久事件；必要时恢复至 G-025 只排队模式 |
| M23 | `0.16.0-vibe-coding-mvp` | W23-01 Sandbox Provider；W23-02 Firecracker/Kata 生命周期；W23-03 只读基础镜像与临时盘；W23-04 cgroup/seccomp/capability；W23-05 默认断网与代理白名单；W23-06 DNS/Metadata/宿主逃逸测试；W23-07 清理与残留检测；W23-08 短期 Run Token、Supervisor scope 校验和恢复身份绑定；W23-09 独立 Supervisor HTTP/health/readiness、binding store、结构化 preflight 与部署边界；W23-10 Firecracker Unix API/进程/cgroup/vsock Guest Agent 适配、运行元数据恢复和替换依赖测试 | G-026、可用微 VM 环境 | G-027：执行隔离 Gate | 禁止回退到宿主/容器直跑；Token/Provider/Runtime/Guest Agent/Network 校验失败或 Gate 失败时停用 `execute` Profile，保留只读 Session 与历史事件 |
| M24 | `0.16.0-vibe-coding-mvp` | W24-01 Workspace/Repository 绑定；W24-02 短期 Git 凭据；W24-03 checkout/base SHA 校验；W24-04 临时分支/Checkpoint Commit；W24-05 Diff/测试/扫描；W24-06 Artifact 上传/下载授权；W24-07 清理和保留期 | G-027、Forgejo/S3 测试实例 | G-028：Vibe Coding 交付 Gate；当前只通过本地 Provider/内存对象存储和 PGlite 控制面证据 | 禁止自动推送/PR；失败时保留只读 diff 与本地 checkpoint 摘要，撤销下载授权并销毁 Workspace |
| M25 | `0.17.0-resource-governance` | W25-01 Skill 元数据；W25-02 Package/Extension OCI；W25-03 签名/扫描/SBOM；W25-04 审核/灰度/撤销；W25-05 解析优先级；W25-06 ResourceLoader allowlist；W25-07 Run 快照/缓存失效 | G-028、OCI/签名服务 | G-029：资源供应链 Gate；E-052/E-089/E-090/E-091 已通过本地 Registry/签名/扫描状态、PGlite/Postgres、allowlist、ResourceLoader/Guest boundary 接线和 Postgres release 状态一致性部分证据，真实资源供应链仍未开始 | 未启用未批准资源；撤销/签名/快照失败停止新 Run；Package/Extension 无 OCI materializer 时死信；绝不回退到仓库自动发现 |
| M26 | `0.18.0-mcp-policy-approval` | W26-01 MCP 注册与所有权；W26-02 Schema 探测/固化；W26-03 OAuth/Secret Broker；W26-04 Binding/Scope；W26-05 Tool Gateway 双重校验；W26-06 网络代理/超时/熔断；W26-07 结果分级与审计；E-092/E-093 补齐 Pi 与直接 Gateway 调用的 Session/Run scope 连续性及 PostgreSQL 新审计写入约束；E-094 将历史约束验证状态接入预生产 readiness | G-029、OpenBao、至少一个测试 MCP | G-030：MCP/Tool Gateway Gate | 单个 Server 熔断和撤销，不影响 Pi 核心；恢复到无 MCP Tool 集合，历史调用保留不可改写摘要；scope 约束失败或历史约束未验证时关闭 MCP consumer，不直接回退到无审计调用 |
| M27 | `0.18.0-mcp-policy-approval` | W27-01 风险规则；W27-02 approval proposal/hash/TTL；W27-03 暂停与恢复（实际 Runner/Tool wiring 受 G-030 约束）；W27-04 审批时重新授权/重算版本；W27-05 submit-change/PR（E-095 本地控制面）；W27-06 merge/release proposal（E-095 本地控制面）；W27-07 未知结果/补偿（E-095 terminal unknown/no replay） | G-030、组织审批人与发布策略 | G-031：高风险动作 Gate；E-055/E-095 只证明本地审批/交付控制面、Postgres RLS/CAS、HTTP 安全边界和适配器失败关闭，不替代真实 Tool/MCP/Forgejo/发布依赖 | 停止 Outbox consumer 和所有外部 Gateway；保留 Submission/Proposal/PR 事实与不可改写事件，禁止自动重放；已发生的外部事实不回滚，`unknown` 转人工核对；控制面故障时回退到 M27 审批只读/草稿与 M24 只读 Diff/Checkpoint |
| M28 | `0.19.0-unified-agent-platform` | W28-01 Session branch/tree；W28-02 恢复/压缩摘要；W28-03 Profile Registry；W28-04 子 Agent 委派；W28-05 深度/并发/预算；W28-06 父子上下文与 Artifact 汇总；W28-07 循环/爆炸防护 | G-031 | G-032：多 Agent 与恢复 Gate；E-056 已完成本地控制面回归 | 默认关闭 delegation；撤销/取消/排空本地委派，父 Session 回到最后一致 Checkpoint；真实 Runner/多进程能力未就绪时不降级执行 |
| M29 | `0.19.0-unified-agent-platform` | W29-01 员工对话（本地已实现）；W29-02 SSE 游标重连（本地已实现）；W29-03 Diff/Artifact/Checkpoint 视图（本地已实现）；W29-04 审批交互（待 G-030/G-031）；W29-05 Session Tree（本地已实现）；W29-06 Registry 管理快照与资源动作（本地已实现），Policy/Quota/Profile 发布仍待；W29-07 移动/PWA 可访问性（本地已验证） | G-032、稳定 API 合同 | G-033：用户与治理体验 Gate；E-057/E-058 只证明本地员工端和治理控制面切片，不证明生产端到端 | UI 按 API capability 回退；写入口可独立关闭，保留只读观察；不得通过前端隐藏代替服务端撤权；G-033 失败时回退到只读历史、资源摘要和 capability 状态 |
| M30 | `0.20.0-security-candidate` | W30-01 Model Gateway（已实现本地）；W30-02 数据分类路由（已实现）；W30-03 token/cost/latency（已实现）；W30-04 Trace/Metric/Log（本地摘要已实现）；W30-05 Eval 数据集/回归（本地结果/告警已实现）；W30-06 Quota/预算/限流（本地原子预留已实现）；W30-07 告警/运营视图（已实现真实 API/UI） | G-033、模型与遥测测试环境；真实 Provider/OTel/生产限额仍缺 | G-034：模型治理与运营 Gate | 模型路由回退到上一个批准策略；超预算停止新 Run；遥测故障按数据等级 fail-closed；当前批次回退到 M29 只读治理/员工端与已批准能力摘要 |
| M31 | `0.20.0-security-candidate` | W31-01 跨租户矩阵（本地 Kill Switch/恢复 Adapter 隔离已实现）；W31-02 提示注入/恶意仓库（本地不可信封装已实现）；W31-03 SSRF/DNS/Metadata（真实攻击矩阵待环境）；W31-04 沙盒逃逸/DoS（真实微 VM/资源长稳待环境）；W31-05 供应链/签名撤销（复用 M25 控制面，传播窗口待环境）；W31-06 Queue/DB/Object 故障（本地故障计划和安全事件已实现）；W31-07 性能/容量/背压（本地容量租约已实现，生产压测待环境）；W31-08 备份/恢复/升级回滚（本地 AES-GCM/确认/回退动作已实现，生产灾备待环境） | G-034、隔离安全测试环境、最终候选制品 | G-035：安全与韧性 Gate；E-060 只证明本地控制面，不推进真实 Gate | 任何隔离或越权失败立即 no-go；回退最近签名镜像/策略/Schema，轮换受影响凭据并隔离证据；本地控制面失败时停用执行能力并保留取证 |
| M32 | `0.21.0-preproduction` | W32-01 已实现发布候选/版本、image/manifest/signature/SBOM digest 和幂等动作摘要；W32-02 已实现 `PiPreproductionService`/FailClosed Probe、Readiness 检查和失败摘要；W32-03 已实现 Secret Lease 租户范围、TTL、引用 digest、撤销和不回显；W32-04 已实现 `0034` 四表 Postgres/InMemory Store、FORCE RLS、原子审计事件；W32-05 已实现 promote/rollback、旧 active 自动回退和运营快照；W32-06 已实现 HTTP API、Schema、错误映射、M32 单元/PGlite/API 隔离测试和运营 UI；W32-07 真实 OIDC/基础设施/备份/轮换/蓝绿 readiness 运行手册仍待外部环境 | G-035、预生产资源/负责人 | G-036：真实预生产 Gate；E-061 只证明本地控制面和安全边界 | 蓝绿回退至上一签名版本；数据库采用兼容窗口；撤销外部下载与运行凭据；`/ready` 立即降为 503；本地 FailClosed Probe 不得替代真实探针 |
| M33 | `1.0.0-pilot-rc` | W33-01 `PiPilotService` 建立 Pilot/成员/项目范围/窗口/退出策略；W33-02 固定六类 journey，记录 verified/pending/rejected 样本 digest 与 Run/PR/Artifact 摘要；W33-03 记录稳定性、质量、成本、安全、采纳观察和最小数据抽检；W33-04 记录脱敏事故、人工核对和退出；W33-05 汇总 fail-closed readiness；W33-06 运营摘要和只读导出 | G-036、试点授权；本地仅允许受控 Fixture/注入 Probe | G-037：企业试点 Gate；E-062 只证明本地 Pilot 控制面 | 停止新会话，原子撤销 Pilot capability，保留只读历史/代码 Artifact 摘要和事件，导出证据并恢复人工开发流程；不得伪造真实旅程或采纳率 |
| M34 | `1.0.0` | W34-01 `PiReleaseGovernanceService` 创建发布候选并冻结 upstream/API/Schema/制品/SBOM/回退摘要；W34-02 登记 G-025～G-037 Gate Attestation、证据有效期和风险清单；W34-03 双人职责分离签字与发布 Gate evaluation；W34-04 tenant/ability 灰度、推进、回退和撤销动作；W34-05 Pi upstream 兼容、迁移窗口和持续评测；W34-06 安全事件/回归触发 revoke proposal 与运营摘要 | G-037；签名制品、发布委员会和真实部署目标 | G-038：正式发布 Gate；E-063 只证明本地 Release Governance 控制面 | 按租户/版本/能力停止 rollout，回退上一签名兼容版本，撤销 capability/Run/Secret Lease，保留发布/审批/评测/审计事实；未知外部写结果转人工核对，不自动重放 |

### 关键依赖与并行规则

- M21→M22→M23→M24 是不可并行跨越的执行关键路径；没有真实独立 Runner 不得接微 VM，没有微 VM 不得开放 Shell/Git 写能力。
- M25 的 Registry 数据模型可在 M24 后半段设计，但 ResourceLoader 切换必须在 G-028 后；M26 必须消费 M25 的不可变能力摘要。
- M27 的审批领域可提前复用现有 R3/R4 内核，但 Pi Tool 暂停/恢复与 `submit-change` 只能在 G-030 后启用。
- M28 的 Session Tree 可与 M27 后半段并行开发；E-056 已形成本地 Tree/Profile/Delegation 控制面，但受限 Sub-Agent 的真实执行必须等 G-031 的策略/审批能力通过。
- M29 只对已经冻结的 API 版本做前端；未通过 Gate 的动作显示“不可用/待配置”，不得用 Fixture 冒充。E-057 的员工工作台只调用实际本地 API/SSE，未生成模型回复或虚构执行结果；审批、submit-change、PR/合并/发布和治理写操作保持禁用。
- M30 的基础遥测从 M21 即开始埋点，但成本 Gate、评测基线和配额执行在端到端链路稳定后统一验收。
- M31 必须针对最终候选镜像、策略、Registry 摘要和网络拓扑执行；早期安全单测不能替代该阶段。

## 阻塞项

| 阻塞 | 影响 | 解除条件 | 状态 |
|---|---|---|---|
| 尚无三平台测试企业应用授权 | 无法执行真实飞书、钉钉、企业微信外部验收；不影响协议实现和 Fixture 测试 | 用户在各平台创建测试自建应用，并通过受控密钥录入工具配置 | 外部验收待授权 |
| 企业微信企业可信 IP 未配置到实际运行出口 | 独立配置已生效，但企业微信只读 API 返回 `60020`；无法安全解析“王渊芃”的企业 UserID或执行真实 `message/send` | 将应用部署到企业自有且出口 IP 固定的服务器，把该服务器公网出口加入自建应用“企业可信 IP”，只读预检通过后重新确认测试消息 | 外部网络安全配置待完成 |
| `office.vastmind.com.cn` 云侧入口尚未打通 | 实例 `82.157.0.21` 内部已运行 Nginx 与 Web 容器，但 DNS 尚无 `office` 记录，腾讯云安全组仍阻断公网 80/443，证书无法签发，企业微信 URL/可信域名校验无法执行 | DNSPod 增加 `office -> 82.157.0.21`；实例安全组放行 TCP 80/443；签发并验证正式证书 | 云侧网络配置待处理 |
| 生产 IdP/SSO 尚未指定 | 无法验证真实企业 SSO；不影响本地身份提供者与权限内核 | 用户确认 OIDC/SAML/企业身份源 | 生产集成待决策 |
| 生产数据库、Secret、遥测与备份目标尚未提供 | 无法完成真实灾备、密钥轮换、SLO 告警与回滚演练 | 用户提供隔离的预生产基础设施和运维负责人 | 生产演练待环境 |
| 试点团队尚未指定 | 无法形成真实管理闭环、采纳率和运营反馈证据 | 用户指定 1 个管理团队、负责人和四周试点窗口 | 试点待组织 |
| 独立 Pi Runner 运行环境未配置 | 当前已能验证本地 dedicated entrypoint、detached claim、独立 Supervisor 心跳、lease 绑定写入和过期恢复分流，但不能证明真实进程分离、持久租约、跨进程事件连续性和 kill/restart 恢复 | 提供可部署 Runner 的 Kubernetes/VM 环境、PostgreSQL 与持久队列，并执行 G-025/G-026 全阶段故障演练 | M22 外部基础设施待配置 |
| Firecracker/Kata RuntimeClass 与受控出站代理未落地 | 本地 Sandbox Control Plane 已完成契约和 fail-closed 适配，但 Shell、Git 和测试不能作为生产能力开放 | 提供支持微 VM 的节点池、RuntimeClass、CNI/NetworkPolicy、DNS/HTTP egress proxy 与隔离测试权限，并执行 G-027 攻击矩阵 | M23 外部基础设施待配置 |
| Forgejo、S3 兼容对象存储和 OCI Registry 未绑定 | M24 已有 fail-closed Remote Adapter、Postgres 元数据和本地虚拟验收，但无法完成真实 checkout、对象上传/下载、包供应链与 PR 交付 | 提供隔离测试实例、租户命名空间和短期凭据颁发能力 | M24/M25 外部基础设施待配置 |
| OpenBao 与测试 MCP Server 未绑定 | 无法验证服务端 Secret、OAuth、MCP 撤销和 Tool Gateway 出站边界 | 提供 OpenBao 测试路径、至少一个只读和一个受控写 MCP Server | M26 外部集成待配置 |

## 公开发布记录

- E-101（2026-08-26）按用户补充要求在 M35 Agent 开发工作流的测试阶段新增 `agentops-awesome-list` 推荐，API、工作台与说明文档同步展示其适用时机和边界：按实际复杂度执行只读健康检查，发现架构缺口、功能风险与优化建议，不修改项目、不授予权限、不替代功能测试或交付 Gate。API 定向 1 项及全量 123 个测试文件/451 项通过，8 个文件/26 项按既有条件跳过；typecheck、零警告 Lint 和 Next 生产构建通过。本增量不改变 F-076 已通过的本地 Gate 范围，不生成新版本。
- E-100（2026-08-26）完成 M35 Agent 开发团队工作流：新增服务端领域/应用/Presenter、InMemory/PostgreSQL Store、`0044_agent_development_workflow.sql` 及 down migration、四组 API、独立工作台和阶段 Skill 建议。需求交接原子生成五份 `project-to-act` 文档；版本强制保存 Git SHA、完整 Diff、摘要和功能清单；测试绑定具体版本；交付缺少任一版本 Diff/功能/通过测试时失败关闭，完成后冻结最新五文档与版本/测试摘要。全量 123 个测试文件/451 项通过，8 个文件/26 项按既有条件跳过；typecheck、零警告 Lint、Next 生产构建通过。宽屏与 390×844 浏览器完成需求→版本→失败测试不放行→通过复测→交付清单旅程，移动端无横向溢出，弹窗固定定位修复后完整可操作，控制台无错误/警告。证据只证明本地工程与 PGlite/PostgreSQL 适配合同，不替代真实 IdP、生产角色授权、生产 PostgreSQL、备份恢复或企业运行验收。
- E-099（2026-08-20）已完成公开开源快照：初始化本地 `main` 仓库，按 `.gitignore` 排除 `.env*` 实例、构建输出、内部台账、企业环境部署文件和运行产物；新增脱敏 `docs/FEATURE_CATALOG.md`、`docs/OPEN_SOURCE_HANDOFF.md`、`CONTRIBUTING.md`、`SECURITY.md` 和 Apache-2.0 `LICENSE`，修正公开部署/连接器示例为占位值。`npm.cmd test -- --reporter=dot` 为 119 个文件通过、8 个文件跳过，441 项通过、26 项跳过；typecheck、Lint、Pi Runner bundle、隔离 Next production build 均退出 0。公开 commit `3fee719840976943e3eedae492585898093c768f` 已推送并由 `git ls-remote`、`gh repo view`、GitHub contents API 回读确认，仓库为 PUBLIC、许可证为 Apache-2.0、`origin/main` 一致。该证据只证明公开源码交付，不改变生产 Gate；后续代码继续以本机 `.project-to-act` 为唯一事实源。

## 下一步

1. 先按既有回退矩阵补齐 G-025/G-026/G-027/G-028/G-029 的真实前置；在这些 Gate 未通过前，继续保持 Runner、Shell/Git 写入、资源分发和生产 MCP capability 关闭。
2. M26 本地控制面已形成 E-053/E-054/E-092/E-093/E-094；下一批必须先接入 OpenBao、只读/受控写测试 MCP 和隔离出站代理，完成真实 Secret/OAuth、Schema 漂移、撤销、超时/熔断、SSRF/DNS/Metadata、配额/幂等和跨租户矩阵，并清理历史 NULL 审计数据、验证 `0039` constraint，未完成前不把 G-030 记为通过。
3. M27 已形成 E-055/E-095/E-096/E-097：可继续维护本地审批、Change Delivery、决策历史、Postgres RLS/CAS、审批等待态 CAS 激活、持久 Worker/lease fencing、ResourceLoader 强制注入的 Tool policy/approval Extension 和 fail-closed Forgejo 适配；在 G-030/G-031 通过前，HTTP 只允许创建受审批保护的提案/队列事实，实际 MCP Tool pause/resume、Forgejo PR、合并、发布和外部 dispatch 保持关闭，不得以本地 Fixture 替代 Gate。
4. M28 已形成 E-056：可继续维护本地 Session Tree/Profile/Delegation 控制面；默认 Runtime 的 `spawnChildRun` 保持失败关闭，真实多进程恢复、子 Agent 执行、Artifact 汇总和 G-032 必须在 G-031 前置通过后验证。
5. M29 已形成 E-057 的本地员工工作台切片：继续以 capability 为准显示 Profile、运行状态、树、Diff、Artifact 和 Checkpoint；审批决策、变更提交、治理管理和真实执行未具备前保持不可用，不用前端隐藏替代服务端权限。
6. 任一 G-025～G-033 失败按对应回退锚点恢复：只排队/只读、关闭 execute/delegation Profile、禁止 push、撤销凭据和下载授权、停止资源/MCP capability、保留不可改写事件；企业微信、OIDC 和原办公平台外部 Gate 保持并行阻塞。

## 进度历史
- 2026-08-20：形成 E-099 公开开源快照。当前源码、测试、通用设计文档、脱敏功能清单和开源交接手册已提交至公开 GitHub 仓库 `redmaplewww/nexus-office-agent`，commit `3fee719840976943e3eedae492585898093c768f`；远程 `main` 回读一致。公开边界排除内部 `.project-to-act`、真实环境部署文件、环境实例、运行产物和企业地址；公开仓库使用 Apache-2.0，不能把本地控制面回归或公开提交当成生产 Gate。
- 2026-08-20：按 M27 W27-01～W27-04/PI-MOD-04/PI-MOD-13/PI-API-001 形成 E-097。新增受控 `enterprise-policy-extension` inline Extension，ResourceLoader 在已批准资源物化完成后强制注入；Tool Call 进入前按工具类别与 Profile 风险上限分类，未知/未分类/R4/越权直接失败关闭。R2/R3 创建数据库 Approval Proposal，在同一次 Pi 调用中等待审批状态；批准后通过 `PiApprovalService.resumeToolCall` 重新读取 Session/Run 的 manifestDigest、profileVersion、policyVersion、sandboxRunId，发生对象版本漂移时拒绝执行；拒绝、过期、撤销、取消、中断和等待超时均失败关闭。Runner 将 `approval_required`、`approval_resumed`、`approval_denied` 映射到 Session/Run 状态和事件，并在等待期间保持 lease heartbeat；生产入口接入 fail-closed approver directory，未配置组织审批目录时高风险 Tool 不执行。新增 `PiRuntimeApprovalObjectVersionReader`、Runtime/ResourceLoader/Runner wiring、`awaiting_approval -> running` 状态转换以及 5 项 policy Extension 和 1 项 ResourceLoader 回归；定向 4 个测试文件/32 项通过，全量 119 个测试文件/441 项通过、8 个文件/26 项跳过，typecheck、零警告 Lint、Pi Runner bundle、隔离 Next 生产构建通过。关键 SHA-256：`enterprise-policy-extension.ts`=`0B9B09BFD52B00D4E88B51308BB8961EB31F5EA7452D5F4FE16C9EB1603BE80D`，`pi-runtime-approval.ts`=`1D89E5231659E961E3BB3BEE51C819FB9BD8D604AC72AFB89C5ABBCB2DA2D459`，`runtime-adapter.ts`=`76E4E4310E6289E2FADAD6B221607A682BB535C39076A392B61698E62524267D`，`runner.ts`=`A5F89365143EEE95C36EB41231A0D80DD7B6A3A30BF9D1405B15858158DACCBA`，policy 测试=`D6D5F9777492E52A4E86CA200D4468D2C530464978FFB6C2A8EB5721E48EAF60`，ResourceLoader 测试=`C10411592171512E7BC78EB8059BD60AF22C72C7DA4FE472101ED87F610D5371`，bundle=`2BD3F3CBE59E9E49A74CD12E29805BA08E114E2A5170C979C17D148486B66342`。`pi-sandbox:preflight` 当前 `not_ready`/本次退出 1；Docker Desktop 版本检查通过但不替代 Firecracker/Kata。真实 Pi 模型/Tool 执行、组织审批、微 VM、OpenBao/受控出站、真实 Forgejo/发布目标和 G-027/G-030/G-031 仍 no-go。回退停止 approval service/高风险 Tool consumer，保持 fail-closed，保留 Session Event/Approval Proposal，并回到 E-096 不可变制品。
- 2026-08-20：按 M27 W27-05～W27-07/PI-MOD-16/PI-API-015 形成 E-096。修复审批等待态不能直接进入外部 Outbox 的状态机缺口：PR/merge/release 先保持 `awaiting_approval`，审批服务重新读取 Change Delivery 对象版本并通过 CAS 激活为 `queued` 后才允许 claim。新增 `PiChangeDeliveryOutboxWorker`，注册 `pi-change-delivery` Worker 角色和 `0041_pi_change_delivery_worker.sql`；新增 `0042_pi_change_delivery_leases.sql`，以 owner/token/expiry fencing 防止旧 Worker 终态写入，过期 lease 只记录 `unknown`、不自动重放。新增服务端 `ForgejoPiChangeDeliveryGateway`，按租户 Repository 的 `credentialRef` 从 OpenBao/受管 Secret 解析凭据，支持 PR 幂等发现、mergeability 刷新和显式开启的 merge；外部交付默认关闭，HTTP/凭据/上游未知结果均 fail-closed。聚焦 5 个测试文件/20 项通过；全量 118 个测试文件/435 项通过、8 个文件/26 项跳过；typecheck、零警告 Lint、Pi Runner bundle 和隔离 Next 生产构建通过；`pi-sandbox:preflight` 当前 Windows 返回 `not_ready`，真实 Firecracker/Kata、OpenBao、受控出站、组织审批人、真实 Forgejo/发布目标和 G-027/G-030/G-031 仍 no-go。回退停止 `pi-change-delivery` Worker 和外部 Gateway，保留 Submission/Proposal/Outbox/Event 事实；未批准队列保持等待，过期 lease/外部未知只转人工核对，不自动重放。
- 2026-08-20：按 M27 W27-05～W27-07/PI-MOD-16/PI-API-015 形成 E-095。新增 `PiChangeDeliveryService`、Change Delivery domain contracts/presenter、InMemory/PostgreSQL Store、迁移 `0040_pi_change_delivery.sql` 与 down migration、Submit/PR/Merge/Release Proposal/Admin HTTP routes；服务端重验 Workspace/Repository/base/head、Checkpoint、Test/Scan Artifact、approval hash、Session/Run 和对象版本，受保护动作只入 Outbox，`dispatchOutbox`/`recordExternalResult` 强制 system Runner，HTTPS external URL 无凭据，异常进入 terminal `unknown` 且不自动重放。新增单元 4 项、Postgres/PGlite 2 项、HTTP 3 项聚焦回归；全量 116 个测试文件/425 项通过、8 个文件/26 个测试跳过，typecheck、零警告 Lint、Pi Runner bundle 通过。真实 Forgejo/发布目标、组织审批人、OpenBao/MCP 前置、实际 Pi Tool pause/resume、微 VM、持久 Runner/Outbox 运维和 G-030/G-031 仍 no-go；回退停止 Outbox consumer，保留提案/审计事实，未知不重放并转人工核对。
- 2026-08-20：按 M26 W26-07、M32 readiness/PI-MOD-11～12 形成 E-094。新增 `PostgresMcpAuditScopeReadinessStore`、`McpAuditScopePreproductionProbe` 和 `CompositePiPreproductionProbe`；生产数据库存在缺失或 `NOT VALID` 的 `pi_mcp_call_audits_execution_scope_check` 时，预生产 readiness 明确返回失败，避免将兼容历史 NULL 的未验证约束误判为 Gate 通过；runtime 版本提升并在 PostgreSQL preproduction service 中接入该检查。新增 1 项回归，定向 3 个测试文件/14 项、全量 113 个测试文件/416 项通过、8 个文件/26 个测试跳过，typecheck、Lint、Pi Runner bundle 通过。该证据不执行历史 NULL 数据清理或真实 constraint validation，不证明真实数据库运维、OpenBao/MCP/出站代理、SSRF/DNS/Metadata 矩阵、生产配额/幂等或 G-030；回退保持 readiness fail-closed，停用 MCP/发布 consumer。
- 2026-08-20：按 M26 W26-04～W26-07/PI-MOD-11～12/PI-INT-023 形成 E-093。收紧 MCP 直接调用边界：`McpInvocation` 与 `McpCallAudit` 的 `sessionId/runId` 改为必填，ToolGateway 与 McpBridge 双重执行 scope 校验，拒绝空 scope 和 context/session 不一致；PostgreSQL audit adapter 对非 UUID scope 失败关闭，新增 `0039_pi_mcp_execution_scope.sql`，对后续审计写入施加 Session/Run 非空 CHECK（NOT VALID 以兼容历史数据）并建立 scope 索引。定向 3 个测试文件/13 项、全量 113 个测试文件/415 项通过、8 个文件/26 个测试跳过，typecheck、Lint、Pi Runner bundle 通过。该证据只覆盖本地 InMemory/PGlite/测试 Gateway，不证明历史 NULL 审计数据清理、真实 MCP/OpenBao/出站代理、SSRF/DNS/Metadata 矩阵、生产配额/幂等或 G-030；回退停用直接 MCP/Gateway consumer，保留历史摘要，禁止写入缺 scope 审计。
- 2026-08-20：按 M26 W26-04～W26-07/PI-MOD-11～12/PI-INT-023 形成 E-092。修复 Pi 注册 MCP Tool 进入 ToolGateway 时缺少权威 Session/Run scope 的审计连续性缺口：`createPiMcpTools` 强制接收 Session 与 Run，Runner 使用持久 Session ID 与 lease `runId`，Runtime 对已有 MCP Binding 但缺少 Gateway 或 Run scope 失败关闭；新增 Pi MCP 调用审计 scope 回归及无 Gateway 失败关闭回归。聚焦 3 个测试文件/13 项、全量 113 个测试文件/415 项通过、8 个文件/26 个测试跳过，typecheck、Lint、Pi Runner bundle 通过。该证据只覆盖本地 InMemory/PGlite/测试 Gateway，不证明真实 MCP/OpenBao/出站代理、SSRF/DNS/Metadata 矩阵、生产配额/幂等或 G-030；回退关闭 Pi MCP capability/Tool Gateway consumer，保留调用摘要和 Session/Run 证据，禁止直接 MCP。
- 2026-08-20：按 M25 W25-04～W25-07/PI-MOD-09 形成 E-091。修正 `PostgresPiResourceRegistryStore.updateSkillRelease` 与 `updateArtifactResourceRelease` 的 `revoked_at` partial update 语义：只有 patch 明确包含 `revokedAt` 时才写入（包含明确清空的 NULL），approve 可清除历史撤销时间，rollout/scan 等未提供该字段的局部更新保持原撤销状态；新增 Skill 与 OCI Package revoke→re-approve PGlite 回归。聚焦 2 项、全量 113 个测试文件/413 项通过、8 个文件/26 个测试跳过，typecheck、Lint、Pi Runner bundle 通过。该证据证明 Postgres Store 状态一致性，不证明真实 OCI/SBOM/SCA/签名 Registry、缓存传播、Guest Attestation、微 VM 或 G-029；回退停止 Registry 写 consumer，保留撤销/审批事实和只读资源快照，不允许覆盖性重试。
- 2026-08-20：按 M24 W24-05/PI-MOD-15/PI-INT-011～012 形成 E-086。新增 `PiValidationPlan`、`PiWorkspaceValidationService` 和 Runner validation wiring；Plan 只接受服务端 Profile/tenant policy 来源，拒绝控制字符/重复 ID/越界预算，命令以 `commandDigest` 进入事件，测试/扫描输出按分类写入当前 Workspace 的 `test_report`/`scan_report` Artifact；非零退出是已知 `failed` 并使用 `PiRunStore.fail` 进入 `dead_lettered`，执行器错误/不可用是 `unknown` 且不自动重试。集成聚焦 7 项通过：成功 test+scan、已知失败、执行器不可用、既有 Prompt/Checkpoint 和两类 cleanup failure；全量 111 个测试文件/405 项通过、8 个文件/26 个测试跳过，typecheck、Lint、Pi Runner bundle 通过。该证据仍是 local/virtual/cooperative，不替代真实微 VM、Forgejo/S3、SAST/SCA、URL 攻击矩阵或 G-028；回退关闭 validation/execute consumer，保留报告和 unknown/failed 事实，不重放未知副作用。
- 2026-08-20：按 M24 W24-06/PI-MOD-15 形成 E-087。修正 `InMemoryPiWorkspaceStore.expireArtifacts` 只按租户过期的边界，使本地 retention consumer 与 PostgreSQL 实现统一按 `tenantId + actorId` 作用域变更状态；新增同租户跨员工 Artifact 过期回归，当前员工的 retention 不改变另一员工状态。聚焦 Workspace control 14 项、相邻 Vibe Coding/Runner 28 项、全量 111 个测试文件/406 项通过、8 个文件/26 个测试跳过，typecheck、Lint、Pi Runner bundle 通过。该证据仅证明 InMemory/local actor scope，不替代真实 PostgreSQL RLS、S3/URL 过期/越权矩阵或 G-028；回退保留服务端 scope，禁用不一致的 local retention consumer 并保留只读 Artifact 状态。
- 2026-08-20：按 M24 W24-06/PI-MOD-15/PI-INT-012 形成 E-088。收紧 `S3WorkspaceObjectStore.put`：对象键由 tenant/artifact/version 形成后，同 scope 同内容/媒体类型/分类写入幂等返回，不同 actor/session/run scope 不得覆盖，内容或元数据变化以 `PI_OBJECT_DUPLICATE` 拒绝；新增本地 fake S3 HTTP 回归，覆盖对象原文保留、跨 scope issue/revoke 拒绝、短期 Grant 过期、撤销后下载拒绝。聚焦 2 项、相关 Supervisor 8 项、全量 112 个测试文件/408 项通过、8 个文件/26 个测试跳过，typecheck、Lint、Pi Runner bundle 通过。该证据仍是本地 S3-compatible transport，不替代真实 S3、对象版本一致性、URL 越权/过期攻击矩阵或 G-028；回退关闭对象写入 consumer，保留只读 Artifact/Grant 元数据并禁止覆盖性重试。
- 2026-08-20：按 M25 W25-05～W25-07/PI-MOD-04/PI-MOD-09～10 形成 E-089。新增 `PiResourceMaterializer` 与快照一致性校验，Runner/Runtime 对 Package/Extension 缺少物化器、缺少/额外/重复/空制品、Virtual Provider 均失败关闭；物化后的 Extension Factory 通过真实 Pi SDK `EnterpriseResourceLoader` 加载，关闭仓库默认资源发现，Loader 的 cwd/agentDir 固定为 Sandbox root，Pi SDK 启动失败会释放已物化资源。资源治理聚焦 8 项、全量 113 个测试文件/412 项通过、8 个文件/26 个测试跳过，typecheck、Lint、Pi Runner bundle 通过。该证据仅是本地 SDK/接口接线，不是 OCI/签名 Registry/SBOM/SCA/缓存传播或真实 Firecracker/Kata 证据；真实资源供应链和 G-029 仍 no-go，回退保持 Package/Extension 分发关闭并保留 Skill/快照审计事实。
- 2026-08-20：按 M25 W25-05～W25-07/PI-MOD-04/PI-MOD-09～10 形成 E-090。补充 `PiSandbox.executionBoundary`，可执行 Package/Extension 现在必须同时满足 Firecracker/Kata provider 和显式 `guest` boundary；Runner/Runtime 双重检查，host/缺失 boundary、Virtual Provider 均在 materialize 前拒绝，物化器接收 AbortSignal。聚焦资源治理 9 项、全量 113 个测试文件/413 项通过、8 个文件/26 个测试跳过，typecheck、Lint、Pi Runner bundle 通过。该证据不证明真实 Guest Attestation、Firecracker/Kata 内执行、OCI/签名 Registry/SBOM/SCA 或 G-029；回退继续停止 materializer consumer，不降级到宿主或普通容器。

- 2026-08-20：按 M24 W24-01～W24-07、PI-MOD-06、PI-MOD-15、PI-INT-011～012 形成 E-080。新增 `PiWorkspaceSupervisorStateStore`、`JsonFilePiWorkspaceSupervisorStateStore` 和启动恢复流程；显式白名单序列化，token/secret/password/authorization/private key 不得进入状态文件；恢复前校验状态 schema、Git root/origin/实际 HEAD 和对象/Grant scope，过期 lease/Grant 清理，坏状态、写入失败或 workspace 证明缺失失败关闭。真实 Docker Forgejo/MinIO + HTTPS Supervisor 集成测试在重建服务后恢复 diff 和下载 Grant，并验证跨租户 lease mismatch；聚焦状态 2 文件/6 项、真实集成 2 项、全量 108 个测试文件/388 项通过、7 个文件/25 个测试跳过，typecheck、零警告 Lint、37 页隔离 Next build、Runner/Supervisor bundle 通过；preflight `not_ready`/退出 2。该批次只覆盖单实例文件恢复，不证明 PostgreSQL/RLS、多进程协调、数据库故障恢复、六类旅程或 G-028；回退禁用 state file consumer、停止 Supervisor、撤销 Git/Grant 并保留只读摘要。

- 2026-08-20：按 M24 PI-MOD-06、PI-INT-011～012 形成 E-081。新增 `PostgresPiWorkspaceSupervisorStateStore` 和迁移 `0038_pi_workspace_supervisor_state.sql`；状态按 `state_id + tenant_id` 分区，启动逐租户 claim owner lease，保存按 changed tenant 执行 version CAS，支持 renew/release，强制 RLS 仅暴露当前租户，JSON payload 继续由显式白名单和无密钥校验保护。PGlite 状态/分区/owner/CAS 聚焦 2 文件/6 项通过；真实非表所有者 PostgreSQL 1 项通过，验证跨租户不可见、第二 owner fencing 和 stale version 冲突失败关闭；全量 109 个测试文件/391 项通过、8 个文件/26 个测试跳过，typecheck、零警告 Lint、37 页隔离 Next build、Runner/Supervisor bundle、preflight `not_ready`/退出 2 通过。该批次不证明 Firecracker/Kata、微 VM checkout、测试/扫描、URL 攻击矩阵、六类旅程或 G-027/G-028；PGlite 超级用户不作为 RLS 可见性证据，回退先停止 Supervisor、释放 owner lease、撤销 Git/Grant，恢复 JSON 只读/单实例或关闭 DB state consumer 并保留事实。

- 2026-08-20：按 M24 PI-MOD-06、PI-MOD-15、PI-INT-012 形成 E-082。新增 `tests/integration/pi-vibe-coding-journeys.test.ts`，通过 virtual provider 覆盖新功能、Bug 修复、重构、测试失败修复、只读 Review 和 PR 六条路径；验证 exact base、`pi/` 临时分支、Diff、写入路径 Checkpoint、测试报告 Artifact、Review 的 `workspace:write` 拒绝、PR 分支 push、终态清理和清理失败 `unknown`。聚焦 7 项，测试文件 SHA-256 为 `00A5C05CA106B921E7E9684331169C8818125454DF0D0D71A44ABB79FA933551`，全量 110 个测试文件/398 项通过、8 个文件/26 个测试跳过，typecheck、零警告 lint、project-to-act check/validate 通过；该 virtual/local 证据不替代真实微 VM、Forgejo/S3、测试/扫描、URL 攻击矩阵或 G-028。回退停止执行型 journey consumer，禁止 push/submit-change，撤销 Git/Object 凭据与下载 Grant并保留只读 Diff/Checkpoint 元数据。

- 2026-08-20：按 M22 PI-MOD-04、M24 PI-MOD-06/PI-MOD-15 形成 E-083。新增 `tests/integration/pi-runner-vibe-coding.test.ts`，通过 `PiRunnerWorker` 编排 `SandboxOrchestrator`、`PiWorkspaceService`、Virtual Sandbox 和 cooperative test runtime；Prompt Run 验证 Sandbox create/limits/network/mount、Workspace exact base/`pi/` 分支、Pi 事件顺序、资源清理先于成功终态，Checkpoint 命令验证 Checkpoint 持久化；Workspace cleanup failure 验证 Workspace `unknown`、Sandbox destroy verification、Run/Session/Command `unknown` 和 `PI_RUN_CLEANUP_UNKNOWN`，不自动重放。聚焦 3 项，测试文件 SHA-256 为 `C8BD91E2ACA3CE8CCEEF4553D697A2297DA9651B0D88D5BE15A38B8A770FAE79`，`runner.ts` SHA-256 为 `1B69F11F6BEFF2ECA46B172F134D4E517B6CADEB8BEF47620900974E640223EA`；全量 111 个测试文件/401 项通过、8 个文件/26 个测试跳过，typecheck、lint、Pi Runner bundle 通过。该批次只证明 local/virtual Runner 编排和回退合同，不替代真实 Pi 模型/Tool、持久 Queue 长稳、Firecracker/Kata、Forgejo/S3、测试/扫描或 G-026/G-028；回退停止 execution consumer，保留 unknown 事实，不重放未知副作用。
- 2026-08-20：按 M23 W23-01～W23-07、M24 G-027/G-028 前置形成 E-084。Sandbox preflight 按预期 `not_ready`/退出 1；Docker/Compose capability check 通过，但当前 Windows 缺少 Linux/KVM/vhost-vsock/cgroup/provider/rootfs/kernel/HTTPS Supervisor endpoint/Run Token，工具路径未发现 Firecracker/Kata/Forgejo/MinIO/psql。该批次只记录环境 no-go，不修改源码或生产状态；回退保持 execute Profile、Shell/Git 写入、自动 push/PR 和真实测试/扫描交付关闭，不降级到宿主/普通容器，待环境就绪后重跑 G-027/G-028。
- 2026-08-20：按 M22 PI-MOD-04、PI-EVT-007～018 形成 E-085。错误路径现在在失败/重试分流前执行 Workspace/Sandbox 清理；清理异常写入 `PI_RUN_CLEANUP_UNKNOWN`，Run/Session/Command 进入 `unknown`，不自动重放或增加 retry。聚焦 4 项、全量 111 个测试文件/402 项通过、8 个文件/26 个测试跳过，typecheck、Lint、Pi Runner bundle 通过；真实 Pi/Queue/微 VM/生产 Gate 仍未验证，回退继续关闭 execution consumer。

- 2026-08-20：按 M24 W24-01～W24-07、PI-MOD-06、PI-MOD-15、PI-INT-011～012 形成 E-079。新增 `PiWorkspaceSupervisorService`、Forgejo Git adapter、S3-compatible object store、HTTPS HTTP server/entrypoint 和 Docker Forgejo/MinIO E2E compose；server-only credentials 使用进程内 Git extraHeader，不进入 URL、请求体、持久记录或模型上下文；所有请求 scope 从受控 tenant/actor/session/run/trace headers 解析，body 拒绝身份/Secret 字段。真实 Docker Forgejo/S3 + HTTPS Supervisor 回归通过 checkout、精确 base、`pi/` 分支、文件变更、diff、checkpoint、push、artifact digest/Grant/download/revoke/delete、跨租户 revoke 和 cleanup；全量 107 个测试文件/385 项通过、7 个文件/25 项跳过，typecheck、Lint、37 页隔离 Next build、Runner/Supervisor bundle 通过。Windows/WSL2 preflight 仍 `not_ready`/退出 2，真实微 VM、测试/扫描、URL 过期/越权矩阵和 G-028 六类旅程仍 no-go；回退撤销 Git/Object lease 与下载 Grant、停止 Supervisor/Compose 测试实例、禁止新 push/PR/Workspace 写入并保留只读 diff/checkpoint 摘要。

- 2026-08-20：按 M24 W24-02、PI-MOD-06、PI-INT-011 形成 E-078。修正 Remote Git Credential issue/revoke 不得用 workspaceId 冒充 session/run、revoke 不得使用 system 身份，以及 Workspace provider 不得把 context 直接带入请求体的问题；PiWorkspaceService 的 Remote Workspace/Credential 请求统一使用持久 Workspace 的真实 session/run，所有 Workspace 请求体只保留白名单业务字段，Credential 请求体不携带 actor、credentialRef 或 context，InMemory revoke 对跨租户/用户/Session/Run 失败关闭。聚焦新增 4 项回归、全量 106 个文件/382 项通过、5 个文件/23 个测试跳过；typecheck、零警告 Lint、37 页隔离 Next build、Pi Runner/Supervisor bundle 和 project-to-act 校验通过；Windows/WSL2 preflight 按预期 `not_ready`/退出 2，真实 Forgejo、短期凭据服务和 G-027/G-028 仍 no-go，回退撤销凭据并禁止 push/PR/Workspace 写入。

- 2026-08-20：按 M24 W24-01～W24-07、PI-MOD-06、PI-MOD-15、PI-INT-012 形成 E-076。Remote Workspace endpoint 只允许无凭据/query/hash 的 HTTPS，path 拒绝 `//`、`..` 和控制字符并透传 tenant/actor/session/run；providerWorkspaceRef 只允许内部 scheme；Remote Object Storage put 校验本地 SHA-256/size，download grant URL 只允许无凭据 HTTPS。聚焦 1 个文件/7 项、全量 106 个文件/376 项通过、5 个文件/23 项跳过；typecheck、Lint、37 页隔离 Next build、Runner/Supervisor bundle、Docker health/readiness smoke 和 project-to-act 校验通过；真实 Forgejo/S3、短期凭据、测试/扫描、URL 过期/越权和 G-028 仍 no-go，回退禁止 push/PR/Workspace 写入并撤销凭据/下载授权。
- 2026-08-20：按 M24 W24-06～W24-07、PI-MOD-15、PI-INT-012 形成 E-077。对象存储 put/download/revoke/delete 全生命周期统一携带真实 tenant/actor/session/run/trace scope，scope 只进入受控请求头，身份字段不进入对象请求体；缺少真实 Run scope 时服务和 Remote adapter 均失败关闭。聚焦 1 个文件/9 项、全量 106 个文件/378 项通过、5 个文件/23 项跳过；typecheck、Lint、37 页隔离 Next build、Pi Runner/Supervisor bundle 和 project-to-act 校验通过；真实 S3/Forgejo、短期凭据、URL 过期/越权和 G-028 仍 no-go，回退撤销凭据/下载授权并保留只读摘要。
- 2026-08-20：按 M23 W23-10、PI-MOD-05、PI-INT-005 形成 E-074。恢复先校验 `/proc/<pid>/exe` 与配置 runtime 一致、network policy 编译 digest/字段一致和 `cgroup.procs` 成员，销毁验证同时检查 runtime/socket/cgroup 残留；聚焦 2 个文件/7 项、全量 106 个文件/373 项通过、5 个文件/23 项跳过，typecheck、Lint、37 页隔离 Next build、Runner/Supervisor bundle、Docker health/readiness smoke 和 project-to-act 校验通过；当前 Windows/WSL2 无 KVM/vhost-vsock，G-027 真实隔离仍 no-go，回退关闭 execute Profile，不降级到宿主/普通容器。
- 2026-08-20：按 M23 W23-10、PI-MOD-05、PI-INT-005 形成 E-075。mount 只接受 `workspace://`、`virtual://`、`forgejo://` opaque ref；Guest Agent read/list/write/patch 前拒绝绝对路径、反斜杠、`..` 和超过 1.5 MB 文本载荷。聚焦 1 个文件/4 项、全量 106 个文件/373 项通过、5 个文件/23 项跳过，typecheck、Lint、37 页隔离 Next build、Runner/Supervisor bundle、Docker health/readiness smoke 和 project-to-act 校验通过；当前 Windows/WSL2 无 KVM/vhost-vsock，真实 Guest Agent rootfs、Forgejo/S3、网络攻击矩阵和 G-027/G-028 仍 no-go，回退关闭 execute Profile，不降级到宿主/普通容器。

- 2026-08-20：按 M23 W23-10、PI-MOD-05、PI-INT-005 形成 E-074。恢复先校验 `/proc/<pid>/exe` 与配置 runtime 一致、network policy 编译 digest/字段一致和 `cgroup.procs` 成员，销毁验证同时检查 runtime/socket/cgroup 残留；聚焦 2 个文件/7 项、全量 106 个文件/373 项通过、5 个文件/23 项跳过，typecheck、Lint、37 页隔离 Next build、Runner/Supervisor bundle、Docker health/readiness smoke 和 project-to-act 校验通过；当前 Windows/WSL2 无 KVM/vhost-vsock，G-027 真实隔离仍 no-go，回退关闭 execute Profile，不降级到宿主/普通容器。

- 2026-08-20：按 M23 W23-10、PI-MOD-05、PI-INT-005 形成 E-073。恢复路径由 `cgroupController.open(id, limits)` 收紧为带 PID 的 `open(id, limits, pid)`，File cgroup controller 读取并验证 `cgroup.procs` 成员后才重新应用 limits；Guest Agent 增加可替换连接工厂以覆盖 CONNECT/OK、JSONL requestId 相关性和非法握手失败关闭。聚焦 5 个文件/16 项、全量 106 个文件/370 项通过、5 个文件/23 项跳过，typecheck、Lint、37 页隔离 Next build、Runner/Supervisor bundle、Docker health/readiness smoke 和 project-to-act 校验通过；当前 Windows/WSL2 无 KVM/vhost-vsock，G-027 真实隔离仍 no-go，回退关闭 execute Profile，不降级到宿主/普通容器。

- 2026-08-20：按 M23 W23-09/W23-10、PI-MOD-05、PI-INT-005 形成 E-072。新增独立 `pi-sandbox-supervisor` HTTP 入口、`/healthz`/`/readyz`、File/InMemory binding store、结构化 preflight、Dockerfile/Kubernetes 部署合同；`FirecrackerSandboxBackend` 仅通过 Unix API Socket 配置 machine/boot/rootfs/vsock，启动时使用最小环境并加入 cgroup v2，运行元数据保存 PID/Socket/limits/compiled network policy 但不保存 Token/Secret；`VsockPiSandboxGuestAgent` 通过 CONNECT/OK 握手和 JSONL 请求承载文件、Patch、Exec、Snapshot、Usage、Workspace/Network Policy，缺 Guest Agent/KVM/vsock/rootfs/kernel/cgroup 或 Network Controller 时失败关闭。聚焦 Supervisor/Preflight 7 项、Firecracker Backend 2 项通过；`npm.cmd run typecheck`、`npm.cmd run lint`、Supervisor bundle、Docker image build、healthz=200/readyz=503 smoke 通过；当前 Windows/WSL2 无 `/dev/kvm`，真实 Firecracker/Kata、Guest Agent rootfs、RuntimeClass/seccomp/capability、网络/逃逸矩阵、10,000 次残留和 G-027 仍未验证，回退停用 execute Profile，保留只读 Session/事件，不降级到宿主/普通容器。

- 2026-08-20：按 M23 W23-08、PI-MOD-05、PI-INT-005 形成 E-071。新增 `HmacPiSandboxRunTokenIssuer`、远程 Supervisor `Authorization` 头和 Sandbox scope：创建请求绑定 tenant/actor/session/workspace/run/provider，后续操作额外绑定 `sandboxId`；`SandboxOrchestrator` 在创建、调用、恢复和销毁前复验身份，明文 Token 不进入请求体、RunManifest、持久记录、日志或模型上下文。聚焦 `npx.cmd vitest run tests/unit/pi-sandbox-control.test.ts tests/unit/pi-run-control.test.ts --reporter=verbose` 通过 2 个文件/19 项；一次性 Docker `postgres:16-alpine`、迁移 `0001`～`0037`、非表所有者 `nexus_app`、真实独立 Runner bundle 和 HTTP Supervisor 夹具验证 process 1/1、fault matrix 14/14；全量 `npm.cmd test -- --run` 通过 102 个文件/359 项，5 个文件/23 项跳过；`npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run pi-runner:bundle`、隔离 Next build 和 project-to-act 校验通过。该证据只覆盖短期凭据与控制面身份绑定，不替代真实 Firecracker/Kata、egress proxy、宿主/邻居/metadata/SSRF/DNS/端口攻击矩阵或 G-027，回退关闭 execute Profile，保留只读 Session/事件。

- 2026-08-20：按 M21 PI-MOD-03/M22 PI-MOD-04 形成 E-070。新增 `PiRunScheduler` 控制面门面，Runner 的 claim、heartbeat renew、成功终态提交和 drain admission 统一经过调度层；InMemory/PostgreSQL Store 新增带当前租约 owner/token/run 校验的 `release`，以及 `complete/fail/deadLetter`，其中 PostgreSQL 在同一租户事务内同时提交 Run 状态与命令终态，过期/旧租约失败关闭。新增单元与 PGlite 聚焦测试共 21 项通过；一次性 Docker `postgres:16-alpine`、迁移 `0001`～`0037`、非表所有者 `nexus_app` 下真实控制面 5 项通过，验证两个竞争 Scheduler 的 claim 去重、租户隔离、release 不额外增加 attempts、原子 complete/fail 和 drain 后不再 claim；全量 102 个测试文件/358 项测试通过、5 个文件/23 项测试跳过，typecheck、Lint、隔离 Next build、Pi Runner bundle 和 project-to-act 校验通过。该批次仍不证明持久 Queue 运维/消费者排空、24h/生产长稳、真实 Pi 模型/Tool、Firecracker/Kata、生产部署和正式回退；G-025/G-026 继续 no-go，回退停止 consumer、排空并恢复只入队/只读，保留 Run/命令/事件/lease 事实。

- 2026-08-20：按 M33 W33-01～W33-06 完成本地 Pilot Governance 控制面批次并形成 E-062。新增 Pilot/participant/journey/observation/data sample/incident/readiness/event 合同、`PiPilotService`、FailClosed Evidence/Probe、Postgres/InMemory Store、`0035_pi_pilot_control.sql` 八张强制 RLS/审计表、M33 API 和运营摘要；聚焦单元/HTTP/PGlite 10 项、全量 102 个测试文件/340 项测试、typecheck、零警告 Lint、隔离 Next 构建、顺序 Runner bundle、project-to-act 校验和桌面/移动浏览器通过；`pilot-operations`=200、`/ready`=503，默认 evidence/probe 保持 no-go。真实 G-036、企业四周试点、Git/PR/Artifact 账本和 G-037 未验证，回退停止新 Session、撤销 Pilot capability 并恢复人工开发流程。
- 2026-08-20：按 M34 W34-01～W34-06 完成本地 Release Governance 控制面批次并形成 E-063。新增 Publication/Gate/Risk/Approval/Rollout/Evaluation/Event 合同、`PiReleaseGovernanceService`、FailClosed Gate、Postgres/InMemory Store、`0036_pi_release_governance.sql` 八张强制 RLS/审计表、M34 API 和运营摘要；G-025～G-037 Attestation/有效期、P0/P1 风险、双人职责分离、灰度推进/回退/撤销和持续评测聚焦覆盖，且全量 102 个测试文件/340 项测试、typecheck、零警告 Lint、隔离构建、顺序 Runner bundle、project-to-act 校验和桌面/移动浏览器通过；`release-governance`=200、`/ready`=503，默认 Gate 保持 no-go。真实 G-037、签名制品、发布委员会、生产 Deployment/Runner、发布后评测和 G-038 未验证，回退停止 rollout 并按租户/能力回退上一兼容签名版本。
- 2026-08-20：按 M21/M22 后续加固形成 E-064。Run Store 增加 lease 绑定的状态终态写入、过期 claim 标记、旧 lease 的 ack/requeue/unknown/status 拒绝；Pi Runner 增加 detached claim、run heartbeat、运行前重排队与运行中 unknown 不重放；Supervisor 心跳独立于长任务并在排空时串行落状态，生产无 Firecracker/Kata 时 Session 创建失败关闭。聚焦 Runner/lease/生产 fail-closed 回归、全量 102 个测试文件/346 项测试、typecheck、Lint、隔离构建和 dedicated bundle 通过；真实 PostgreSQL、持久 Queue、多进程 kill/restart、Firecracker/Kata、G-025/G-026 仍未验证，回退停止 consumer、排空并恢复只入队/只读。
- 2026-08-20：按 M21/M22 真实回归形成 E-065。启动一次性 Docker Engine `29.6.1` 与 `postgres:16-alpine`，应用 `0001`～`0036` 迁移并使用非表所有者 `nexus_app`；真实测试证明 100 并发同幂等键只生成 1 个 Run/Manifest、跨租户读取被 RLS 拒绝、过期 lease 可被新 Runner 接管且旧 Runner 不能写终态；独立 bundle Runner 子进程在 claim 后被强杀，lease 过期后第二进程记录 `run_reclaimed`、增加 attempts 且 Manifest 仍为 1。全量 102 个测试文件/346 项测试、typecheck、Lint、隔离 Next 构建和 bundle 通过。真实 Gate 仍未通过：迁移 rollback/forward、持久 Queue、所有创建/模型/Tool/event flush 强杀矩阵、interrupt/timeout/长稳、Firecracker/Kata、生产部署和回退演练待完成；回退停止 consumer、排空并恢复只入队/只读，不删除共享表。

- 2026-08-20：按 M21/M22 故障恢复批次形成 E-067。新增 `runner-faults.ts` 的开发/测试限定故障注入器和完整阶段 checkpoint，Runner 对 AbortSignal、interrupt、timeout、event-chain flush 和旧 lease 恢复进行显式处理；SandboxOrchestrator 在 provider handle 持久化后可重建并验证销毁，遵循“一次 Run 至多一个 Sandbox”，有资源句柄的崩溃不自动重放。一次性 Docker PostgreSQL、非表所有者 `nexus_app`、真实 bundle 子进程、本地 HTTP Supervisor 和 opt-in cooperative test runtime 完成 12 个故障矩阵用例、interrupt、timeout、旧进程回收和事件连续性验证；聚焦 2 文件/16 项、故障矩阵 14 项、旧进程 1 项、全量 102 个文件/352 项通过，typecheck、Lint、隔离 Next build、Runner bundle 和账本校验通过。E-067 仍是 G-025/G-026 的部分证据：持久 Queue 运维/消费者排空、真实模型和 Tool、副作用外部系统、Firecracker/Kata、24h/长稳及生产回退仍未验证；回退停止 consumer、排空、只入队/只读，已创建 Sandbox 的 Run 保持 unknown 并人工核对，不删除事件/命令/lease/沙盒事实。

- 2026-08-20：按 M22 graceful drain/bounded soak 批次形成 E-068。`WorkerSupervisor` 在 abort 到达时同步设置 Runner claim barrier 并调用 `beginDrain`，先写 `draining=true` heartbeat 再等待活动 Run；Runner 对排空中的 Run 按资源事实分流：已启动 Runtime 或已创建 Sandbox/Workspace 的 Run 标记 `unknown` 且不重放，尚无资源句柄的 Run 才重排队。真实 PostgreSQL 非表所有者 `nexus_app`、真实 bundle 子进程、cooperative runtime 验证 graceful drain 下排队命令 attempts=0、活动 Run unknown、Sandbox destroyed；48 次 bounded soak 验证每个 Run 一次 claim、事件序列连续、Manifest 唯一且无孤儿 Sandbox。聚焦单元 2 文件/19 项、真实 drain/soak 2 项、故障矩阵/进程回收 15 项、全量 102 个测试文件/354 项通过、5 个文件跳过/21 项跳过，typecheck、Lint、隔离 Next build、Runner bundle 和账本校验通过；Windows `ChildProcess.kill(SIGTERM)` 不执行 Node signal handler，故仅 development + cooperative runtime 增加 `NEXUS_PI_TEST_SHUTDOWN_FILE` 测试 sentinel，生产 SIGTERM/SIGINT 路径未改变。24h soak、持久 Queue、真实模型/Tool、Firecracker/Kata、生产部署和正式回退仍未验证，G-026 保持 no-go；回退停止 consumer、先发布 draining heartbeat 并排空，资源或 Runtime 已创建的 Run 保持 unknown，不删除事实或重放副作用。
- 2026-08-20：按 M21 W21-02/W21-06 形成 E-069。`PiRunStore` 新增 `listBacklog(tenantId, query)` 契约，InMemory/PostgreSQL 实现均只按服务端租户作用域读取，默认返回 `accepted/queued/leased/cancel_requested`，支持状态过滤、`available_at/created_at/id` 稳定排序和 `limit` 1～1000（默认 100）边界；PostgreSQL 路径通过 `withTenant` 与 SQL tenant predicate 执行。聚焦 `tests/unit/pi-run-control.test.ts`、`tests/integration/postgres-pi-run-control.test.ts` 共 19 项通过；一次性 Docker `postgres:16-alpine`、迁移 `0001`～`0037`、非表所有者 `nexus_app` 角色的 `tests/integration/real-postgres-pi-run-control.test.ts` 4 项通过，验证跨租户 backlog、acknowledged 排除、租约状态过滤和 RLS 作用域；全量 102 个文件/356 项通过、5 个文件/22 项跳过，typecheck、Lint、隔离 Next build、Pi Runner bundle、project-to-act 校验通过。真实持久 Queue 运维、消费者排空、24h/生产长稳、Firecracker/Kata、真实模型/Tool 和正式回退仍未验证；回退保持只入队/只读，不删除命令或审计事实。

- 2026-08-20：按 M29 W29-01、W29-02、W29-03、W29-05、W29-07 形成 E-057。新增 `PiCodingWorkbench` 员工开发工作台、Profile capability catalog、Session Tree/fork/resume/compact API 和 SSE 游标重连；页面通过实际 Session API 创建会话、入队消息、显示 queued/run/unknown/terminal 状态，提供 Diff/Artifact/Checkpoint 只读交付轨和明确的审批/submit/PR/合并/发布不可用状态；本地验证桌面 1280×720、移动 390×844、键盘发送、移动导航、断线游标、Tree/fork/resume/compact 与空交付列表。87 个测试文件/306 项测试、typecheck、零警告 Lint、隔离 Next 构建、Runner bundle 和账本校验通过；真实 Runner/MCP/Forgejo/S3/OpenBao/审批执行、治理控制台、跨进程 SSE、G-030～G-033 与 `/ready=200` 未验证，`/ready` 仍为 503。

- 2026-08-20：按 M28 W28-01～W28-07 形成 E-056。本批新增 Session Tree/Branch/Summary/Delegation 合同、`0031_pi_session_tree_delegation.sql`、Postgres/InMemory Tree/Delegation Store、fork/tree/history/resume/compact/continuity 服务、确定性摘要、Profile Registry、父子权限/Tool/风险/数据范围/预算交集、深度/并发/循环防护和委派审计账本；默认 Runtime 的真实 `spawnChildRun` 仍失败关闭。M28 聚焦 2 个测试文件/5 项测试、全量 87 个测试文件/306 项测试、typecheck、零警告 Lint、隔离 Next 构建、dedicated Runner bundle 和 project-to-act 校验通过；真实 G-031、跨进程 kill/restart、真实子 Agent 执行、Artifact 汇总、循环/爆炸真实矩阵和 G-032 仍未通过，`/ready` 仍为 503。

- 2026-08-20：完成 M21/M22 本地实现批次并形成 E-049。Web Pi Service 已改为只创建 Manifest/命令，Postgres/PGlite 与内存租约仓储支持幂等、续租、旧 token 拒绝、重试/死信/unknown；Pi SDK 迁移至独立 `pi-runner` 入口，Runner Adapter 支持事件顺序写入、超时、中断、优雅排空和本地故障测试。74 个测试文件/266 项测试、typecheck、零警告 Lint、隔离目录生产构建和 dedicated esbuild 通过；G-025/G-026 仍未通过，真实 PostgreSQL、跨进程 kill/restart、微 VM 和生产签名保留为门禁。
- 2026-08-20：按 M23 W23-01～W23-07 完成本地控制面批次并形成 E-050。新增 `SandboxOrchestrator`、InMemory/Postgres `sandbox_runs` 仓储与 `0026_pi_sandbox_control_plane.sql`；Provider 生命周期覆盖 create/mount/limits/network/read-write-patch/list/exec/usage/terminate/destroy/verifyDestroyed；`EgressPolicyCompiler` 默认 deny、代理引用、精确域名/端口、metadata/私网/通配符/解析绕过输入拒绝；远程 Firecracker/Kata Supervisor 仅走受控 HTTPS 适配，无 endpoint 或生产不允许 Provider 时失败关闭。Runner 按 Run 注入 Orchestrator，部署入口修正为 `dist/pi-runner.mjs`，本地 75 个测试文件/271 项测试、typecheck、Lint、隔离目录 Next build 和 dedicated esbuild 通过；真实 RuntimeClass、egress proxy、微 VM 攻击矩阵、10,000 次销毁残留、G-025/G-026/G-027 仍未通过。
- 2026-08-20：按 M24 W24-01～W24-07 完成本地控制面/适配器批次并形成 E-051。新增 Workspace/Git/Artifact 领域合同、`0027_pi_workspace_git_artifact.sql`、Postgres/InMemory Store、`PiWorkspaceService`、Remote/Virtual/FailClosed Workspace Provider、短期 Git Credential Broker、对象存储和下载 Grant；Runner 在配置服务时按 Run 准备 Workspace、挂载只读/可写工作区、checkpoint 后生成 Git/Diff/Artifact 摘要并在终态清理。新增 Workspace/Artifact API，租户/Actor 过滤、精确 base SHA、受保护分支、摘要校验、对象版本和短期授权测试通过；77 个测试文件/276 项测试、typecheck、零警告 Lint、隔离目录 Next build 和 dedicated esbuild 通过。真实 Forgejo/S3、G-027 微 VM 前置、测试/扫描任务、浅克隆/大仓库、下载 URL 过期/越权真实 E2E 和 G-028 仍未通过，生产 `/ready` 仍为 503。
- 2026-08-20：按 M25 W25-01～W25-07 完成本地资源治理控制面批次并形成 E-052。新增 `0028_pi_resource_supply_chain.sql`、Skill/Package/Extension Registry 的 InMemory/Postgres Store、Ed25519/FailClosed 签名验证、Skill 摘要/清单/审核/灰度/撤销、Package/Extension SBOM/扫描状态门禁、`EnterpriseResourceLoader` allowlist、Session/RunManifest 不可变资源快照、Skill Catalog API 和 Runner 资源失败关闭；`pi:registry:scan` 才能写入 passed 扫描状态，恶意 `.pi`/`.agents`/`AGENTS.md` 不被发现，OCI 制品缺少 materializer 时不降级且不重试。79 个测试文件/282 项测试、typecheck、零警告 Lint、隔离目录 Next build、dedicated Runner esbuild 和 project-to-act check/validate 通过；真实 G-028 前置、OCI 构建/SBOM/SCA/签名服务、缓存传播窗口、真实 Registry、Package/Extension 运行 materializer、G-029 仍未通过，生产 `/ready` 仍为 503。

- 2026-08-20：按 M29 W29-06 完成本地治理控制面批次并形成 E-058。新增 `GET /api/v1/pi/admin/overview` 安全快照、Skill/Package/Extension 发布入口和 approve/revoke/rollout/scan 动作 API、MCP Server/Binding 脱敏管理摘要与租户/权限重算；新增 `PiGovernanceConsole`，Profile 只读展示、资源空状态、MCP 空状态和治理动作均消费真实 API，不生成演示资源，不接触 Secret、签名原文、endpoint 或执行凭据。新增治理 API 集成测试 4 项，针对资源/MCP/治理策略的聚焦测试与全量回归共 88 个测试文件/308 项测试通过；typecheck、零警告 Lint、隔离 Next 生产构建、dedicated Runner bundle 和 project-to-act 校验通过。浏览器真实验证治理页刷新、Profile/资源/MCP 快照、桌面 1280×720、移动 390×844（clientWidth/scrollWidth=375）无横向溢出；`/ready` 仍 503，真实管理员、MCP/OpenBao、Runner、Profile/Policy/Quota 写入、跨进程恢复和 G-033 仍未通过。
- 2026-08-20：按 M30 W30-01～W30-07 完成本地模型治理/运营控制面批次并形成 E-059。新增 `0032_pi_model_observability_quota.sql` 八张租户 RLS/审计表、`EnterpriseModelGateway`、`PiTelemetryService`、`PiQuotaService`、Postgres/InMemory Store、模型路由/授权/用量、Trace/Metric/Evaluation/Regression、配额策略/原子预留/消费/释放、模型/遥测/评测/配额 API 和 `PiOperationsConsole`。聚焦 2 个 M30 单元/API 文件与 1 个 PGlite 租户集成文件，全部回归 91 个测试文件/315 项测试通过；typecheck、零警告 Lint、隔离 Next 构建、dedicated Runner bundle、M30 页面浏览器验证和 project-to-act 校验通过。模型 Provider、两类真实模型、OTel、生产配额/限流、Run/Tool 全链路、跨进程 Runner 与 `/ready=200` 未验证，G-034 保持 no-go。
- 2026-08-20：按 M31 W31-01～W31-08 完成本地安全/韧性控制面批次并形成 E-060。新增 `0033_pi_security_resilience.sql` 五张强制 RLS/审计表、`PiSecurityResilienceService`、Postgres/InMemory Store、全局/租户/Profile/Model/Resource Kill Switch、不可变安全事件、不可信 Prompt/Repository 封装、容量策略/幂等租约、非生产有界故障注入，以及 `PiRecoveryService` 的租户范围加密备份、显式恢复确认和发布回退动作摘要；Runner/Model 执行前门禁和 M31 安全/容量/故障/恢复聚焦测试通过。恢复切片未接入生产 Secret/Object/Deployment，真实微 VM、SSRF/DNS/Metadata/供应链传播、跨进程故障、24h soak、生产 RPO/RTO 和 `/ready=200` 未验证，G-035 保持 no-go。
- 2026-08-20：按 M32 W32-01～W32-07 完成本地预生产控制面批次并形成 E-061。新增 `0034_pi_preproduction_control.sql` 四张强制 RLS/审计表、`PiPreproductionService`、FailClosed Readiness Probe、Postgres/InMemory Store、发布候选/就绪快照/Secret Lease/预生产事件、promote/rollback、Secret 引用 scope/TTL/revoke、M32 管理 API 和运营页预生产门禁。聚焦单元/API/PGlite 测试 5 项，全量 98 个测试文件/330 项测试、typecheck、零警告 Lint、隔离 Next 构建、dedicated Runner bundle、桌面/390×844 浏览器和 `/api/v1/pi/admin/preproduction=200` 通过；`/api/v1/ready=503` 保持正确。真实 OIDC、PostgreSQL 角色 RLS、Queue、Forgejo/S3、OCI/OpenBao、微 VM、MCP、模型/OTel、Secret/Object/Deployment 灾备、密钥轮换、蓝绿回退、7 天 `/ready=200` 和 G-036 未验证，M32 保持 no-go。

- 2026-08-20：按 M26 W26-01～W26-07 完成本地 MCP/Tool Gateway 控制面批次并形成 E-053。新增 `0029_pi_mcp_policy_gateway.sql`、MCP Registry 的 InMemory/Postgres Store、Ed25519/FailClosed Server 校验、HTTPS/精确出站 allowlist、Capability Probe/Schema freeze、Server/Binding 审核撤销、Managed Secret/OAuth Broker 接口、MCP Bridge、Tool Gateway 双检、结果脱敏、调用审计、按 Server 熔断、Session/RunManifest MCP 快照和管理/目录 API。2 个 M26 聚焦文件/7 项测试、全量 81 个测试文件/289 项测试、typecheck、零警告 Lint、隔离目录 Next build、dedicated Runner esbuild 和 project-to-act check/validate 通过；真实 OpenBao/OAuth、测试 MCP、出站代理、SSRF/DNS/Metadata 攻击矩阵、生产 QuotaPort、持久幂等账本、G-029 前置和 G-030 仍未通过，生产 MCP capability 保持关闭。

- 2026-08-20：按 M26 安全加固形成 E-054。修复 MCP 注册所有权不信任客户端输入、凭据引用必须绑定 `secret://tenants/{tenant_id}/`、Probe/Schema freeze/Binding resolve 的签名与 Server digest 重验、生产直连 Transport 失败关闭；新增注入式 `McpEgressClient`、tenant/actor/trace 透传且阻止凭据覆盖安全头、OpenBao secret client、原子化 Postgres 熔断计数和成功恢复。3 个聚焦文件共 11 项测试、全量 82 个测试文件/293 项测试、typecheck、零警告 Lint、隔离目录 Next build、dedicated Runner esbuild 和 project-to-act 校验通过；Docker、OpenBao、测试 MCP 和真实出站代理仍不可用，G-030 仍 no-go。

- 2026-08-20：按 M27 W27-01、W27-02、W27-04 形成 E-055。本批新增 Approval Contract/Policy Resolver、R2 单人与 R3 双人审批、R4 禁用、proposal hash、TTL、职责分离、决策摘要历史、CAS 幂等、过期/拒绝/撤销/取消、对象版本重验和 superseded 状态；新增 `0030_pi_approval_gateway.sql`、Postgres/PGlite/InMemory Store、持久 Session Event Sink、审批查询/决策 API 与 fail-closed Approver/ObjectVersion/Execution 适配器。审批控制面聚焦测试 7 项、全量 85 个测试文件/301 项测试、typecheck、零警告 Lint、隔离 Next build、dedicated Runner esbuild 和 project-to-act 校验通过；实际 Pi Tool 暂停/恢复 wiring、MCP/Tool Gateway 真实依赖、Forgejo/PR/合并/发布和未知结果补偿未实现，G-030/G-031 保持 no-go，`/ready` 仍为 503。

- 2026-08-20：启动 M21/M22 实现。当前批次先将 Pi Web 控制面改为 RunManifest + 持久命令入队，并把 Pi SDK 执行迁移到独立 `pi-runner` Worker；G-025/G-026 尚未通过，真实微 VM、部署隔离和多进程崩溃演练仍保留为阻塞条件。

- 2026-08-19：按 D-021 完成 M20.2 规划 Gate。仅修改 `.project-to-act` 五份事实文件，将 F-053/M20 的剩余范围拆为 M21～M34、F-054～F-075、PI-MOD/PI-API/PI-INT/PI-EVT 契约、版本列车、G-025～G-038 和逐阶段回退锚点。后续任何实现必须先绑定功能/接口/Gate，形成新鲜 E 证据后才能更新状态；本次不改变源码和生产能力结论。证据 E-048。

- 2026-08-18：按 D-020 启动 M20/F-053。锁定 `@earendil-works/pi-coding-agent@0.84.2`，新增 `PiAgentService`、Profile/权限策略、无项目资源发现的 ResourceLoader、ModelRuntime 环境引用、虚拟沙盒与 fail-closed 沙盒接口、Pi Session/事件/检查点/差异 API、`0024_pi_enterprise_runtime.sql`、Kubernetes Kata 隔离模板和跨租户/路径/工具边界测试。当前实现证明控制面和开发适配器可运行；独立 Firecracker/Kata Runner、持久队列/Worker 恢复、Skill/MCP Registry/签名与审批仍未通过，因此 M20 不标记完成。

- 2026-08-17：形成 E-046 VastMind 单机试点入口证据。通过专用 Ed25519 密钥以 `ubuntu` 登录腾讯云 CVM `ins-kjaai8iv`（北京七区，公网 `82.157.0.21`），安装并启用 Nginx 1.24、Docker 29.7.2、Compose 5.4 与 UFW；主机仅放行 OpenSSH 和 Nginx Full。新增 `deploy/single-host/vastmind-office.nginx`（SHA-256 `54adf0fb241d1be55e23a92732f3c0905293753cc3e8a4283a49b3787b950ff4`），代理到仅绑定 `127.0.0.1:3000` 的应用端口并关闭代理缓冲。配置腾讯云内网 Docker Hub 镜像加速后，按仓库 Dockerfile 构建 `vastmind-office:0.14.0`（镜像 ID `sha256:6f77309c953a8739229ee00d979a94fe3db21b878dab18dd23abbf7bbc797974`），容器以 `nexus` 非 root 用户和 `unless-stopped` 策略运行；`nginx -t`、容器 healthy、反向代理 `/api/v1/health=200` 均通过，未配置生产依赖的 `/api/v1/ready=503` 按预期失败关闭。验证命令均退出 0；证据位置为本条、`deploy/single-host/vastmind-office.nginx` 与目标实例 Docker/Nginx 运行状态，有效期至云侧网络或部署版本变更。公网 80/443 仍被安全组阻断，`office` DNS 与 HTTPS 未完成，不创建 E-035。

- 2026-08-17：按 D-018 完成 M18/F-051，形成 E-044。新增 `0022_agent_memory.sql` 与分级记忆运行时：对话、上下文、长期、任务、情景五层均具作用域、分类、可见性、保留期、版本与来源幂等；共享和显式长期写入经权限/确认门禁，数据库 RLS 与原子审计覆盖。主对话实测真实 `openai-compatible/gpt-5.6` 模型，未降级，第二轮已按权限回注对话、上下文与情景记忆；PGlite 集成覆盖任务与交接记忆。修复待签收交接的冻结状态映射为 HTTP 409。68 个测试文件/240 项测试、typecheck、零警告 Lint 与生产构建通过；`/ready` 仍为 503，真实 IdP、生产 PostgreSQL/运维和企业渠道 E2E 未豁免。

- 2026-08-17：执行 E-043 真实企业微信预检。当前本地 Agent HTTP 服务可达，模型已配置，企业微信连接器明确显示 `configured=true`、`source=dedicated-environment`；三项应用配置存在性检查通过且未输出明文。企业微信 `agent/get` 返回 `60020`，按官方错误码属于不安全的访问 IP。当前调用出口并非已确认的企业自有生产服务器稳定出口，故未扩大可信 IP、未解析收件人、未调用 `message/send`，向“王渊芃”的消息未发送。AAWO 初始 `status=healthy` 假设失败并保留，按实际 `status=ok` 契约纠正回归通过。

- 2026-08-16：按 D-017 完成企业微信独立配置源 F-050/E-042。应用凭据不再进入通用 `.env.local`；专用 `.env.wecom.local` 存在时三项配置完全以该文件为准，避免后续 AI/平台凭据串用。令牌交换、定向消息、应用控制、验收探测、管理通知、回调 CorpID 和 readiness 已统一。CorpID/AgentId 已预填，App Secret 仍由管理员本机录入；65 个测试文件/233 项测试、typecheck、零警告 Lint和 Next.js 16.3.0 生产构建通过，未执行真实外发，外部 Gate 不变。

- 2026-08-15：按用户上线前测试请求执行 E-041 AAWO 审计。`/ready` 返回 503（生产模式仍为演示身份、无 OIDC/PostgreSQL/密钥管理/可观测性/网关防护/备份/企业微信凭据）；任务交接快照、非接收人签收拒绝、消息池反馈隔离、英文提示注入拒绝与任务/消息 SSE 首事件均形成可校验账本，消息池与注入的初始断言偏差已回归通过。发现两项发布阻断：待签收交接的状态推进对外返回 500 而非 409；主 Agent 实际为 `provider=unavailable` 的安全降级，无法完成真实 LLM 路由或交接确认提案。64 个测试文件/231 项测试、typecheck、零警告 Lint、Next.js 16.3.0 生产构建与桌面/390×844 UI 检查通过。目标进程清理被环境策略阻止，演练数据仅留在本机内存进程，不构成生产写入。

- 2026-08-14：按 D-016 完成 M17 本地工程 Gate，形成 E-040/G-019。AI 新增 `wecom.send_application_message`，网页端 R3 确认后由服务端读取应用可见通讯录，完整姓名唯一匹配才调用 `/cgi-bin/message/send`；模型只收到姓名和回执摘要。补充 `invaliduser` 即使 `errcode=0` 也判失败、重名/无匹配停止、租户激活连接和幂等门禁。64 个测试文件/231 项测试、typecheck、零警告 Lint、迁移回归和 Next.js 16.3.0 生产构建通过。加载 `.env.local` 后三项企业微信凭据均缺失，因此未向“王渊芃”发送真实消息，E-035 不创建。

- 2026-08-14：完成 M16 本地工程 Gate，形成 E-039。新增 `wecom-access-control` Skill、只读权限面与网页端 R3 应用配置 Tool、`agent/get`/`agent/set` 网关、管理系统只读 API 和 `0020_wecom_access_control_permissions.sql`。普通自建应用可见范围、通讯录写入、敏感字段和内部角色按企业微信官方边界保持禁用或转人工；AgentId/Secret 仅从服务端配置解析，首页/可信域名限制为白名单 HTTPS。63 个测试文件/226 项测试、typecheck、零警告 Lint、迁移回归和 Next.js 16.3.0 生产构建通过。真实企业微信 Secret 尚未录入、连接未激活、DNS/HTTPS/回调未完成，因此 F-045/E-035 外部 Gate 不变。
- 2026-08-12：在真实“云湃智算”企业微信后台创建并启用自建应用 `VastMind AI 办公助手`，AgentId `1000004`，按用户行动时确认设为全企业可见；应用 Secret 保持隐藏且未写入项目。生成并上传临时图标 `public/brand/vastmind-wecom-icon-512.png`，Kubernetes ConfigMap 写入非密钥 AgentId。已进入“接收消息/API 接收”配置页，但 `office.vastmind.com.cn` 尚无可用 DNS/HTTPS，且正式 `tenantId`/`connectionId` 与生产 Secret broker 未就绪，因此没有提交必然失败的回调校验，不创建 E-035。
- 2026-08-12：完成 M15，形成 E-038。新增 `0019_work_task_handoffs.sql`、个人/跨部门交接权限、任务版本和状态冻结、签收/退回与原子责任切换。`work.initiate_task_handoff`、`work.respond_to_task_handoff` 和 `work.get_task_handoff_trail` 均通过声明式 `work-orchestration` Skill 暴露给模型；发起和签收强制人工确认，查询只读。115/115 项需求行为证据、62 个测试文件/222 项测试、typecheck、零警告 Lint、Next.js 16.3.0 生产构建通过；桌面与 390×844 实测交接链、文件/资料计数和无横向溢出。真实企业 Gate 不变。
- 2026-08-12：完成 M14，形成 E-037。新增 `0018` 的消息池、反馈、事件账本以及任务部门目标字段；正式任务 Tool 改为强制人工确认，并在个人/部门目标、数据范围和承接时重验。新增 `company-communication` Skill，`communication.publish_message` / `communication.add_feedback` 为 R1 沟通能力，不创建任务、负责人、时限、验收或业务状态。112/112 项需求行为证据、62 个测试文件/218 项测试、typecheck、零警告 Lint、Next.js 16.3.0 生产构建通过；桌面实测任务/消息分栏、消息反馈和无横向溢出。真实企业 Gate 不变。
- 2026-08-12：修复本地生产预览与配置了正式 `PUBLIC_APP_ORIGIN` 时的请求来源误拒绝：仅允许同协议、同端口的 loopback 别名（`localhost`/`127.0.0.1`/IPv6 loopback）作为本地例外，正式来源仍固定校验。补充 `proxy` 单元测试；重做主对话工作带，桌面输入区实测 904px、用户消息恢复单列宽气泡，390×844 无横向溢出。受控 `gpt` 配置档状态正常，真实只读请求返回“已确认。”；typecheck、零警告 Lint、62 个测试文件/214 项测试和 Next.js 16.3.0 生产构建通过。形成 E-036，未扩大真实企业 Gate 结论。
- 2026-08-12：M13 的统一企业办公入口界面重新通过本地工程 Gate，形成 E-034。首页移除任务指挥大屏、大标题、显式路由流程、任务状态轨迹和团队容量展示；主对话承接审批、项目、会议、知识、经营、组织与任务等全部意图，任务缩减为右侧高频轻量栏。typecheck、零警告 Lint、61 个测试文件/212 项测试和 Next.js 16.3.0 生产构建通过；桌面与 390×844 生产页面无横向溢出，审批快捷入口和任务“新建”均正确回填自然语言提示。`0.14.0-work-command-center` 恢复为本地工程发布候选，外部企业 Gate 不变。
- 2026-08-12：启动 VastMind 企业微信真实接入。确定生产入口 `https://office.vastmind.com.cn`，将 Kubernetes `PUBLIC_APP_ORIGIN` 与 OIDC 回调替换为正式域名，生成 `docs/19-vastmind-wecom-rollout.md`。DNS 只发现 `www.vastmind.com.cn -> 82.157.0.21`，裸域名无 A/AAAA，`office` 无记录，目标 IP 的 80/443 直连超时；企业微信后台已打开管理员扫码登录页。LLM `gpt` 配置通过受控工具注入 `.env.local` 且未显示凭据。真实回调尚未执行，不得创建 E-035 或标记通过。
- 2026-08-12：根据 D-011 重新打开 M13 界面验收。用户明确统一大前端不是任务管理产品，而是全部企业办公意图经过 LLM Skill/Tool 路由的极简入口；任务只作为高频侧栏。当前开始移除大标题、路由流程、宣传说明、任务状态轨迹和团队容量面板，待形成 E-034 后再恢复发布候选状态。
- 2026-08-12：完成主对话与实时任务发布栏的产品级前端重构。保留所有 Agent/API/任务状态逻辑，新增企业路由账本、对话优先层级、任务发布入口、任务状态轨迹、可访问 Tab、`Ctrl/⌘ + Enter` 发送及 390×844 响应式布局；typecheck、零警告 Lint、61 个测试文件/212 项测试和 Next.js 16.3.0 生产构建通过。桌面和 390×844 浏览器完成视觉验收且移动端 `scrollWidth=375≤390`；切换生产服务后的浏览器交互重连被 URL 安全策略阻止，未绕过且未把该子旅程记为通过。形成 E-033，真实企业 Gate 不变。
- 2026-08-11：M13 本地工程 Gate 通过，形成 E-031/E-032、G-015。`0.14.0-work-command-center` 已实现持久主对话、权限化 Skill/Tool 目录、模型原生工具调用循环、任务使命/任务包、定向分派、公开承接、CAS 并发防护、证据状态机、SSE 可恢复事件流和企业微信同源 Agent 消息链。61 个测试文件、212 项测试、110 项需求证据、typecheck、零警告 Lint、生产构建和桌面/390×844 浏览器通过；配置模型实测可基于当前任务选择 `enterprise-analysis` 且未产生越权写入。真实企业微信、IdP、目标基础设施演练和四周试点未豁免。
- 2026-08-11：启动 M13。用户将“主对话 + 实时任务发布栏”确定为最终产品核心，要求任务可被 Agent 实时拆包、定向分派或由成员主动承接，并可观察个人任务全状态；模型路由必须由 LLM 基于完整上下文自主选择声明式 Skill/Tool，不允许 Python、关键词或固定分支代替理解。版本定为 `0.14.0-work-command-center`，证据待 E-031/E-032。
- 2026-08-05：M12 本地工程 Gate 通过并形成 E-029/E-030、G-014。`0.13.0-management-intelligence` 新增管理节奏与 AI 会前事实包、指标语义/质量、项目组合情景、企业事项 SLA、AI 四维治理和企业微信受控动作；14 个版本化 API、`0016` 强制 RLS/原子审计迁移、内存/PostgreSQL 仓储和持久 Worker 回调链完成。56 个测试文件、203 项测试、103 项行为证据、typecheck、零警告 Lint、生产构建及桌面/390×844 真实浏览器通过。外部企业微信、IdP、基础设施演练和四周试点继续作为生产 Gate。
- 2026-08-05：启动 M12。用户确认在稳定 `0.12.0` 候选上新增网页端 + 企业微信管理智能中枢；范围锁定为管理节奏、指标语义与质量、项目组合情景、企业事项、AI 治理、企业微信受控动作，版本为 `0.13.0-management-intelligence`。基线 52 个测试文件、190 项测试通过；真实企业微信授权仍是外部 Gate。
- 2026-08-05：用户确认“按这个方向”，M11 本地工程总 Gate 通过并形成 E-028。生产工作台改为认证工作区引导和权限过滤的真实项目事实，管理驾驶舱按“目标—风险—决策—行动—证据”闭环呈现，补齐加载、空、失败、开发 Fixture 标识和 Agent 排队/执行/未知结果状态；`DR-010` 进入静态、HTTP 与权限回归，87/87 条行为证据无待办。最终 52 个测试文件、190 项测试、typecheck、零警告 Lint、Next 生产构建和桌面/390×844 真实浏览器通过。最新源码重建 Web/Worker/operations 三镜像，均以 nexus 非 root 用户运行；health=200、无依赖 ready=503、Worker/迁移缺数据库失败关闭。M11 仅代表本地工程候选完成，真实 IdP、三平台企业、预生产灾备/轮换和四周团队试点仍为外部发布 Gate。
- 2026-08-05：M11 第二批非 UI 预验收完成但未形成 E-028。新增 `0015_agent_job_control` 与受控 Job 处置 API：本人只能撤销未开始任务，特权人员凭证据进行未知结果核对、单次重放、终态确认或补偿记录，所有处置幂等并原子审计；排队/处置保持原引用。Worker 增加租户数据库并发槽、角色预算背压指标、瞬时基础设施故障隔离与关闭后停止新领用。87 条规范进入 `behavior-evidence.json`，86 条绑定精确测试名/失败路径，`DR-010` 明确待用户确认。48 个测试文件、183 项测试、typecheck、零警告 Lint、Next 生产构建通过；新增迁移 Runner 回归另有 2 项聚焦测试通过。Web/Worker/operations 三镜像以 nexus 用户构建，health=200、未配置 ready=503、Worker 缺数据库失败关闭、生产依赖漏洞 0；真实 PostgreSQL 16 旅程发现并修复连接池 `UNSAFE_TRANSACTION`，随后 15 个迁移原子应用且三角色同版本心跳落库。Kubernetes 清单 13 个 YAML 文档本地解析通过；本机集群 API 不可用，未取得服务端 dry-run 证据。M11 唯一本地实现缺口为生产 UI 事实纪律，不能发布。
- 2026-08-05：M11 第一批实现证据完成。新增 `0014_durable_runtime`、完整 Inbox 信封、Inbox/Agent/Outbox 原子租约仓储、Agent 确认原子排队、权威渠道身份与执行前权限重检、确定性风险/Outbox ID、崩溃重放、未知结果停止、租户轮转与优雅排空；Readiness 真实检查迁移、三类同版本心跳并实际发送 OTLP 探测。聚焦 7 个测试文件、27 项测试与 typecheck 通过，其中 PostgreSQL 旅程证明确认时风险数仍为 0、Worker 后为 1，崩溃重放后风险/Outbox 仍各 1；证据 E-027。M11 尚未过 Gate：生产 UI、语义证据矩阵、部署制品、全量回归与容器旅程待完成。
- 2026-08-05：启动 M11。T4 架构审计重新检查运行时构造、数据库状态、部署清单、工作台事实来源与测试语义，确认 159 项测试通过不能证明持久 Worker 存在；F-018/F-020 重新打开，`1.0.0-pilot` 前新增 `0.12.0-durable-runtime`。历史 Gate 作为当时范围证据保留，不再用于声称当前生产运行链完整。
- 2026-08-05：完成 M10 企业接入验控制面；`0.11.0-enterprise-acceptance` 增加 OIDC Discovery/JWKS 与三平台分步真实预检、外部企业绑定 Gate、只追加安全证据、绑定收件人摘要的两阶段测试通知和 `unknown` 停止重试。43 个测试文件、159 项测试、类型、Lint、生产构建、0 生产依赖漏洞和敏感字面量扫描通过。AAWO 三条真实 HTTP 旅程证明未配置 IdP/飞书企业时必须 `blocked`；这是失败关闭契约通过，不是外部接入通过。接入控制面 p95 读 50.73ms/预检写 39.5ms；桌面与 390×844 浏览器及日志 0 通过；Web/operations 镜像以 nexus 用户构建，容器 health=200、ready=503、测试外发 Gate=409。证据 E-025、E-026；G-012 仅在本地工程范围通过。
- 2026-08-05：完成 M9 企业管理控制面；`0.10.0-enterprise-governance` 增加生产授权按请求权威重算、目标关联项目原子立项、组织变更独立审批和责任/权限交接、项目基线独立审批及补偿、管理关注去重、结项原子 Gate、决定替代与加签回归。39 个测试文件、145 项测试、类型、Lint、生产构建和 0 生产依赖漏洞通过；73 个设计需求全部进入测试追踪。最终 Web 镜像以 UID 1001 运行并包含 PWA 资产，health=200、未配置 ready=503；HTTP p95 读 24.0ms/写 27.9ms/Agent 26.1ms；桌面与 390×844 真实浏览器通过，组织离职形成 1 条风险交接证据。证据 E-023、E-024；Gate G-011 仅在本地工程范围通过。
- 2026-08-05：完成 M8 自建客户端与全量工程收敛；`0.9.0-client-platform` 包含安装型 PWA、设备 bootstrap/注册/撤销/最低版本/托管策略、加密 Push 订阅、安全 Service Worker、非明文 IndexedDB Internal 草稿与撤销清理。新增全租户表强制 RLS 修复、数据库事务内原子摘要审计和审计历史不可改写策略；33 个测试文件、123 项测试、生产构建、0 生产依赖漏洞和密钥扫描通过。双镜像实际构建/运行，Web 非 root、PWA 资源 200、未配置 ready=503、operations 无配置 fail-closed；HTTP p95 读 29.5ms/写 26.3ms/Agent 降级 21.6ms；真实浏览器桌面/390×844、登记、离线草稿与撤销清理通过且日志 0。证据 E-021、E-022；Gate G-010 仅在本地工程范围通过。
- 2026-08-05：完成 M7 本地生产工程 Gate；`0.8.0-production-readiness` 包含 OIDC Code Flow + PKCE/State/Nonce/JWKS、显式主体映射、签名会话与宽限轮换，生产就绪门禁、受管 Secret broker、结构化脱敏遥测、安全响应头/CSRF/认证限流、AES-256-GCM 备份与校验恢复、双容器镜像、Kubernetes 发布样例和试点/事故手册。29 个测试文件、110 项测试、生产构建、0 高危生产依赖和密钥扫描通过；本地 HTTP P95 达标、双镜像实际构建通过。证据 E-019、E-020；真实 IdP、数据库灾备、三平台企业和团队试点未被豁免。
- 2026-08-05：完成 M6 数据智能、经营与人才治理；`0.7.0-intelligence` 包含战略主题、OKR/KPI 分离、指标口径/来源/观察证据、项目组合、经营复盘、事实/推断/提案分层、RACI 唯一负责人、容量规划和人才证据保护。Agent 经营预判与人才证据包均只读，明确排除 1:1、人才标签、私聊和监控式代理指标；人才事实要求调用人进入显式可见名单。25 个测试文件、101 项测试、生产构建、安全扫描和桌面/390×844 浏览器验收通过。证据 E-017、E-018；Gate G-008 通过，下一步进入 M7。
- 2026-08-05：完成 M5 流程、审批、会议和知识治理；`0.6.0-governance` 包含流程定义版本锁定、条件节点、并行会/或签、职责分离、驳回、委托、加签、撤回、SLA 超时升级，会议多人确认及决定/行动幂等沉淀，文档版本/分级/检索前后双过滤/引用，以及只读 Agent 预审和会议准备。22 个测试文件、92 项测试、类型、Lint、生产构建、高危依赖与密钥模式扫描通过；浏览器实测桌面和 390×844 人机治理闭环。证据 E-015、E-016；Gate G-007 通过，下一步进入 M6。
- 2026-08-05：完成 M4 三平台协议连接器；`0.5.0-connectors` 包含飞书长连接/HTTP、钉钉 Stream/加密 HTTP、企业微信 HTTPS 回调，统一事件、渠道身份、消息/卡片、Token、回执、原子去重、限流重试和 R3 渠道确认重鉴权。16 个测试文件、70 项测试、类型、Lint、生产构建、高危依赖与密钥模式扫描通过；浏览器实测桌面和 390×844 集成控制台。证据 E-013、E-014。真实企业 E2E 仍为 A-005 外部授权项，下一步进入 M5。
- 2026-08-05：完成 M3 Agent 平台；`0.4.0-agent` 包含权限感知上下文和引用、模型路由与降级、Tool Registry、R3 提案哈希确认、并发幂等执行、R4 禁用、持久化审计及恶意输入评测。12 个测试文件、50 项测试、类型、Lint、生产构建和高危依赖审计通过；浏览器实测经营会降级事实包及风险建议—确认—执行—项目回写，390×844 Agent 抽屉可用。证据 E-011、E-012。下一步进入 M4。
- 2026-08-05：完成 M2 管理纵向闭环；`0.3.0-management-loop` 包含目标、项目、里程碑、任务、风险、问题、决策、行动项状态机，PostgreSQL 持久仓储，权限感知 API 和响应式交付指挥舱。31 项测试、类型、Lint、生产构建通过；浏览器实测风险—决策—行动—证据及任务推进，390px 无横向溢出。证据 E-009、E-010。下一步进入 M3。
- 2026-08-05：完成 M1 平台地基；`0.2.0-foundation` 包含模块化领域结构、PostgreSQL Schema/RLS、权限策略、审计脱敏、Outbox/Inbox、Fake Model/Connector 和安全运行状态 API。19 项测试、类型、Lint、生产构建全部通过。证据 E-008。下一步进入 M2。
- 2026-08-05：完成 M0 设计基线；11 个 Markdown 文档、1482 行、73 个定义需求，无空文档、重复定义和失效本地链接。飞书、钉钉、企业微信设计依据已按官方文档核对。证据 E-006、E-007。下一步进入 M1。
- 2026-08-05：完成 0.1.0 可交互原型；保留为视觉概念验证，不视为生产领域实现。证据 E-001 至 E-005。

- 2026-08-27：启动“任务进度落地”第一批（D-041 / F-077～F-080）。需求与方案已写入本台账：F-077 必填门禁+工期（新增 started_at/estimated_days）、F-078 时间线（复用 work_task_events）、F-079 临期/逾期（读时计算+分组）、F-080 AI 只读事实卡（只读工具）。本机仍为内存仓储（未接 PostgreSQL），持久化与后台提醒依赖后续数据库接入；代码改动不提交远端。证据待 E-064。


- 2026-08-27：任务进度落地第一批（D-041 / F-077～F-080）本地工程完成，证据 E-064。typecheck 通过；新增与既有任务测试全绿（全量 452 通过 / 6 失败为 firecracker 沙箱环境 EPERM / 26 跳过）；API 冒烟：缺 startedAt/estimatedDays 发布返回 422，带字段发布 201，GET packages/:id/timeline 200。补建基线缺失的 artifacts 路由。剩余：真实 PostgreSQL 持久化、后台到期提醒 Worker、看板/报表/交接单（F-081～F-086 已规划）。


- 2026-08-27：启动第二批（D-042 / F-081～F-082）。F-081 标准交接单：新增结构化字段与迁移；F-082 看板：board 只读接口 + 模块页。证据待 E-065。


- 2026-08-27：第二批（D-042 / F-081～F-082）本地工程完成，证据 E-065。F-081 结构化交接单（当前进度/已完成/未完成/注意事项）+ 0046 迁移 + 前端展示；F-082 看板 board 接口 + 模块页 + 导航。修复 task-command runtime 版本守卫 4→5。剩余：真实 PostgreSQL 持久化、后台到期提醒、F-083～F-086 规划项。

- 2026-08-27：本地持久化基础设施落地（证据 E-102）。安装 PostgreSQL 16.6 绿色便携版至 D:\pgsql16（无 Windows 服务，pg_ctl 管理）：补齐系统缺失的 VC++ 运行库 DLL、initdb 初始化（UTF8 / scram-sha-256 / 超级用户 postgres）、启动 127.0.0.1:5432、建库 nexus、在 .env.local 写入 DATABASE_URL（已 git 忽略，不提交）、全量 0001～0046 迁移应用、导入演示租户/部门/人员/职位种子数据；重启开发服务器后 task-command 已切换 PostgresTaskCommandRepository，API 发布任务可落库查询。旧内存演示任务随重启清空属预期。启动/停止脚本：D:\pgsql16\start-postgres.ps1 / stop-postgres.ps1。剩余：后台到期提醒、F-083～F-086。

- 2026-08-27：持久化验证通过（补充 E-102）。重启开发服务器后：2 条聊天消息、1 个任务包均从 PostgreSQL 读回（workspace 的 availableTasks/publishedByMe、board 的 tasks 均可见）；人员/部门种子数据正常。稳定性修复：PostgreSQL 与开发服务器均改为 Start-Process 独立后台进程（不再挂靠 exec 会话，避免会话结束被带崩）；PostgreSQL 加入登录自启（Startup 目录 nexus-postgres.vbs）；脚本位于 D:\pgsql16\start-postgres.ps1 / stop-postgres.ps1 / start-dev.ps1。

- 2026-08-27：启动第三批（D-043）：后台到期提醒 + F-083～F-086。需求与方案已写入台账（见 D-043）。本批依赖真实 PostgreSQL（已就绪，E-102）。证据待 E-103。

- 2026-08-27：第三批（D-043 / F-083～F-086 + 后台到期提醒）本地工程完成，证据 E-103。实现：到期提醒+阻塞升级扫描（collectTaskReminderCandidates 纯函数 + service.runReminderScan + scripts/task-reminder.ts --once/--watch，消息用确定性 UUID 幂等去重，阻塞任务走升级通道避免重复提醒）；F-084 成员负载（WorkPerson 扩展 inProgressTaskCount/dueSoonTaskCount/capacityPoints，Postgres/InMemory listPeople 统计，定向分派负载过高返回 warnings，新增只读工具 work.get_member_workload，看板人员负载条）；F-086 报表导出（service.exportReport + GET /api/v1/task-command/reports/export CSV/JSON，看板导出按钮）；F-083 周期摘要（service.generatePeriodicSummary + scripts/task-summary.ts）。验证：typecheck 0 错误；新增单元测试 6/6 + 相关套件 28/28 通过；真实 Postgres 冒烟——负载字段正确、提醒扫描 created=3→再扫 dedup=3→拨快5天 overdue=3、公司池可见 7 条提醒/摘要、CSV 导出带表头、JSON 按人过滤正确。遗留：postgres 集成测试 PGlite/WASM 在当前 Node24 环境 worker 崩溃（既有环境问题，git HEAD 同样崩溃）；blocked 升级用纯函数单测覆盖。

- 2026-08-28：完成“人员 × 项目多视图与拖拽分配提案”（F-087）和离线模型连接恢复修复，形成 E-104。人员 × 项目模块从空白占位重写为矩阵/项目泳道/人员泳道三种视图，复用 board 的 mission.projectId + assigneeId 展示实时关系与进度；人员池/矩阵行头拖拽到项目生成 Agent 提案（onAsk）。修复模型网关瞬时网络失败：重试 + 归一为 MODEL_* 可降级错误，编排器不再把离线 fetch failed 当成 500 硬错误。补齐演示数据：scripts/seed-development-data.sql/.ts 将演示目标/项目/里程碑/任务/风险写入 PostgreSQL，并把已有非模板 mission 关联到演示项目（package.json 增加 db:seed:development）。验证：typecheck 0 错误、lint 0 错误/0 警告、定向 vitest 14/14 通过、真实 PostgreSQL board/bootstrap 冒烟通过。遗留：拖拽仅生成提案未接确认写入；真实企业 Gate 不变。

- 2026-08-28：新增“员工画像”（F-088），形成 E-105。新增左侧导航“员工画像”与独立模块页，从 board 的 requiredSkills 派生员工技能画像，汇总负载/容量/参与项目，列出当前任务，并基于技能重合与开放任务生成“任务匹配建议”，一键触发 Agent 生成分派提案。验证：typecheck 0 错误、lint 0 错误/0 警告、真实 board 返回 requiredSkills、页面渲染成功。遗留：演示数据暂无未分派开放任务（建议区为空态）；拖拽/分派仍走 Agent 提案未接确认写入；真实企业 Gate 不变。

- 2026-08-31：零风险架构清理（E-106，见 docs/ARCHITECTURE_CLEANUP.md）。完成三项：新增 `components/board-client.ts` 统一三个视图（人员×项目、员工画像、任务进度）的 board 类型/取数/状态文案并接入 `useTaskBoard()`；`office-shell.tsx` 以 `viewRenderers` 查表替代嵌套三元路由；`api-response.ts` 改为有序 `errorRules` 表驱动错误映射。验证：typecheck 0 错误、lint 0 错误/0 警告、`tests/unit/api-response.test.ts` 通过、project-people/employee-profile/command/task-progress/today 五页渲染正常。行为不变，不改变任何 RLS/审计/权限语义。

- 2026-08-31：新增“公告中心”（F-089），形成 E-107。消息池消息增加 `kind`（announcement/notice）：0047 迁移新增列与索引，领域 `WorkPoolMessage.kind`、`publishPoolMessageSchema.kind`、Postgres 仓储、Agent `communication.publish_message`（kind 参数）与日报/提醒（notice）全部打通；左侧导航新增“公告中心”页，公告/通知分栏展示，原公告可反馈，发布走 Agent 提案；右侧消息栏未改动。验证：typecheck/lint 0、三个相关单测 25/25 通过、workspace 返回 kind（1 公告 + 7 通知）、页面渲染成功。遗留：公告已读/置顶未做。

- 2026-08-31：架构收敛四件套（E-108，见 docs/ARCHITECTURE_CLEANUP.md）。1) 新增 `components/workspace-client.ts`，工作对话与公告中心共用 `useWorkspace()`，消除 workspace 取数复制；2) 新增 `src/platform/runtime/module-runtime.ts`，12 个 runtime 全部改为模块代际单例（重编译即重建），删除手工版本号；3) 拆 board：新增 `GET /api/v1/task-command/people|packages|missions`，`board-client` 并行取细粒度接口，旧 board 保留兼容；4) 范围收敛：左侧导航默认只显示核心 5 项（对话/任务/审批/公告/人员×项目），其余折叠进“更多模块”；Agent 工具按意图注入（默认办公核心工具，企业微信仅在提及渠道时注入）。验证：typecheck/lint 0、相关单测 37/37 通过、新接口 people=4/packages=3/missions=2、四页渲染正常、Agent 实测 200 正常回答、board 兼容保留。遗留：双仓储切换（#4）与 Pi 部署边界（#7 后半）未做。

- 2026-08-31：按用户要求撤销 E-108 中的“左侧导航更多模块折叠”，恢复完整导航（office-shell/globals.css 回退）；Agent 工具按意图注入保留。仅 UI 回退，无功能损失。

- 2026-08-31：按用户要求移除“人员 × 项目”页的项目泳道/人员泳道视图，只保留矩阵并精修 UI（顶部概览条、项目列进度/成员数、人员行负载、格子悬停与拖拽高亮、统一间距），形成 E-109。验证：typecheck/lint 0、页面不再包含泳道与模式切换、概览条正常渲染。

- 2026-09-01：按用户要求将“人员 × 项目”模块更名为“项目管理”（导航、页面标题、kicker、说明同步），定位明确为“任务与人员管理平台”；矩阵保持 项目列 × 成员行 × 任务格 的形态，形成 E-110。验证：typecheck/lint 0、页面标题与导航更新正常。

- 2026-09-01：按用户要求移除“员工画像”（F-088）：删除导航、viewRenderers 注册、组件 `components/employee-profile.tsx` 与 `.ep-*` 样式，核心明确收敛为“项目管理”；工作对话仍保留任务/沟通能力，形成 E-111。验证：typecheck/lint 0、页面不再包含员工画像入口。

- 2026-09-01：按用户要求将“公告中心”改为“我的任务”（F-090）：移除公告中心页面/导航/样式，新增个人任务页（只看分配给自己的任务，按状态分组，支持开始/解除阻塞/提交验收/完成/交接和“让 Agent 整理”）；消息池 `kind` 字段与右侧消息栏保留，形成 E-112。验证：typecheck/lint 0、导航与页面切换正常。

- 2026-09-02：按用户要求移除“我的任务”（F-090）与“项目管理”（F-087）导航入口与页面组件，任务闭环收敛到工作对话任务侧栏（我的/可承接/已发布/待交接）与任务进度页；连同此前未提交产品改动与 runtime 清理一起提交（commit `ba64439`），形成 E-113。验证：typecheck 0。被移除组件可自 git 历史找回。

- 2026-09-02：任务执行链路缺陷修复与本地授权补齐，形成 E-114。1) Worker 运行时上下文未接 taskCommand，导致含任务包的提案版本校验误报 `PROPOSAL_OBJECT_VERSION_CONFLICT`（`src/platform/workers/runtime.ts` 补齐 taskCommand）；2) 提案确认对无 projectId 的交接/签收类提案强制重建项目上下文而抛 `INTERNAL_ERROR`（`src/modules/agent/application/orchestrator.ts` 改为有 projectId 才做版本校验，与 Worker 语义一致）；3) 本地真实 PostgreSQL 补齐 RBAC 种子（enterprise_manager/employee 角色、权限映射与 user_roles），使 Worker 按数据库授权执行不再 `TOOL_PERMISSION_DENIED`。真实 PostgreSQL 端到端验证：direct 与 open_claim 发布→提案确认→Worker 执行 succeeded→成员承接→发起交接→签收 accepted 全链路通过，事件链 package_published/claimed/handoff_initiated/handoff_accepted 完整，typecheck 0。说明：本地演示需运行独立 Worker 进程并配置角色权限，否则会复现“确认后不执行 / 权限拒绝”；本机 tsx 因 `os.userInfo` ENOMEM 需以 NODE_OPTIONS 预加载补丁启动。

- 2026-09-04：按用户要求放宽任务发布门禁（F-077 语义变更），形成 E-115。用户在对话里怎么写就怎么发：`publishMissionSchema` 的任务包必填字段降为仅 title，缺字段由 `service.publishMission` 归一化为“待补充”占位并记录 mission/package `missingFields`（默认 open_claim 公开承接、占位不参与提醒扫描）；Agent 系统提示与 `work.publish_task_bundle`/`work.create_task_template` 工具描述同步修改——用户要求发布/下发/等待承接时直接调用发布工具，不再要求先补全、不再转纯文字预览；任务卡对非模板同样展示“待补充”标签。验证：typecheck 0；真实 PostgreSQL 冒烟——按“AI 产品经理项目，工期这周末，大概需要 2-3 人”发布 201，package 状态 published/open_claim、出现在可承接池，missingFields 含验收标准/截止时间/优先级/工期/容量点等；Agent 原话触发 `work.publish_task_bundle` 生成待确认提案而非拒绝。遗留：任务补全/转正式入口（占位任务的后续编辑）待后续排期；真实企业 Gate 不变。

- 2026-09-04：工作对话改为纯 Agent 路由（E-116）：移除输入框上方“任务/消息/审批/项目/会议/知识/经营”常用办公入口快捷键（不再手动选域、不再向输入框注入长段规则提示）；“发布任务/推送”按钮点击只填入极短意图词（“发布一项任务”/“推送一条沟通消息”），长分节模板不再注入输入框，域与工具选择完全交给 Agent 按自然语言意图路由。验证：typecheck 0、无残留引用。遗留：主系统提示瘦身与业务规则下沉为各域 Skill（意图路由强化）待后续排期；本地 Worker 需随代码更新重启。

- 2026-09-04：任务管理全流程真实回归（E-117）：管理员按原话发布缺字段任务（open_claim、missingFields 记录）→ 陈屿承接 → 陈屿开始 → 陈屿提交验收 → 管理员验收完成，双角色按数据库授权执行，package 最终 completed（assignee 陈屿、version 5、evidence 落库、completed_at 有值），事件链 package_published→package_claimed→package_status_changed×3 完整；typecheck 0。顺带修复：`requiredSkills` 在直连 service 调用缺失时的容错（undefined 判空）与 schema 声明为可选默认空数组。遗留：占位任务补全/转正式入口与交接签收在完整链路上的再次验证待排期。

- 2026-09-04：发布拦截五类门禁逐一实测（E-118）：1) 权限——无 `work_task:create` 拒绝、无 `work_task:assign` 但定向分派拒绝；2) 引用——负责人不存在、部门不存在、错误会话分别拒绝；3) 逻辑——开始晚于截止、截止在过去（DB `work_packages_check4`）、direct 带部门、open_claim 带负责人均拒绝；4) 安全——注入特征词被安全策略拒绝（不调模型），正常发布生成 R2 待确认提案而非直接生效；5) 漂移——测试发现漏洞并修复：发布提案若输入缺 `projectId`，确认/Worker 的版本漂移校验会整段跳过（模型漏填即漏检，实测漂移后仍可确认执行）。修复：`orchestrator.handleToolCall` 在生成提案时从运行 contextRefs 的项目引用补入 `projectId`（仅 publish_task_bundle），修复后漂移场景确认返回 `PROPOSAL_OBJECT_VERSION_CONFLICT`。验证：9 项服务级拦截全部按预期拒绝、注入 0.6s 拒绝、漂移修复前后对比明确；typecheck 0；测试脏数据与项目版本已还原。遗留：该修复需并入测试分支供对方验证。

- 2026-09-04：修复"受限信息"自污染误拦（E-119）。根因：1) 记忆快照中的组织/对象 UUID（含摘要截断尾巴的残段）未被归一化，宽泛的"13-19 位连续数字"敏感规则把 ID 前缀误判为受限内容，导致整轮上下文包被判 restricted；2) 上下文构建先写入情景/上下文记忆、后计算分级，受限拒答因此自我复制并污染后续轮次；3) 受限拒答以普通 answer 类型写回，进一步进入记忆。修复：`data-classification.ts` 将连字符十六进制 ID（含被截断的尾段，8-hex + 1-4 个 1-12 hex 分段）整体归一化为 `[id]`；`context-provider.ts` 先计算 dataClassification，restricted 时跳过 captureSituation/captureContext；`orchestrator.ts` MODEL_POLICY_DENIED 拒答输出 kind 改为 refusal（不进入对话记忆捕获）。验证：清理污染记忆/会话后，按用户原话"门禁漂移验证2"发布不再受限，正常生成待确认提案；确认后 Worker 执行成功，任务已发布（open_claim published）；typecheck 0。遗留：Agent 检索"未找到"类回答未实际调用任务查询工具即下结论（8c53dcd0 实例）与摘要截断 UUID 的生成侧修复待排期；敏感规则命中定位日志待排期。

- 2026-09-04：查找任务强制走任务事实源（E-120）。新增只读工具 `work.find_task` 与服务方法 `service.findTask`：按标题/描述/所属使命名称在当前用户可见任务范围（我的/可承接/已发布/模板/交接参与/待签收）搜索，返回任务 ID、状态、所在分类与版本；Agent 系统提示与工具描述规定"询问任务是否存在/在哪/按名称查找时，必须先调用 work.find_task，只有返回空才可回答未找到"，不得仅凭记忆或知识库检索下结论。验证：typecheck 0；按"我再哪里看门禁漂移验证2"实测，Agent 路由 tools 含 work.find_task 并正确返回任务 f4150c4c（published/可承接）与位置，不再出现"未找到"误答。遗留：模型网关偶发"暂时不可用"属外部瞬时故障，与本次路由无关。

- 2026-09-04：核心三功能回归（发布/交接/追踪，E-121）：管理员按原话发布缺字段任务（open_claim、missingFields 记录）→ 陈屿承接并开始 → 陈屿发起交接给管理员 → 管理员签收 → 管理员提交验收并完成。package 7662c562 全程 version 1→6，最终 completed（证据 evidence://core-regression-accept、completed_at 有值）；时间线 7 条事件顺序完整：published→claimed→status_changed→handoff_initiated→handoff_accepted→status_changed×2，操作人分别为管理员/陈屿并正确切换；workspace 已发布可见且 status=completed、我的列表按规则排除 completed、board 任务 status=completed；typecheck 0。临时脚本已清理。真实企业 Gate 不变。

- 2026-09-04：项目进度可查性验证（E-122）：1) 数据接口——`GET /api/v1/task-command/board` 返回 12 个任务并按状态分组（assigned×2/claimed×3/completed×2/in_progress×1/published×4，含逾期/临期标记）；`GET /api/v1/management/snapshot?projectId=30000000-0000-4000-8000-000000000001` 返回项目健康 at_risk、目标 88/95%、风险"接口联调晚于基线 2 天"(4x4)、里程碑"华东客户灰度验收:at_risk"、管理任务 in_progress/in_review/blocked 与行动项计数，字段完整可读。2) Agent 自然语言查询（"项目现在进度怎么样/列出未完成任务"）实际调用了 work.find_task / work.get_task_progress / office.read_governance_workspace / office.read_enterprise_intelligence 等查询工具，工具链执行成功；但收尾合成回答时模型网关第二轮请求不稳定，连续两次返回"工具已执行…可在任务栏核验"兜底，未产出可读汇总。遗留：Agent 多工具执行后若模型第二轮失败，应改为确定性的结构化摘要兜底（把已执行工具的结果拼成进度清单），避免返回无信息兜底话术。

- 2026-09-04：工具执行后的确定性汇总兜底（E-123）。`orchestrator.ts` 记录每个已执行非提案工具的返回（executedResults），模型第二轮失败且已执行工具时，不再返回"工具已执行…可在任务栏核验"空话，而是由 `formatExecutedSummary` 把 work.find_task 等返回的 tasks 去重过滤（排除 completed/cancelled/模板）后直接拼成按状态分类的清单。验证：typecheck 0；复现原问题提问"列出智能客服 2.0 华东上线项目里尚未完成的任务"，模型第二轮仍失败但输出已变成可读清单（华东上线联调支持·已分派、华东监控告警值班·已承接、华东客服路由压测·已承接，含所在分类）；tools=work.find_task×4 + office.read_governance_workspace。说明：清单覆盖范围取决于模型实际发起的检索关键词；完整项目盘点应引导走任务进度看板/work.get_task_progress（遗留：为"项目全部未完成任务"提供无关键词的全量只读工具待排期）。

- 2026-09-04：项目任务全量盘点只读工具（E-124）。新增 `work.project_task_inventory` 与 `service.projectTaskInventory`：按 projectId 一次返回当前用户可见的全部任务（含状态、所在分类、使命标题、dueState、模板/待补充标记、负责人），不依赖关键词；Agent 系统提示规定"列出某项目全部/未完成任务或盘点时优先一次调用本工具，不要用 find_task 多关键词猜测"。确定性兜底 `formatExecutedSummary` 对返回的 tasks 同样生效。验证：typecheck 0；原问题复测 Agent 路由含 work.project_task_inventory，输出项目 8 项未完成任务完整清单并按 可承接/已分派/已承接/进行中 归类（含截止、逾期、负责人、待补充字段与测试类任务提示）；模型本轮正常产出汇总。遗留：测试类脏任务（门禁漂移验证2×2 等）归属清晰性待后续核对/清理。

- 2026-09-04：移除输入框上方可见小字"说说你要处理什么"（E-125）：删除 `<label>` 可见文本，改以 textarea `aria-label` 保留无障碍名称，placeholder 维持"输入一件要处理的事…"。验证：typecheck 0。

- 2026-09-04：输入提示文案由"Ctrl / ⌘ + Enter 发送"改为"Ctrl + Enter 发送"（E-126）。验证：typecheck 0。

- 2026-09-04：发送键位改为 Enter 发送 / Shift + Enter 换行（E-127）：`handleComposerKeyDown` 在非组合输入（isComposing）且非 Shift 时回车即 `requestSubmit`，Shift+Enter 保留默认换行；提示文案同步为"Enter 发送 · Shift + Enter 换行"。验证：typecheck 0。

- 2026-09-04：移除输入区安全提示小字"只使用当前账号有权访问的数据和能力"（E-128），保留右侧发送键位提示。仅 UI 文案删除，鉴权逻辑不变。验证：typecheck 0。

- 2026-09-04：清理重复任务（E-129）。Agent 核验确认两对重复（门禁漂移验证2：dd945937/f4150c4c；AI产品经理项目：04c7f2b3/c4f57d1c），但工具目录无删除能力且需模型调用状态工具。直接以管理员身份将多余份标记 cancelled（保留 f4150c4c、c4f57d1c），两对各剩一份 published 且保留审计链（cancelled v2）。说明：用户侧首次"取消"失败原因=模型网关返回"暂时不可用"导致工具未执行；模型网关对瞬时网络错误有最多 3 次退避重试，但对话 run 无自动重跑（需重发），Worker 作业才有租约重试/死信。遗留：任务卡无"取消/删除"直接操作按钮，需经 Agent 或 API；物理删除未提供（当前用 cancelled 保留可追溯）。

- 2026-09-04：取消任务直接操作 + Agent 取消能力明确（E-130）。任务卡顶栏新增"取消"按钮（发布者/承接人视角、非模板、无待签收交接、状态 published/assigned/claimed/in_progress/blocked/in_review 可取消），点击 window.confirm 确认后经 transition 接口置 cancelled，保留审计；`work.update_my_task` 描述与 Agent 系统提示同步明确"取消/删除/清理重复任务=置 cancelled（无物理删除），重复先确认保留份"。验证：typecheck 0。说明：取消按钮与 Agent 两条路径共用同一状态机与服务校验。

- 2026-09-04：取消按钮 UI 路径实测（E-131）：headless Edge 打开工作台定位"华东监控告警值班-公开承接测试"卡片 → 点击"取消"→ confirm 弹窗文案正确并接受 → 任务包 93979b1d 状态变为 cancelled（v3），截图 flow-shots/05-cancel-ui.png。验证完成。

- 2026-09-04：Agent 取消任务强制人工确认（E-132）。新增 `work.cancel_task`（confirmationPolicy: always，只生成待人工确认的取消提案，确认后由 Worker 执行置 cancelled，保留审计）；`work.update_my_task` 移除取消用途（描述、JSON schema 与 zod refine 均拒绝 nextStatus=cancelled，防止绕过取消确认）；Agent 系统提示规定取消/删除/清理重复一律走 work.cancel_task。验证：typecheck 0。说明：部署/测试需重启 Worker 加载新工具。

- 2026-09-04：Agent 取消确认提案实测（E-133）：重启 Worker（含 work.cancel_task）后让 Agent"取消任务 AI产品经理项目 c4f57d1c"，输出 kind=proposal / status=awaiting_confirmation，tools=work.cancel_task，提案预览"将取消任务包…取消后不可恢复并保留审计"，未直接执行；测试提案已清理，任务保留。验证完成。

- 2026-09-04：移除"我的"任务卡上的"开始"按钮（E-134）：assigned/claimed 状态不再显示一键开始按钮，卡片底部改显示相对截止时间；状态推进仍可经 Agent 或后续动作完成。验证：typecheck 0。

- 2026-09-04：待交接改为双向可见（E-135）：服务端 `workspace.pendingHandoffs` 从"仅待我签收（to=我）"扩展为"待我签收 + 我发起待对方签收"，每条带 `direction: incoming|outgoing`；任务侧栏"待交接"列出两类，卡片对 incoming 保持 签收/退回 按钮，对 outgoing 显示"等对方签收"标记（前端用两张映射分离，不干扰原签收逻辑）；find_task / project_task_inventory 分类标注相应调整为 待签收/待对方签收。验证：typecheck 0；workspace 返回 3 条 outgoing（华东客服路由压测、门禁漂移验证2、AI产品经理项目）。说明：出向交接暂无"撤回"动作，属后续项。

- 2026-09-04：撤回我发起的待签收交接（E-136）。服务端新增域函数 `revokeTaskHandoff`（发起人可撤回，状态 rejected + responseNote"发起人撤回交接" + respondedBy=发起人，无接收人限制）、`service.revokeTaskHandoff` 与 `POST /handoffs/[id]/revoke`；Agent 工具 `work.revoke_task_handoff`（confirmationPolicy always，生成待确认撤回提案）；前端 outgoing 卡片增加"撤回交接"按钮（window.confirm），交接链对 rejected && respondedBy=fromAssigneeId 显示"已撤回"而非"已退回"。验证：typecheck 0；服务层与 HTTP 路由分别撤回 5aa4c588（门禁漂移验证2）与 4015e5d6（华东客服路由压测）成功，状态 rejected、respondedBy=发起人，pending outgoing 由 3 降至 1（保留 AI产品经理项目交接作演示）；409 版本不符时正确拒绝。说明：撤回不改变任务负责人（继续留在原负责人）。

- 2026-09-04：待签收交接改为直接签收/退回（E-137）：incoming 卡片的"签收/退回"不再往输入框注入让 Agent 代劳的提示词，改为直接调用 `POST /handoffs/[id]/response`——签收走 window.confirm，退回用 window.prompt 要求至少 4 字原因；成功后刷新并提示。验证：typecheck 0；签收/退回均不再依赖模型网关。说明：Agent 侧仍可用 work.respond_to_task_handoff（带确认提案），两条路径并存。

- 2026-09-04：交接中状态明示（E-138）：任务卡顶栏对存在未完成出向交接的任务显示"正在交接中 · 等待 XX 确认"（在"我的"也可见，不再只靠隐藏按钮让人困惑）；"待交接"标签内仍显示"等对方签收"。验证：typecheck 0。

- 2026-09-09：任务管理 MVP-FIX P0/P1 里程碑（E-139）。P0：任务进度页在看板之外新增表格视图（标题/使命-项目/负责人/部门/状态/开始/工期/截止/到期态/容量点/待补充），支持范围/负责人/使命/状态/仅逾期过滤与按到期态稳定排序，导出复用 `GET /reports/export` 且服务端按同一组参数过滤；看板、表格、聚合统一读取 `/api/v1/task-command/board` 单一授权数据源，进度总览按使命/人员聚合并提供成员负载行（不含完成率，与 P3 解耦）。P1：`development-context` 定义 manager/delivery/product/operations 四个开发验证身份，新增 `GET/POST /auth/development-identities[/switch]`（签名会话 Cookie、服务端白名单按 actorId 反查权限、生产未显式开启时失败关闭），`office-shell` 顶部提供身份切换器，切换后 bootstrap/board/workspace 与任务侧栏按所选身份重建。提交 `3ff66cc`。验证：typecheck exit 0、lint exit 0（0 errors/0 warnings）、全量 513 项测试通过、生产构建 exit 0；切换后 bootstrap 解析为周然、board actorId 随身份变化；浏览器视觉验收留待有驱动的环境。

- 2026-09-09：P2 双通道与 R3 可编辑预览卡（E-140）。提交验收改为直连按钮 + 证据对话框（证据只接受 http(s) 链接或"类型:引用"，服务端仍强制非空）；`in_review` 离开到 `completed`（decision=accept）或退回 `in_progress`（decision=reject + reviewNote ≥4 字）只允许发布人或管理员，执行人自我验收返回 `POLICY_DENIED:work_task:review_decision`，决定与原因写入 `package_status_changed` 事件；发布任务新增表单入口（`POST /missions`，source=human，缺字段标记待补充），口述通道保留；AI 起草的交接单可在提案卡逐字段「修正草稿」，服务端以 supersede 作废旧提案并生成需再次确认的新提案（R3 输入不可原地篡改）；同时收尾 A4 证据格式门禁与按项目健康/里程碑进度条，并移除不属于产品方案的全局"任务时间线"页（仅保留单任务卡时间线与 SSE）。提交 `109b755`/`be50da1`/`221ae2f`/`fe19e26`/`d61309d`/`58c3742`/`576bf9e`。验证：typecheck 0、lint 0、全量测试逐批 513→520 通过、生产构建 exit 0；docs/08 与 docs/18 契约同步；真实 PostgreSQL 重测提交验收→通过/退回链路。

- 2026-09-09：任务包子任务清单（P3，全部按推荐决策）（E-141）。迁移 `0048_work_package_subtasks.sql`（含 down）新增 `work_package_tasks`（RLS + 原子审计 + 版本 CAS + 二态 pending/done + 可选完成说明与证据引用）并扩展 `work_task_events` 事件类型 `package_progress_updated`；域/仓储/服务新增子任务 list/add/update/delete，发布人、承接人与管理员都可拆且记录操作人；任务进入 `in_review/completed/cancelled` 后清单锁定（`WORK_PACKAGE_SUBTASKS_LOCKED`），有未完成子任务时禁止提交验收（`WORK_PACKAGE_SUBTASKS_PENDING:done/total`）；`progress:{done,total}` 挂到 workspace 任务清单，任务卡新增子任务面板（勾选/重开/删除/新增 + 让 Agent 起草勾选建议），Agent 侧新增 `work.list/add/update_package_subtask`（只生成 R3 提案，不直接改状态）。修复：子任务 POST/PATCH 路由改为先注入路径参数再校验 body（此前真实客户端恒 422），`*_LOCKED` 错误码映射为 409。提交 `0360375`/`92a103e`。验证：新增单测与 API 级路由测试、PGlite 集成（落库/CAS/进度聚合/锁定期）、本地开发库应用 0048 后 workspace 恢复 200；真实链路 新增→门禁拦截→勾选→删除→验收→锁定 全部通过。

- 2026-09-09：身份切换 Cookie 超限修复（E-142）。根因：会话 Cookie 内嵌整份 roles/permissions/dataScopes，"开发管理员"约 140 项权限使 Cookie 达 ~4.96KB，超过浏览器单 Cookie ~4KB 上限被静默丢弃，导致切回管理员后仍停留旧身份（周然）。修复：Cookie 只承载最小身份标识（tenantId/actorId/channel/sessionId），权限每请求由服务端按 actorId 从开发白名单或生产授权解析器重建（Cookie 快照从不被信任）；开发切换与 OIDC 回调两条签发通道同时瘦身；旧 Cookie 因字段改为可选校验仍可验签。提交 `4591825`。验证：manager Cookie 实测由 ~4.96KB 降至 ~343B，delivery/manager Cookie 回放解析分别得到周然/开发管理员，新增"最大权限身份 Cookie < 2KB 且权限来自服务端"回归测试；全量 528 项测试通过。

- 2026-09-09：成员管理（员工目录）页（E-143）。`organization` 模块新增 member-directory 纵切（域不变量 + 应用服务 + Postgres/InMemory 仓储 + runtime）：`GET/POST /api/v1/organization/members`、`PATCH/DELETE /api/v1/organization/members/:id`；读需 `organization_member:read`、写需 `organization_member:admin`（开发白名单仅管理员持有；正式环境由授权解析器预置，默认失败关闭）；约束邮箱租户唯一、岗位必须属于所选部门、版本 CAS、禁止停用自己、仍有进行中任务禁止停用（需先完成或交接）；停用为软删除（`users.status=departed` + `archived_at` + 结束现行任职），保留历史任务与审计。入口并入"组织与人才"页（成员目录表 + 新增/编辑对话框 + 停用二次确认，写操作仅管理员可见）。提交 `7c444a4`。验证：单测 13 项 + PGlite 集成（CRUD/RLS/users 与 memberships 审计行/在岗任务保护/邮箱大小写不敏感唯一）、全量 541 项通过、typecheck/lint/build exit 0；真实 dev server 链路 新增→任务可指派人员即时可见→改部门岗位→软停用→目录消失 通过。

- 2026-09-10：员工入职登记 Agent 通道（E-144，按用户要求"把名字打上就好"）。新增 `organization.list_members`/`add_member`/`update_member`/`deactivate_member` 四个工具与 `organization-member-directory` Skill，接入 agent runtime、技能目录与系统提示词：登记与资料维护为 R1 直写（只建立/维护名册，**不授予角色或权限**、无外部副作用、可软删除），停用/离职为 R2 必须人工确认；姓名放宽为唯一必填（单字姓名合法，部门/岗位/邮箱可留空，后续用 update 补全），并明确禁止编造 orgUnitId/positionId。同时修复两个通用缺陷：(1) `filterToolsByIntent` 的核心技能白名单未包含新技能，导致新工具在注入模型前被裁掉（模型因此回答"没有员工登记通道"）；(2) 工具入参不合规时会把整轮对话打成 422，改为把校验问题回灌模型纠正重试，仅在反复失败且从未执行时降级为可读说明。真实会话验证：对"今天新入职了一名员工叫张三"，Agent 调用 `organization.add_member` 仅凭姓名登记张三（memberId `834e63e2-fb96-4e6b-8138-4712a3c36bc5`、active、v1、部门/岗位/邮箱留空），skills=["organization-member-directory"]、tools=["organization.add_member"]，成员目录与任务可指派人员列表均出现张三。验证：typecheck 0、lint 0、新增工具单测 6 项、全量 548 项测试通过、dev server 真实 E2E 通过。

- 2026-09-10：停用/离职员工不得再进入枢纽 Agent（E-145，用户报告"删去的员工仍能进入"）。根因：生产路径本就按 `users.status='active' AND archived_at IS NULL` 拒绝（`PostgresAuthorizationResolver` 非在职返回 null → 401），但**开发/内网验证身份路径没有校验员工状态**——会话 Cookie 只证明"曾经是谁"，停用后旧 Cookie 仍可进入；且开发身份切换器仍会列出并允许切换到已停用成员；`organization.deactivateMember` 也只改 users/memberships，未收回角色授权、委托、客户端设备与外部身份。修复：新增平台级 `EmployeeStatusChecker`（Postgres 查员工档案；无数据库时用内存标记，内存成员仓储在停用/新增时同步）；`resolve-request-context` 开发分支在验签通过后校验在职状态，非在职直接 `AUTHENTICATION_REQUIRED`（**不回退成默认管理员**，避免把"已停用"变成提权）；`GET /auth/development-identities` 过滤掉已停用身份，`POST .../switch` 拒绝并返回 `403 DEMO_IDENTITY_INACTIVE`；成员停用在同一事务内追加收回 `user_roles`（到期）、`delegations`（撤销）、`client_devices`（revoked）、`external_identities`（revoked），与组织异动"离职"口径一致。验证：新增 `tests/integration/development-identity-gate.test.ts` 2 项（停用后旧会话 401 且不降级为管理员；停用身份从切换列表消失、切换被拒、其他在职身份不受影响）与 Postgres 集成"停用收回设备与角色"断言；真实 dev server 复核——本地库中用户已在界面停用的陈屿：可切换身份只剩 manager/delivery/product，切换到 operations 返回 403 `DEMO_IDENTITY_INACTIVE`，为其签发的旧会话访问 `/organization/members` 与 `/task-command/workspace` 均 401 `AUTHENTICATION_REQUIRED`，在职开发管理员访问成员目录仍 200。

- 2026-09-10：重新启用（恢复在职）成员（E-146，用户要求补"重新启用"入口）。新增 `POST /organization/members/:id/reactivate`（`expectedVersion` CAS + 可选 `orgUnitId/positionId/isManager`，留空沿用停用前任职）：域函数 `reactivateMember` 只允许 `departed` → `active` 并清空 `archived_at`（非停用成员报 `MEMBER_NOT_DEPARTED`），仓储新增 `lastMembership`（取最近一次任职供沿用）与 `reactivate`（更新 users + 新建现行 membership，同一事务）；`GET /organization/members?includeDeparted=true` 会连已停用成员一起返回，默认仍只在职；成员管理页新增"已停用成员"分区与「重新启用」对话框（可指定部门/岗位/负责人，留空沿用停用前设置）；Agent 侧新增 `organization.reactivate_member`（R2、强制人工确认，确认卡说明"不自动恢复角色授权与设备"）。设计边界：只恢复在职与任职，停用时被收回的角色授权、委托、客户端设备与外部身份**不自动回滚**（停用已覆盖原到期时间，无法精确还原），需管理员重新授予或重新登录/重新绑定，避免一次误停用把权限静默恢复。验证：域/服务单测（仅在停用可启用、版本 CAS、岗位归属、部门不存在、includeDeparted 可见性）、Agent 工具单测（R2 确认策略、预览、重新启用后不可重复启用）、Postgres 集成（重新启用后 users.status=active/archived_at 清空、新建现行任职、留空沿用原部门岗位）、身份门禁测试补充"重新启用后旧会话恢复可用且可再次切换"；真实 dev server 复核——把用户先前停用的陈屿重新启用（版本 2→3，恢复运营中心/运营负责人），可切换身份重新出现 operations，切换到 operations 返回 200 且会话可访问员工目录，任务可指派人员列表重新包含陈屿。

- 2026-09-10：project-to-act 台账改为强制门禁（E-147，用户要求"台账要一直写"）。此前台账只在 `.ai-team/TASK.md` 中作为约定，缺少可执行校验，导致 2026-09-09~10 的一整批交付（P0~P3、成员管理、Cookie 修复）漏记。现在把"改代码必须写台账"做成和 TASK.md 同级的门禁：`.ai-team/check.mjs --base <base>` 在比对 Git 变更时，若存在代码/产品文件变更但 `.project-to-act/PROJECT_PROGRESS.md` 未更新，直接判为 blocked（错误文案 "Code or product files changed without recording the batch in .project-to-act/PROJECT_PROGRESS.md"）；`.project-to-act/` 归入协作文件类别（不因此额外要求 TASK.md），台账目录不存在时（如对外公开快照）该条不适用。AGENTS.md 增补"长期要求"章节：明确 PROGRESS 每次必写、FEATURES/VERSIONS/ACCEPTANCE 按适用性同步，并写明强制校验命令。验证：新增 `tests/integration/project-ledger-gate.test.ts` 2 项（临时 Git 仓库中"只改代码不写台账"→ 门禁 exit 1 且输出含台账路径与 Result: blocked；"代码+台账同批"→ exit 0 且 Result: valid）通过；对真实仓库运行 `node .ai-team/check.mjs --base 1ee59d8` 得到 Result: valid。

- 2026-09-10：离职名单仅管理员可见（E-148，用户要求"只有管理员能看到谁已离职"）。此前 `includeDeparted=true` 只要求 `organization_member:read`，任何成员都能枚举谁被停用/离职。修复：`MemberDirectoryService.list` 把该参数收紧为 `organization_member:admin`——非管理员显式索取直接 `403 ACCESS_DENIED`（不静默降级，便于暴露越权），默认目录只含在职成员；`applicationErrorResponse` 增加 `POLICY_DENIED:organization_member:admin` 的专门文案（"已停用/离职成员只有管理员可以查看；成员的停用与重新启用也需要管理员"）；Agent 工具 `organization.list_members` 增加 `includeDeparted` 入参并说明"只有管理员可用"；成员管理卡片改为先取在职目录拿到 `canManage`，仅管理员再补取已停用名单，普通成员的目录里根本不含离职行。验证：服务单测（普通成员默认看不到、显式索取被拒、管理员可见且状态为 departed）、Agent 工具单测（普通成员经工具同样拿不到）、新增 HTTP 契约测试 `tests/integration/organization-member-api.test.ts`（普通成员默认目录不含离职者；`?includeDeparted=true` → 403 ACCESS_DENIED；管理员 → 200 且能看到 departed；非管理员重新启用 → 403）；真实 dev server 复核：以周然身份取目录只在职且 canManage=false、索取离职名单 403 ACCESS_DENIED，以开发管理员身份可取到含已停用成员的目录。同时清理了本次会话测试产生的三条停用噪声记录（验收测试员工×2、离职可见性验证），保留演示成员与用户自己删除的张三。可见性边界经用户复核确认为 D-044：非管理员保留在职同事目录，只有已停用/离职名单限管理员（未采纳"整个目录仅管理员可见"）。

- 2026-09-10：P4 站内通知最小版（E-149，用户按改造计划要求继续 P4）。问题（缺陷清单 A3）：分派/交接/验收全链路没有主动提醒，只有需要外部调度常驻的临期/逾期提醒脚本，任务管理退化成"自己不停刷新"。本批补齐站内通道：新增迁移 `0049_work_task_notifications.sql`（+down）建 `work_task_notifications`（收件人/操作人/六类 kind/指向任务包与交接/`source_event_id`/`read_at`，`(tenant_id, recipient_id, source_event_id)` 唯一，FORCE RLS 四策略 + `nexus_atomic_audit` 触发器）；域新增 `WorkTaskNotification`/`WORK_NOTIFICATION_KINDS`/`createWorkTaskNotification`；仓储契约的五个写方法（`publishMission`/`claimPackage`/`transitionPackage`/`initiateHandoff`/`respondToHandoff`）增加可选 `notifications` 参数，Postgres 实现把它们插在**同一 `withTenant` 事务**里（CAS 守卫之后、事件之后），InMemory 实现同语义；服务层在六个触发点生成通知——定向分派→负责人、公开承接被他人领取→发布人、交接发起→接收人、交接签收/退回/撤回→对手方、提交验收→发布人、验收通过/退回→承接人（退回原因进正文），收件人等于操作人时不生成，`open_claim` 只挂部门池无收件人，子任务勾选不通知；新增 `GET /task-command/notifications`、`POST /task-command/notifications/:id/read`、`POST /task-command/notifications/read-all`（复用 `work_task:read`，收件人恒为会话身份，越权与不存在统一 404 `WORK_NOTIFICATION_NOT_FOUND` 不泄露存在性），`workspace` 载荷同时携带 `notifications` + `unreadNotificationCount`；前端工作台右栏新增「通知」页签（未读角标、逐条/全部已读、「查看任务」跳到对应任务卡）与顶栏全局铃铛（30 秒轮询 + `nexus:task-command-changed` 立即刷新）；Agent 侧只加一个只读工具 `work.list_my_notifications`（R0/never，只返回本人通知）。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、新增单测 `tests/unit/task-notifications.test.ts` 9 项与 `tests/unit/work-notification-agent-tool.test.ts` 2 项、PGlite 集成在 `tests/integration/postgres-task-command.test.ts` 增 1 项（同事务落库 + `source_event_id` 对齐事件 + 旧版本 CAS 冲突不写通知/不写事件 + 只能本人读取与标记已读 + 越权 404 + FORCE RLS/审计触发器/策略数）、新增 `tests/integration/task-notification-api.test.ts` 2 项（分派通知只对收件人可见并可幂等标记已读/全部已读；越权标记他人通知 404；`limit=0` → 422 VALIDATION_FAILED）；既有 `member-directory-postgres.test.ts` 迁移清单补 0049（停用保护用例会发布任务包）。边界（写入 docs/18）：只有站内通道，无飞书/钉钉/企微/短信/邮件与 Web Push，无通知偏好/免打扰、无已读回执、无留存清理（列表默认 30 上限 100），提醒脚本常驻调度仍不在此批。证据口径澄清（复核仓储实现后确认）：生产路径的"同事务"由 `src/platform/database/postgres.ts` 的 `withTenant`（`sql.begin`）保证，通知插入位于 CAS 守卫通过之后的同一回调内，异常会随事务回滚；但 `tests/integration/postgres-task-command.test.ts` 的 PGlite 适配器只 `set_config` 不包事务，故该用例证明的是**写入顺序与 CAS 早退（冲突时既不写事件也不写通知）**，回滚语义由生产适配器代码保证、未做故障注入验证；内存实现同样不模拟回滚（与既有仓储一致），仅保持同一组语义。复用 `work_task:read` 而非新增权限码，是因为 `work_task:read` 已在 `development-context.ts` 的四类开发身份与 `DEVELOPMENT_PERMISSIONS` 中，正式环境亦由授权解析器按角色预置，无需新增迁移权限目录行。

- 2026-09-10：P4 第二半——提醒挂常驻调度 + 以「系统」署名 + 提醒到人（E-150，用户确认"以系统署名"）。缺陷：`scripts/task-reminder.ts` 只有 `--watch` + `setInterval`，用 `createDevelopmentRequestContext` 伪造开发管理员身份，并且只扫 `DEMO_TENANT_ID` 一个租户——生产上既扫不到真实租户，也把定时提醒冒名成某个人发出；Node 默认还会因未捕获的 rejection 直接崩掉进程。改造：新增 Durable Worker 角色 `task-reminder`（`TaskReminderWorker implements TenantWorker`），复用 `WorkerSupervisor` 的活跃租户枚举、心跳与 SIGTERM 排空，按租户做间隔节流（`TASK_REMINDER_INTERVAL_MS` 默认 1 小时，失败不推进节流以便下周期重试）并对同租户做重叠保护，扫描结果写结构化日志与 `task_reminder.scan.total` 计数；`src/platform/workers/runtime.ts` 注册该角色（`WORKER_ROLES=task-reminder npm run worker`），`worker_heartbeats.role` 约束经 `0050` 迁移放宽。署名：新增 `runScheduledReminderScan({ tenantId, now })` 系统入口（HTTP/Agent 仍走 `runReminderScan`，按调用人署名、`source='agent'`），两者共用同一段扫描实现；池消息、通知与消息事件以 `system` 署名写入（`work_pool_messages.author_type`、`work_task_notifications.actor_type`、`work_message_events.actor_type`，`actor_id/author_id` 为空，`source='system'`，`CHECK ((actor_type='user') = (actor_id IS NOT NULL))` 保证署名与 ID 互斥），界面显示「系统提醒」。提醒到人：扫描在池公告之外给负责人写 `task_due_soon`/`task_overdue` 通知、给负责人与发布人写 `task_blocked` 升级通知（无人承接的公开任务只留池消息），新增 `saveNotifications` 批量幂等写入；通知没有对应任务事件行，改用 `日期+任务+类型+收件人` 的确定性 `source_event_id`。`scripts/task-reminder.ts` 改为复用同一系统入口（可 `--tenant <uuid>`，默认遍历活跃租户），移除误导性的 `--watch`。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 138 文件/584 项通过、生产构建 exit 0；新增单测 `tests/unit/task-reminder-worker.test.ts` 7 项（系统署名、重复扫描幂等、逾期与阻塞升级的收件人、无人承接不产生通知、人工触发仍按调用人署名、按租户节流与排空、失败不推进节流）与集成 `tests/integration/task-reminder-scheduler.test.ts` 2 项（系统署名落库 + 池消息/通知/消息事件 actor 为空 + 重复扫描 created=0/deduplicated=1、heartbeat 接受新角色、署名约束拒绝 system 挂真人 ID）；真实 dev server + 本地 PostgreSQL 复核：迁移 `0050` applied，`npm run task:reminder` 首次 `created=2/notificationsCreated=2`（临期任务 + 历史逾期演示任务）、第二次 `created=0/deduplicated=2`，库内 `author_type='system'/author_id=NULL/source='system'`，`WORKER_ROLES=task-reminder npm run worker` 常驻运行并写入 `worker_heartbeats` 心跳、每次扫描都幂等（0 created / 2 deduplicated）；测试期间产生的任务、提醒消息/事件/通知与心跳已清理（system 消息 0、通知 0、心跳 0）。边界：Windows 下强杀不触发 Node 信号处理，优雅排空未在本机实测（supervisor 的排空逻辑已有既有证据）；`scripts/task-summary.ts`（周期摘要）仍按调用人身份且未纳入常驻调度，属后续项；外部通道（飞书/钉钉/企微/短信）仍未接。
- 2026-09-10：周期摘要并入常驻调度（E-151，用户要求"把这半收干净"）。原 `generatePeriodicSummary` 只被 `scripts/task-summary.ts` 调用：用 `createDevelopmentRequestContext` 伪造开发管理员身份、只扫演示租户，而且把**个人视角**（我负责/我发布/待承接）的摘要发到**公司消息池**广播给全员——既是死代码路径（无 HTTP 无 Agent 工具可达），语义也错位。改造：新增 `generateScheduledSummary({ tenantId, scope, now })`，产出**租户级**日报/周报（在办任务按状态分布、逾期与临期计数、本周期完成与取消、逾期最长三条、提示"逾期与阻塞已单独提醒到负责人"），以 `system` 署名发公司消息池，消息 ID 由「作用域 + 周期」确定（`task-summary:tenant:{scope}:{periodKey}`，日报=UTC 日期、周报=当周周一，共用导出的 `summaryPeriodKey`）；删除原个人视角方法与其 schema，个人视图继续由工作台「我的」、任务进度看板与 `work.get_task_progress` 承载。`TaskReminderWorker` 扩展为"提醒 + 摘要"两件事：摘要按周期键判断（内存标记避免每轮查库，跨进程重复由确定性 ID 兜底）、提醒按 `intervalMs` 节流；新增 `TASK_SUMMARY_SCOPE=daily|weekly` 与 `TASK_SUMMARY_ENABLED=false` 开关，日志与计数为 `task_reminder.summary_published` / `task_summary.publish.total`；`scripts/task-summary.ts` 改为同一系统入口并默认遍历活跃租户（`--scope`/`--tenant` 可选，移除 `--watch`）。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 138 文件/587 项通过、生产构建 exit 0；单测在 `tests/unit/task-reminder-worker.test.ts` 增至 9 项（新增"摘要按周期键只发一条 + 跨周期重发"、"可用开关关闭摘要"），原 F-083 单测改写为租户级系统署名断言；集成 `tests/integration/task-reminder-scheduler.test.ts` 增至 3 项（新增：摘要系统署名落库、正文含在办与风险、同周期 created=false 且消息 ID 相同、次日重发）；真实 dev server + 本地 PostgreSQL 复核：`npm run task:summary` 首次 `created=true`（`effective=4`、periodKey 2026-09-10）、再次 `created=false` 且 messageId 相同，工作台消息池该条 `authorType=system/source=system/authorId 为空`、正文为租户级统计；`WORKER_ROLES=task-reminder npm run worker` 首轮记录 `summary_published created=false`（与脚本共用同一条幂等记录）且后续轮询不再重发摘要、提醒扫描逐轮 `created=0/deduplicated=1`；测试产生的系统署名消息/事件/通知/心跳已清理（system 消息 0、通知 0、心跳 0）。

- 2026-09-10：P4 全链路实测与发现的一个既有缺陷（E-152）。用户要求"测试一下站内通知（E-149）+ 提醒脚本挂常驻调度"。实测方式：写了一个只用真实 HTTP 接口与真实身份 Cookie 的驱动脚本（`test-artifacts/verify-p4-e2e.ts`），依次跑完 分派→公开承接→交接发起/退回→提交验收/退回/通过→已读/越权/参数校验，共 34 项断言全部通过；随后把测试任务"做旧"（回拨 `created_at/started_at/due_at/updated_at`，因为逾期只能随时间推移产生）再跑 `npm run task:reminder` 与常驻 worker，18 项断言全部通过；周期摘要 `npm run task:summary` 首次 created=true、再次 created=false（messageId 相同）、换周期（`--scope weekly`）created=true；`WORKER_ROLES=task-reminder npm run worker` 写入心跳、首轮 `summary_published created=false`、此后各轮 `created=0/deduplicated=5`（含 6 条通知去重），摘要不再重发。**实测发现的缺陷**：发布会话中把任务包截止时间写成过去时间（想造一个"已逾期"任务）时接口返回 `500 INTERNAL_ERROR`——根因是 `work_packages` 上的数据库约束 `CHECK (due_at > created_at)`（0017 迁移）被触发，而服务层没有对应的领域校验与错误映射；同时这也暴露了一个测试口径问题：既有 F-079 单测靠内存仓储"发布一个 2020 年截止的任务"来验逾期态，而生产 PostgreSQL 从来不允许这种写入。修复：`publishMission` 在归一化后对每个正式任务包校验 `dueAt > now`，违反时抛 `WORK_PACKAGE_DUE_AT_IN_PAST`，并在 `api-response` 映射为 `422`（与 `WORK_INVALID_TIME_RANGE` 同一文案："截止时间必须晚于开始时间，也必须晚于当前时间；已经逾期的任务要随实际时间推移产生，不能在发布时直接写成过去时间"）；F-079 单测改为用同一个领域函数 `dueStateOf` 验证逾期判定 + 真实发布验证临期/正常两态在 workspace 上的口径（并注明为什么不能在建任务时就写成过去）。验证：`npm run typecheck` exit 0、`npm run lint` exit 0、全量测试通过、生产构建 exit 0；真实 dev server 复核"截止写成过去"从 500 变为 `422 WORK_PACKAGE_DUE_AT_IN_PAST`（见 verify-p4-e2e 的第 32-33 项断言）；本轮所有测试任务/通知/系统署名消息/事件/心跳已清理（test_tasks 0、notifications 0、system_messages 0、beats 0），开发库用户可见数据保持原样。边界：无浏览器驱动（headless Edge 在本环境 `--dump-dom`/`--screenshot` 无输出且会挂起），所以顶栏铃铛与右栏通知页签只做到"接口与载荷级"验证，像素级视觉验收仍待有驱动的环境。

- 2026-09-10：P5 第一批——对话体验（E-153，用户要求"开 P5"）。缺陷清单 B1/B2 的收口：模型执行 10~30 秒期间只有转圈、失败要用户重新打字、提案卡直露 `R3 · 人工确认` 与绝对时间、对话空态没有引导。改造：(1) **真实阶段进度**——`AgentOrchestrator.createRun` 增加可选 `hooks.onStage`，在"分类→取授权事实→每轮模型推理→每个工具开始/结束→整理回答"这些真实节点上报 `AgentStageEvent`（`src/modules/agent/domain/agent-stage.ts`，label 由服务端统一生成，含时间戳）；`POST /api/v1/agent/runs?stream=1`（或 `Accept: text/event-stream`）改为 SSE，先 `ready` 再推 `stage`，最后 `final`，运行中失败用 `error` 事件且复用与 HTTP 相同的错误映射（`describeApplicationError` 从 `applicationErrorResponse` 中抽出共用）；不带 `stream=1` 时保持原一次性 JSON 契约，渠道调用方不受影响。阶段上报是旁路：回调抛错或客户端断开都不影响运行与落库（有单测专门验证回调抛错仍成功）。(2) **失败一键重试**——页面把失败回复标记为可重试并保留原始请求内容，新增「重试这条请求」按钮，服务端失败经 `error` 事件转成同一条人话错误，用户不再需要重新打字；流式读取失败时给出明确提示而不是静默。(3) **文案去术语化**——提案卡从 `R{riskLevel} · 人工确认` ＋ 绝对到期时间改为「需要你确认」＋「请在 N 分钟内确认，过期需重新发起」；阶段文案里工具以**所属 Skill 标题**呈现（如「任务拆解与协同调度（第 N 步）」）而不是工具 ID，工具未登记到 Skill 时才回退到 ID。(4) **空态引导**——对话为空时给出三条可点示例（分派任务／登记新员工／拆解项目），点一下填进输入框。顺带修一处数据缺口：`work-orchestration` Skill 的 `toolIds` 只列了 8 个工具（漏了 find_task / get_task_progress / project_task_inventory / 子任务 / 通知等 10 个），导致阶段文案回退成工具 ID；已补全并新增"每个任务协同工具都必须能解析到所属 Skill"的回归测试守住这条不变量。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 138 文件/593 项通过、生产构建 exit 0；新增单测 2 项（阶段序列与"回调抛错不影响运行"、工具阶段含工具与 Skill 与 started/finished）与集成 2 项（SSE 契约：`ready` → `classification`/`context`/`thinking`/`answer` → `final`，阶段文案不含 `R[0-4]`；非流式仍是 `201 application/json`）；真实 dev server 逐帧观测（`test-artifacts/verify-agent-stream.ts`）：`+61ms ready → +130ms 正在理解你的要求… → +144ms 正在检索你有权限查看的事实… → +478ms 正在梳理下一步… → +6207ms 正在执行：任务拆解与协同调度（第 1 步）→ … → +8417ms final kind=proposal`，全程真实阶段、无假进度；探针在主对话留下的 6 条消息已清理。

- 2026-09-10：P5 第二批——纪要文本批量导入任务（E-154，收口 P5 最后一项）。改造计划 P5 第二条："新任务表单同时承载'纪要文本批量导入'（AI 起草 → 逐条确认 → 提交）"。实现：新增只读草稿服务 `TaskDraftService.draftFromMinutes`（`src/modules/agent/application/task-draft.ts`）与接口 `POST /api/v1/agent/task-drafts`（需 `work_task:create`）：把一段纪要按严格 JSON 抽成使命标题/目标/多条任务包（标题、说明、验收标准、所需技能、负责人**姓名**、优先级、时间、工期、容量点），并逐条给出 `missingFields`（待补充）与 `warnings`；**只产草稿不落库**。三条硬边界：(1) 不编造 ID——模型只允许返回负责人姓名，前端按当前名册解析 `assigneeId`，解析不到就按公开承接发布并点名提示；(2) 不编造时间——截止时间必须晚于当前（与 E-152 的发布闸门一致），过期或与开始时间冲突就丢弃并给提醒，不让发布阶段撞库约束；(3) 模型不可用或返回不可解析时退化为"按纪要条目拆候选"，字段全部待补充，功能不因此不可用（已有"缺字段不阻断"逻辑兜底）。前端在「发布任务」对话框加「从纪要批量导入」页签：粘贴 → 「让 AI 拆成任务草稿」→ 逐条勾选/查看待补充项/填使命标题 → 「发布选中的 N 条」走既有 `POST /task-command/missions`（与表单录入完全同一套校验），未选中条目不落库。Agent 侧同一能力为只读工具 `work.draft_tasks_from_minutes`（R0/never，skill work-orchestration），对话里也能"把这段纪要拆成任务"，真正发布仍要经 `work.publish_task_bundle` 人工确认。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 140 文件/602 项通过、生产构建 exit 0；新增单测 `tests/unit/task-draft.test.ts` 6 项（模型抽取规范化 + 不编造 ID + 过去时间丢弃并告警、不可用时按行退化、不可解析时退化、太短/无条目时报错、缺权限拒绝、工具注册为 R0 只读）与集成 `tests/integration/task-draft-api.test.ts` 3 项（草稿前后 `publishedByMe` 数量不变＝不落库；只发布选中的两条且缺失字段继续标待补充；过短纪要 422 而非 500）；真实 dev server 复核：粘贴一段含"周然/林悦/陈屿"的华东上线准备会纪要，返回 `source=model`、3 条任务包且负责人姓名正确抽出、逐条列出待补充字段与两条说明（含"负责人姓名来自纪要原文…对应不上会按待补充处理"）。环境备注：本轮 Next dev 曾因 Turbopack 的 code-frame 高亮器 panic（`end byte index ... is not a char boundary; it is inside '引'`）整体退出——触发条件是编译错误行含中文时 Next 自身格式化报错文本崩溃，属框架缺陷而非产品代码问题；重启 dev server 后一切正常，已记录以便团队遇到时直接重启而不是怀疑业务代码。

- 2026-09-10：P5 真实浏览器验收 + 顺带修掉一个只在新用户身上出现的缺陷（E-155，用户"继续"）。此前 P5 两项的界面证据只到"接口与载荷层"，因为 headless Edge 在本机 `--dump-dom`/`--screenshot` 无输出或挂起。本轮改用 **Playwright 缓存里的 Chromium（`chromium-1234`）+ 原生 CDP**（Node 内置 `fetch`/`WebSocket`/`child_process`，不依赖 playwright 包）驱动真实浏览器：脚本 `test-artifacts/browser-check.mjs` 17 项断言全部通过——顶栏站内通知铃铛渲染（`aria-label=站内通知`）、有历史对话时不出现空态引导、发布对话框两个页签（表单录入/从纪要批量导入）、**真实模型拆分后逐条渲染 3 条草稿**、草稿标注「待补充」与姓名来源说明、发布按钮计数正确（发布选中的 3 条）、草稿标题确实来自纪要（压测/话术）、`/agent/task-drafts` 返回 200、切换到周然身份成功、**无历史对话时渲染空态引导（3 个可点示例）**、点示例把话术填进输入框；并留下截图 `test-artifacts/p5-shots/{01-command-history,02-publish-form,03-minutes-draft,04-empty-guide,01-command-empty-guide}.png`（我这边无法查看图片内容，截图仅作为团队/用户的视觉证据，结论以 DOM 断言为准）。**真实浏览器验收当场发现一个缺陷**：`office-shell.hydratePrimaryConversation` 只在 `persistedMessages.length` 非空时才 `setMessages`，于是"没有任何历史消息的新用户"永远保留初始那句占位问候，`messages.length` 从不为 0，对话欢迎区与 P5 新加的空态引导**永远不会出现**——正是这个功能最该服务的人看不到它。修复：hydrate 无条件 `setMessages(persistedMessages)`（空对话清掉占位问候，让欢迎区与引导出现；有历史时行为不变），修复后脚本由 9/17（含重试与提案卡）迭代到 17/17。命令：`node test-artifacts/browser-check.mjs`（自行启动/关闭 Chromium）。**覆盖范围随后补全到 17 项**：新增"失败一键重试"（用 CDP `Fetch.failRequest` 强制 `/agent/runs` 失败 → 断言出现「重试这条请求」与"原始内容已保留，不需要重新输入" → 解除拦截后点重试 → 断言拿到真实回复"枢纽核验结果：你（周然…）"）与"提案卡文案"（以管理员身份让 Agent 起草发布提案 → 断言卡片含「需要你确认」「请在 10 分钟内确认，过期需重新发起」且**不含 R2/R3**）；等待条件也从"最后一条 assistant 消息"改为"阶段指示消失"，避免运行中误判。过程中顺带发现两处测试自身的问题并修正：周然没有发布权限、不会产生提案（改用管理员身份验证提案卡）；脚本原先依赖"周然主对话为空"因而不幂等（新增前置清理 `test-artifacts/reset-delivery-conversation.ts`）。同时修掉一条自己写的脆弱断言：`tests/integration/task-reminder-scheduler.test.ts` 原先假设两条通知的创建顺序，毫秒级同刻会翻转；改为按类型取用、不依赖排序。运行产生的对话消息已用 `test-artifacts/cleanup-browser-noise.ts` 清理（探针 0 条、周然对话 0 条）。

- 2026-09-10：P5 第三批——「通知」页签与任务卡按钮的真实浏览器点击验收（E-156，用户"先做 a"，即补上 E-155 遗留的最后一块未验证界面）。方法：沿用 E-155 的 Playwright 缓存 Chromium（`chromium-1234`）+ 原生 CDP 链路，新增夹具脚本 `test-artifacts/prepare-task-browser-fixture.mjs`（只用真实接口与真实身份 Cookie：发布 3 条任务包 → 让周然承接其中一条开放任务，给管理员制造真实的 `task_claimed` 站内通知 → 留一条开放任务给管理员点「承接」→ 把直派给管理员的任务推进到 `in_progress`）与验收脚本 `test-artifacts/browser-check-tasks.mjs`（`node test-artifacts/browser-check-tasks.mjs`，自行启动/关闭浏览器）。30 项 DOM + 网络断言全部通过：① 点「通知」页签渲染通知列表，头部显示「N 条未读 · 共 M 条」；② 周然承接后管理员确实收到「已承接」类通知且标题为「任务已被承接：<任务名>」、署名到人（「周然 · 13:33」）；③ 未读卡片有「标记已读」，点击发出 `POST /task-command/notifications/:id/read` **实测 200**，未读数由 2 降到 1；④ 点「全部已读」发出 `POST /task-command/notifications/read-all` **实测 200**，未读归零且「全部已读」按钮与顶栏铃铛角标同时消失；⑤ 点「查看任务」切回任务栏，并按"该任务与我的关系"选中「已发布」分组，真实定位到 `#task-card-<packageId>`（标题与夹具一致）；⑥ 点顶栏铃铛打开通知页签；⑦ 「可承接」页签列出待承接任务，点卡片「承接」发出 `POST /task-command/packages/:id/claim` **实测 200**，切回「我的」后该卡状态为「已承接」；⑧ 「我的」任务卡点「提交验收」打开证据对话框（`#review-submit-title` 文案=提交验收），填入可核验证据（`https://…/evidence.pdf`）后主按钮由禁用转为可用，提交发出 `POST …/transition` **实测 200**，负责人侧状态转「待验收」；⑨ 「已发布」页签同任务为「待验收」，点「验收通过」弹出确认对话框（`#task-confirm-title`=确认操作，文案含任务名与"任务将标记为已完成"），确认后 `POST …/transition` **实测 200**、状态变「已完成」。截图 `test-artifacts/p5-shots/{07-notifications,08-claim,09-review-confirm,10-completed}.png`（我这边模型不支持读图，截图仅作团队视觉证据，结论以 DOM 与网络断言为准）。**本轮未发现产品缺陷**：三次失败全部是我自己脚本的问题（把 `.textContent` 表达式传进只接受 DOM 节点的取值辅助函数导致二次取属性返回 `1`；以及误以为"提交验收后任务会离开「我的」"，实际 `myTasks` 只排除 completed/cancelled，待验收任务仍留在负责人列表——这是既有设计而非缺陷），修正脚本后 30/30 全绿。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 140 文件 602 项通过（26 skipped）、生产构建 exit 0、`node .ai-team/check.mjs --base <base>` valid；夹具产生的 4 个使命/11 个任务包/4 条通知/32 条事件已用 `test-artifacts/cleanup-task-browser-fixture.mjs` 从开发库清理（残留 0；开发库仍是 14 使命/15 任务包，用户可见演示数据未动）。边界：只验证点击链路与接口落库，未做像素级视觉复核（模型不支持读图）；未覆盖交接签收/退回、取消任务、子任务勾选等其他任务卡按钮；通知的外部通道、留存清理与偏好设置仍属后续项。

- 2026-09-10：P5 第四批——把剩下的任务卡按钮也纳入真实浏览器点击验收（E-157，用户"b"）。覆盖：子任务面板（新增/勾选/重开/删除）、取消任务（确认对话框）、交接签收与退回（含退回原因长度本地校验），并顺带验证在同一个浏览器里切换身份（CDP 注入周然会话 Cookie）后「待交接」页签的两条链路。夹具 `test-artifacts/prepare-task-browser-fixture.mjs` 扩到 8 条任务包：①已被周然承接的开放任务（真实 `task_claimed` 通知）②无人承接的开放任务（点「承接」）③直派管理员的进行中任务（提交验收→验收通过）④⑤由管理员发起、分别等周然签收/退回的交接（任务先落在林悦名下并推进到 `in_progress`，再由发布人发起交接）⑥直派管理员的任务（取消）⑦直派管理员的任务（子任务）⑧直派周然并由周然提交验收的任务——它给管理员制造第二条未读通知「待你验收」，让验收链路的通知也进入同一份断言（本批夹具最初只造出 1 条未读，"标记已读"点完「全部已读」按钮就消失了、那条断言会空转；补上这条任务后两条已读路径都有真实未读可清。E-156 那一批本身是 2 条未读，断言当真）。验收脚本 `test-artifacts/browser-check-tasks.mjs` 扩到 **65 项断言全部通过**，新增部分：子任务面板空态（「还没有子任务。」）→ 填标题点「添加」`POST …/subtasks` **实测 201**、条目为「待办 · 拆分：开发管理员」→ 卡片底部按钮被门禁替换为禁用的「子任务 0/1」→ 点勾选 `PATCH …/subtasks/:id` **实测 200**、条目 `is-done`、meta 变「由 开发管理员 完成」、底部恢复「提交验收」→ 再点一次可重开（`is-pending`）→ 新增第二条 → 删除按钮第一次点击变「确认删除？」、第二次点击 `DELETE …/subtasks/:id` **实测 200** 且只剩 1 条；点卡片「取消」弹确认对话框（文案「确认取消任务「…」？取消后不可恢复。」）→ 确认后 `POST …/transition` **实测 200**、「已发布」中状态变「已取消」且取消按钮消失；切到周然身份后「待交接」列出两条待签收任务、卡片同时提供「签收」「退回」→ 点「签收」弹确认（文案含「责任将切换到你的名下」）→ `POST …/handoffs/<id>/response` **实测 200**、任务离开「待交接」并出现在周然「我的」且状态「进行中」→ 点「退回」弹退回原因对话框，先填 2 个字点确认：**不发任何请求、对话框保持打开**（本地拦截生效），改填 13 字原因后 `POST …/handoffs/<id>/response` **实测 200**、任务离开「待交接」；最后切回管理员身份确认工作台仍可用。截图 `test-artifacts/p5-shots/{11-subtasks,12-cancelled,13-handoff-accept,14-handoff-reject}.png`（我这边模型不支持读图）。过程中修了三处**验收脚本自身**的问题（产品代码零改动）：取值辅助函数只接受 DOM 节点，我却传了 CSS 选择器字符串（`document.querySelector('#task-card-x') footer button`）导致注入脚本文法错误，改为 `?.querySelector('footer button')`；`Network.responseReceived` 的 `params.type` 是资源类型不是 HTTP 方法，改为用 `Network.requestWillBeSent` 的 `request.method` 按 requestId 关联，才能真正断言「PATCH/DELETE」；新增 `waitValue()` 轮询任意表达式，避免用只给文本的等待器断言 `className`。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 140 文件 602 项通过（26 skipped）、生产构建 exit 0、`node .ai-team/check.mjs --base main` valid；夹具残留已清理（3 个使命/23 个任务包/23 条通知/6 条交接/2 条子任务/81 条事件删除，开发库仍为 14 使命/15 任务包）。边界：仍未做像素级视觉复核（模型不支持读图）——截图只作团队视觉证据；本轮未覆盖「发起交接」对话框（交接由夹具经真实接口发起）、撤回交接、任务模板补充、时间线展开等剩余交互。

- 2026-09-10：P4 第五批——站内通知的留存清理（E-158，用户"然后是 c 和 d"，本批为 c 的前半）。缺陷：`work_task_notifications` 是"提醒到人"的运行态数据，随分派/承接/交接/验收与每轮临期扫描持续增长，而 0049 之后没有任何清理路径——「通知」页签按人查询会越来越慢、工作区载荷越滚越大，且未读通知可能无限累积。改造：新增 `src/modules/task-command/application/notification-retention.ts`（策略 + 服务），两条线——`readDays`（默认 90 天）清理**已读**通知、`maxAgeDays`（默认 365 天）是**无论已读与否**的硬上限（专门兜住"永远没人点开"的未读），硬上限早于读窗口或读窗口小于 1 天都在启动时抛 `NOTIFICATION_RETENTION_CONFIG_INVALID` 而不是静默照跑。仓储新增 `deleteNotifications(tenantId, { createdBefore, onlyRead, limit })`：Postgres 用 `WITH doomed AS (SELECT … ORDER BY created_at LIMIT n) DELETE … USING doomed` 按批真删（并带 `tenant_id` 谓词），内存实现同语义；服务按批循环、单租户单次最多 `maxBatches`（默认 20）批。触发点在常驻 Worker 角色 `task-reminder` 的同一租户周期里，按 **UTC 日期**每租户每天最多一次（与提醒的间隔节流、摘要的周期键并列），失败不推进"今天已做"标记以便下个周期立刻重试；手工入口 `npm run task:notifications:prune`（`scripts/task-notification-prune.ts`，可 `--tenant <uuid>`，默认遍历活跃租户）。环境变量到策略的映射只有一处 `notificationRetentionOptionsFromEnv`，常驻 Worker 与脚本共用，避免两条路径各写一套默认值；`TaskCommandService` 构造函数新增可选第二参数（默认即生产口径），HTTP 路径也读同一份映射。清理刻意**不开放 HTTP/Agent 通道**——"删数据"不是同事的业务动作，只由后台调度与运维脚本执行。`0051_task_notification_retention.sql`（+down）补 `(tenant_id, created_at)` 索引：0049 的两条索引都带 `recipient_id`，而清理是全租户按时间扫，用不上它们。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 142 文件 616 项通过（26 skipped）、生产构建 exit 0、`node .ai-team/check.mjs --base main` valid；新增单测 `tests/unit/notification-retention.test.ts` 9 项（已读超窗口删/同批未读留、未读超硬上限删、按批分多批删完、批数上限生效、无候选时只查两次、关闭开关连库都不查、非法配置抛错、环境变量映射、服务入口不制造新事实）与 `tests/unit/task-reminder-worker.test.ts` 新增 2 项（每租户每天只做一次且真删超期行、失败不推进当天标记并立即重试）；新增 PGlite 集成 `tests/integration/task-notification-retention.test.ts` 3 项（按批真删 + 其它租户同龄行原样保留 + `audit_events` 里 `database.delete` 留痕 + 任务事实不动；重复清理幂等且事件数不变；关闭开关不删任何行）；真实本地 PostgreSQL 复核（`test-artifacts/verify-retention.ts` 12 项断言全通过）：`0051` applied、`npm run task:notifications:prune --tenant …` 实测 `readPruned=1 / expiredPruned=1`、120 天前已读被删而 120 天前未读保留、500 天前未读被硬上限清理、2 天前新通知保留、`audit_events` 有 2 条 `database.delete`、重复运行仍 exit 0；常驻 Worker 实跑日志 `task_reminder.notification_retention_pruned {"enabled":true,"readPruned":0,"expiredPruned":0,"batches":2}`（与提醒扫描并列同一轮），复核产生的通知/池消息/心跳已清理（notifications 0、system_messages 0、beats 0）。边界：清理是"删除运行态通知"，不触碰任务与事件链（append-only），也不清理审计历史（审计按设计不可改写）；留存窗口是可配置口径，不是合规保留策略，企业如需按行业要求保留更久要显式提高 `TASK_NOTIFICATION_RETENTION_DAYS`/`TASK_NOTIFICATION_MAX_AGE_DAYS`；本批不含外部通道投递（c 的后半，另批交付）。

- 2026-09-10：P4 第六批——站内通知的外部通道投递与通知偏好（E-159，用户"然后是 c 和 d"的 c 后半）。缺陷：站内通知只在平台内可见，人离开工作台就收不到；同时 `0004` 迁移早就建了 `channel_preferences`（`ordered_providers`/`quiet_hours`/`digest_enabled`）却一直没有读写路径，"外部通道/免打扰"在台账里一直挂着待办。改造三块：(1) **投递管线**——新增 `TaskNotificationChannelDispatcher`（`src/modules/integration/application/task-notification-channel.ts`）：候选是最近 `lookbackMinutes`（默认 60）分钟内产生的站内通知（按 `created_at` 窗口 + `LIMIT` 取，因此不需要新增 `dispatched_at` 列，**跨周期重复由投递台账兜住**），投递键 `task-notification:<通知 ID>` 落到 `connector_deliveries(tenant_id, notification_id)` 唯一约束上；**三重门槛缺一不发**：收件人绑定且**身份已验证**、连接状态 `active`、本人偏好里显式包含该通道；跳过原因进 worker 日志的 `reasons`（`NO_BOUND_CHANNEL`/`CHANNEL_NOT_OPTED_IN`）；免打扰命中的通知本轮记 `deferred: QUIET_HOURS`，留在窗口里等下一轮。(2) **换通道与凭据**——`RuntimeTaskNotificationChannelSender` 按**连接**构造连接器（出站凭据与 `AuthenticatedConnectorTransport` 都是按连接配置的，而 registry 只按 provider 索引），策略是"只有已知不可重试的失败才换下一个通道"：限流与结果未知一律不换，避免"第一条其实发出去了、换通道又发一条"的重复打扰；没有配好凭据时以 `CONFIG_REQUIRED:*`/`UNCONFIGURED` 失败并记进台账，不崩 worker、也不假装成功。(3) **偏好与调度**——新增 `ChannelPreferenceService` 与 `GET/PUT /api/v1/me/channel-preferences`（只写本人，schema `strict()`：带 `userId` 之类多余字段直接 422，没有"管理员替别人设通道"的入口；`quietHours` 传 `null` 清空、不传保留），新增独立 Worker 角色 `notification-dispatch`（`WORKER_ROLES=notification-dispatch npm run worker`，`0052` 迁移放宽 `worker_heartbeats.role`；与 `task-reminder` 分开部署，因为外部投递要碰企业凭据与第三方限流），按租户间隔节流（`TASK_NOTIFICATION_CHANNEL_INTERVAL_MS` 默认 5 分钟）、失败不推进节流时间。**默认关闭**：只有 `TASK_NOTIFICATION_CHANNELS=enabled` 才启用，关闭时连租户扫描都不做。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 148 文件 638 项通过（26 skipped）、生产构建 exit 0、`node .ai-team/check.mjs --base main` valid；新增单测 `tests/unit/task-notification-channel.test.ts` 12 项（开关关闭不扫描、无绑定通道跳过、默认偏好只走站内、opt-in 后投递且消息含抬头/正文/深链、免打扰命中延后与窗口外恢复、已投递不重发、failed/retry_scheduled 分类、空候选、偏好顺序决定优先级、跨午夜免打扰判定、环境变量映射、消息渲染）、`tests/unit/task-notification-channel-sender.test.ts` 6 项（首个通道成功即止、已知不可重试失败才换通道、限流不换通道且排定重试、全失败返回最后分类、`existing` 读台账、无通道显式报错）、`tests/unit/channel-preferences.test.ts` 6 项与 `tests/unit/task-notification-dispatch-worker.test.ts` 6 项（含原因聚合）；集成 `tests/integration/task-notification-channel-postgres.test.ts` 4 项（窗口内候选与批量上限、只认"身份已验证 + 连接 active"、偏好往返一致且外部投递读到同一份、内存仓储同语义）与 `tests/integration/channel-preference-api.test.ts` 3 项（默认值非 404、两个身份各写各的、非法与多余字段 422）；真实本地 PostgreSQL 复核（`test-artifacts/verify-notification-channel.ts` 8 项断言全通过）：`0052` applied、`heartbeat` 接受新角色、**开关关闭时连投递日志都没有**、打开后扫描到 1 条并因"没有绑定通道"记为 `skipped=1` 且 `reasons={"NO_BOUND_CHANNEL":1}`、`delivered=0`（未产生任何外部请求），复核数据（通知 + 心跳）已清理。边界：**真实外部投递未在本地验证**——本机没有企业 IM 凭据与可用连接，因此"消息真的到达飞书/钉钉/企微"只有代码路径与假连接器级证据（`FakeConnector` 级单测 + 失败分类），企业侧仍需在配好连接与凭据后按 docs/18 §4.8 做一次真机验收；偏好设置界面（成员自助页）仍待产品排期，目前只有接口；`digest_enabled` 只做透传保留（站内通知尚无"摘要类收件人"语义）；`orderedProviders` 目前把"顺序"与"opt-in 名单"合并成一份数据，未来若要支持"开了通道但临时静音"需再拆一层。

- 2026-09-10：P5 第五批——Agent 回答的 token 级流式输出（E-160，用户"然后是 c 和 d"的 d）。缺陷/难点：模型执行 10~30 秒期间只有阶段文案在转，回答是"生成完一次性出现"；而 Agent 的模型调用要求**结构化 JSON**（`{"answer": …, "skillsUsed": […]}`，见 `finalAnswerSchema`），所以不能像普通聊天那样直接把原始分片丢给界面——那样用户会看到 `{"answer":"` 这种协议噪声，也等于展示未经服务端校验的模型文本。改造：(1) **增量抽取**——新增 `AnswerStreamExtractor`（`src/modules/agent/domain/answer-stream.ts`）：在 JSON 流里定位**顶层 `answer`** 键的字符串值，**增量解码转义序列**后按可用字符吐出；只认顶层键（嵌套对象里的同名键不会被误当回答）、转义序列跨分片不吐半个字符（`\`、`\n`、`\uXXXX`、代理对都等到凑齐）、模型把 JSON 包在 ```json 代码块里也能工作、`answer` 不是字符串时直接放弃抽取不抛错。(2) **模型通道流式**——`ModelGateway.completeStream?` 为**可选能力**：`OpenAICompatibleModelGateway` 用 `stream: true` 解析 OpenAI 兼容的 `data:` 分片（文本增量与按 `index` 累积的工具调用参数），`FakeModelGateway` 按固定大小切片（便于测试与开发）；上层在没有 `onDelta` 监听或通道不支持流式时退回原来的整段 `complete`。失败语义：**已经流出内容后再出错不重试**（避免同一段话生成两遍），没流出内容时才回退到非流式重试。(3) **编排与契约**——`AgentRunHooks.onDelta` 与 `AgentAnswerDelta`（`{text, round, at}`）；SSE 通道新增 `delta` 事件（在 `final` 之前），非流式 JSON 契约完全不变；界面在"正在处理"气泡里渲染 `.command-draft` 预览，收到 `final` 后用正式回答卡片替换它（预览只是预览，最终回答仍是服务端解析校验后的 `run.output.content`）。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 150 文件 673 项通过（26 skipped）、生产构建 exit 0、`node .ai-team/check.mjs --base main` valid；新增单测 `tests/unit/answer-stream.test.ts` 10 项（逐字符流式的增量拼接等于最终回答、分片边界落在键名/冒号/引号/正文中间、转义与 unicode/代理对跨分片、只认顶层 `answer`、代码块包裹、字段顺序无关、`answer` 非字符串、非法转义与截断流、状态机终止）、`tests/unit/model-gateway.test.ts` 新增 5 项（边收边报增量并拼成与非流式一致的响应、按 `index` 累积工具调用参数、服务端不接受 `stream` 时退回非流式、已流出内容后出错不重试且只请求一次、空流按响应非法处理）、`tests/unit/agent-orchestrator.test.ts` 新增 3 项（增量按顺序且拼接等于最终回答、没有监听或不支持流式时不走流式、预览回调抛错不影响运行结果）；集成 `tests/integration/agent-stream-delta.test.ts` 2 项（**端到端**：把 `OPENAI_*` 指向被替换的 fetch，模型逐字符分包推送 SSE，断言 SSE 里出现多个 `delta`、拼接结果等于 `final.run.output.content`、增量早于 `final`、且不含 `{"answer"` 之类的协议噪声；工具调用轮没有 `answer` 字段时不产生增量，只有最终回答轮产生 `round=2` 的增量）；**真实浏览器验收**（`test-artifacts/browser-check-stream.mjs`，Chromium + 原生 CDP，用 MutationObserver 记录预览的每次变化）：10 项断言全通过——提交后在真实 dev server 上观测到**增量写入**的预览长度序列 `1→61→137→155→164→183→186→194→223→230`（10 次变化、严格递增），结果到达后预览被替换、正式回答卡片出现，且**最终回答文本与预览完全一致（均 230 字）**；截图 `test-artifacts/p5-shots/{15-streaming-preview,16-streaming-final}.png`（我这边模型不支持读图）。边界：模型通道不支持流式（或服务端拒绝 `stream` 参数）时会静默降级为整段显示，界面不会报错——这是有意的失败关闭，但企业自建网关是否支持 `stream` 需要在接入时确认；预览是模型 `answer` 字段的增量文本，工具调用轮若同时吐出 `answer` 字段，预览会先显示随后被正式结果替换；未做"按句子/段落合并"的渲染优化（仍按到达节奏刷新）。

- 2026-09-10：企业信息库第一批——文件存储底座 + 文件型条目/版本/适用范围（E-161，用户确认"先做骨架 + 本地磁盘 + 新增人群/部门/岗位维度"）。用户报告的现象与实测结论：`approvals` 与 `knowledge` 两个导航渲染**同一个组件**（`office-shell.tsx:635-636` → `GovernanceCenterView`，只有 `focus` 影响一行标题；`goals/insights/people` 同模式），而且真实 dev 库里 `documents/document_versions/knowledge_items/meeting_records/process_*/approvals/work_artifacts` **全部 0 行**（`GET /governance/workspace` 五项全 0、`/knowledge/search?q=数据安全` 返回 `[]`），因此**此前没有任何企业文件/文档真的能存进来**：`documents` 只有 `content text`（≤200k 的纯文本）、唯一写入口 `POST /knowledge/documents` 没有界面、"文件"只有一个 `storage_ref` 引用字段而平台自己**不存任何文件字节**（唯一的对象存储是 Pi 工作区的网关且默认 fail-closed）。本批改造：(1) **底座**——`0053` 迁移给 `documents` 加 `kind`（text/file）、`category`（制度/标准/协议/合同/模板/表单/证照/记录/其他）与 `summary`，给 `document_versions` 加 `file_name`/`media_type`/`size_bytes`/`storage_ref`、放开 `content` 非空约束并加 `document_versions_payload_check`（**要么有正文、要么有完整文件元数据**）；不新建并行表，文本知识与文件共用密级、适用范围、版本、替代关系与生效期。(2) **文件存储**——新增 `FileObjectStore` 抽象与 `LocalDiskFileStore`（内容寻址 `<root>/<tenantId>/<sha256[0:2]>/<sha256[2:4]>/<sha256>`，同一份内容只存一份，读取时校验引用形状/目录穿越与 sha256 摘要，摘要不符报 `FILE_OBJECT_CORRUPTED`），`createFileObjectStore` 是唯一的环境变量映射点：`NEXUS_FILE_STORAGE_ROOT` 未配置且 `NODE_ENV=production` 时直接抛 `FILE_STORAGE_NOT_CONFIGURED`（不允许悄悄写容器临时目录），`NEXUS_FILE_STORAGE_PROVIDER=memory` 仅限非生产；另提供 `InMemoryFileStore` 供测试。(3) **业务**——`KnowledgeService.publishFile` 发布/追加文件型版本（先写对象存储再写库，**写库失败即删除刚存的对象**，不留"有版本没字节"的坏数据）、`downloadFile` 走**与检索同一套可见性判定**并在返回前再校验摘要、`listLibrary`/`describeDocument` 支持按分类/类型/关键字/适用范围过滤；文件型条目**不产生 `knowledge_items`**（无正文可切块，也就不会被 Agent 检索）。(4) **适用范围**——`DocumentAccess` 新增 `allowedOrgUnitIds`/`allowedPositionNames`，与本人白名单/角色/项目并列：密级为机密/受限时必须命中其一；判据由请求侧 `resolveActorApplicability` 按当前主体解析（organization 模块新增只查自己的 `actorApplicability`，**不需要人事权限**），解析不到即视为不匹配（失败关闭）。(5) **HTTP**——`GET /knowledge/documents`（列表，服务端过滤）、`GET /knowledge/documents/:id`（详情+版本+`accessBasis`）、`POST /knowledge/documents/files`（multipart 上传）、`POST /knowledge/documents/:id/versions`（追加版本）、`GET /knowledge/documents/:id/versions/:version/content`（下载，带 `x-content-digest`、RFC 5987 中文文件名、`private, no-store`；越权与不存在统一 404）；`api-response` 为上传/存储错误补了 413/422/503/409 映射（排在通用 `_REQUIRED` 规则之前，否则会被误判成状态冲突）。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 154 文件 699 项通过（26 skipped）、生产构建 exit 0、`node .ai-team/check.mjs --base main` valid；新增单测 `tests/unit/file-store.test.ts` 8 项（内容寻址与去重、读写删、租户隔离、目录穿越/畸形引用拒绝、篡改后摘要不符、空文件拒绝、环境变量映射含生产失败关闭、内存实现同语义）与 `tests/unit/knowledge-library.test.ts` 10 项（上传落条目+版本并**不产生知识条目**、追加版本保留历史且旧版本可下载、大小/类型/文件名/空文件拒绝、写库失败回收对象、文本与文件不能互相追加版本、白名单与无关同事的可见性、部门/岗位适用与 `accessBasis`、内部条目不因缺适用范围而不可见、列表过滤、Owner/`document:admin` 才可追加）；集成 `tests/integration/knowledge-library-postgres.test.ts` 3 项（PGlite：0053 生效且 `content IS NULL` + 文件元数据完整 + 知识条目 0 行、文本型仍切块、数据库约束拒绝"既无正文又无文件元数据"的版本行）与 `tests/integration/knowledge-library-api.test.ts` 5 项（HTTP：上传→列表→详情→下载字节与摘要一致、追加版本后历史版本仍可下载、文本入口与 `kind=text` 过滤、超大 413/类型 422/缺文件 422/非 multipart、越权与不存在统一 404 且列表里不出现）；**真实 dev server + 真实本地 PostgreSQL + 真实磁盘复核**（`test-artifacts/verify-file-library.ts`）21 项断言全通过：`0053` applied、真实 multipart 上传返回 201、`kind=file/category=agreement`、适用岗位 `["HRBP","招聘专员"]` 落库、版本行 `content IS NULL` 且带 `storage_ref`、磁盘对象按摘要路径真实存在且字节一致、列表/详情/下载（字节一致 + `x-content-digest` 一致）、非授权身份 404，复核数据与磁盘对象均已清理。边界：本批只做**后端骨架**——三个入口仍是同一个组件、信息库还没有上传/列表/下载界面（E-162 交付）；文件内容不进 Agent 检索（将来做文本抽取再加索引通道）；适用范围按**岗位名**匹配（成员目录目前只暴露岗位名，等岗位 ID 在更广读模型里稳定后再升级并回填历史条目）；未做下载短时授权链接（当前是每次请求都重新过权限）、未做杀毒扫描与预览缩略图、未做"与人绑定/到期提醒/模板生成"（批次 2 计划）。

- 2026-09-10：企业信息库第二批——把三个页面拆开 + 信息库界面（E-162）。用户的原话是"知识与会议和智能审批似乎一模一样…信息单独摘出来"。根因已在 E-161 查清（两个导航渲染同一个 `GovernanceCenterView`），本批交付界面：新增 `components/approval-workbench-view.tsx`（只做审批：待我审批 + 运行中流程 + Agent 预审 + 审批纪律）、`components/meeting-center-view.tsx`（会议列表 + 纪要确认闭环 + 会议准备包）与 `components/library-center-view.tsx`（企业信息库），删除共用的 `governance-center-view.tsx`；导航新增「会议纪要」并把「知识与会议」改名为「企业信息库」，页面标题与导航名对齐（原导航"知识与会议"点进去标题却是"企业知识与依据"）。信息库页的能力：按分类分组的条目列表（标题/密级/摘要/适用对象/`Agent 可检索` 标记）、分类下拉与"只看对我适用的"筛选、关键字筛选与权限感知检索共用输入、**上传文件对话框**（`type=file` + 标题/分类/密级/摘要/生效失效时间/适用岗位与部门/指定成员，部门与成员从成员目录下拉多选）、**发布文本对话框**（制度条文，走原 `POST /knowledge/documents`，提示会切块进检索）、详情面板（可见依据 `accessBasis` 的中文解释、适用范围四格、版本历史表：版本/文件名/大小/生效/失效/发布人/下载）、条目上直接「下载」当前版本。文档新增 `docs/25-enterprise-library.md`：三页分工 + **"企业各类信息存在哪里"的存放总览表**（文本知识/文件/会议/审批/组织/任务/证据/审计各自落在哪张表、谁写、谁可见）+ 条目模型（分类/密级/适用范围/版本与生效期/摘要）+ 边界（25MB 与 MIME 白名单、父子一致性、Owner 才能追加版本、越权与不存在同 404、下载不缓存、摘要校验、不留坏数据、没有硬删除）+ 后续批次（与人绑定/到期提醒/模板生成/文件抽取进检索/杀毒与短时授权）。验证：`npm run typecheck` exit 0、`npm run lint` exit 0（0 errors/0 warnings）、全量 154 文件 699 项通过（26 skipped，测试数不变——本批是界面重组）、生产构建 exit 0、`node .ai-team/check.mjs --base main` valid；**真实浏览器验收**（`test-artifacts/browser-check-library.mjs`，Chromium + 原生 CDP，含 `DOM.setFileInputFiles` 真实选中本地文件）**24 项断言全通过**：导航里三个入口都在、三页正文互不相同（`Set(...).size === 3`）且各自不再混入对方的标志性内容（审批页无「上传文件」、会议页无「审批工作台」、信息库页无「会议确认闭环」）；点「上传文件」打开表单 → 真实选中本地 PDF（`files.length === 1`）→ 填标题/摘要/分类=协议/密级=机密/适用岗位 → 提交 → 列表出现该条目 → 按「协议」筛选后仍在且只剩协议分组 → 点「详情」看到"可见依据"与适用岗位 `HRBP` 与版本历史表 → 条目上的下载链接返回 200、字节大小与上传一致（69B）、带 64 位 `x-content-digest`；截图 `test-artifacts/p5-shots/{17-library-upload,18-library-list,19-library-detail}.png`（我这边模型不支持读图）；验收产生的条目与磁盘对象已用 `test-artifacts/cleanup-library-ui.ts` 清理（documents 1 / versions 1 / objects 1，残留 0）。边界：三个页面仍是"一个信息库 + 一个审批 + 一个会议"的第一版界面，没有做文件预览（PDF 内嵌预览）、拖拽上传、批量上传、编辑已有条目元数据（只能追加版本）、归档操作与"文件到人"的直接绑定；截图未经我视觉复核（模型不支持读图）；上述后续项与 E-161 的边界一致。

